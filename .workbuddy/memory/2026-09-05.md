# 2026-09-05 工作日志

## PR #6（V2 Step 1 正确性修复）评审回应

- **PR #6 已 squash 合并为 `61e3dac`**（评审结论 Approve，8 个 CI job 全绿）。
  用户授权合并，未直推 main。
- GLM-5.3 给出 9 条不阻塞意见（2 条 P2 + 7 条 P3/nit）。
- **用户决策：评审意见先发 Issue，实施后置**，不立即改代码。
  → 建立 **Issue #7**「【V2 Step 1 收尾】PR #6 评审意见（9 条 P2/P3）跟进」，
  含逐条核实结论、建议处理、排期建议（先做 1/3/7/9；随 S1-10 做 2；随 Step 5 做 6；4/5 随手；8 待用户决策）。
  → 在 PR #6 上发了逐条回应评论。

## 已实现的部分（P2-1 前半，尚未发 PR）

分支 `fix/v2-step1-review-followup`，提交 `5386834`，`cargo test --workspace` 全绿：

- 新增 `Index::with_max_values_per_field(n)`：把字段索引基数阈值从「只有 `FieldIndex` 能配」
  下沉到 `Index`。原文档写「可配置，见 `FieldIndex::with_max_values`」是撒谎——调用方无从设置。
  注：阈值**不随快照持久化**（字段索引不入快照，`import` 用默认阈值重建），已在 rustdoc 写明。
- 把名实不符的 `filter.rs::降级字段自动回落全扫` 拆成两个用例：
  - `未降级字段走索引路径且与全扫一致`（原用例改名 + 修正误导注释）
  - `降级字段回落到全扫且结果与oracle一致`（新增）：阈值 8 / 40 篇，`uuid` 与毫秒 `ts` 必然降级；
    覆盖降级后**被跳过**的值（`u8/u20/u30/u39`，索引里没键只能靠回退）、高基数 Range、
    与未降级字段的 `And` 组合、谓词层 `allowed_count` 一致性；并断言 `tag` 未降级钉住「降级按字段隔离」。

## 评审里的新事实（值得记住）

- **数值字段撞基数上限比预想快一倍**：terms 键与 numbers 键**合计**计入 `value_count`，
  所以毫秒时间戳约 **512 篇**就降级，不是 1024。Q-I1 对高基数 Range 场景整体失效
  （退回 O(N) 全扫），这是真实主场景，V2.1 讨论 prefilter 时应以它为第一用例。
- 三条待实施的正确修法（已记 Issue #7）：
  - `Metrics::vector_shortfall` → `min(candidate_k, allowed) - len`（T12 打印口径同步）
  - `query_has_hits` 是 **BM25 词典探针**，Vector 模式下会误报 `AllTermsUnmatched`（文档标边界，暂不改语义）
  - `search_path_b` 的 `filter=None` 分支不可达 → 用 `match filter { Some(f) if ... => path_b(f), other => path_a(other) }`
    重构掉，让不可达分支在类型层面消失（注释会过期，类型不会）

## 其他

- `MEMORY.md` 超限（16.4KB）触发系统要求，已精简重排至 12.6KB：
  V2 Step 1 的详细推导收敛为「结论 + 指向 `v2-step1-design.md`」，保留不可从文档推导的坑。
- **工具坑**：`gh issue create --body "..."` 在 zsh 下**反引号会被当命令替换**，
  且含 `()` 会 parse error。含 Markdown 反引号的 body 一律写文件后用 `--body-file`。
