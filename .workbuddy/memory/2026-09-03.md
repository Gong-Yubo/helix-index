# 2026-09-03 工作日志

## P4 完成（持久化、增量写入与过滤，T4-01~T4-09）

**实测结果**：71 单测 + 4 ignore + 2 集成测试全过；快照 77.8KB（30 篇含向量）build 996ms / 加载 <10ms；损坏快照四类全部正确报错；8 线程并发检索确定。fmt/lint/test/deny 全绿。

**产出**：storage（codec + snapshot）、content_hash（xxh64）+ 幂等 upsert、HnswRsIndex（hnsw_rs 封装）、query/filter（位图过滤 + FilteredOut）、CLI `build --output --vectors` / `search --index` / `--filter`、tests/integration.rs。

## 向量索引 A/B 定案（T4-06a，release，1 万条×512 维）

| 指标 | hnsw_rs（原生增量） | instant-distance（一次性） |
| --- | --- | --- |
| 增量 | ✅ P50=1537µs/条 | ❌ 全量重建 31032ms |
| Top-10 vs 暴力 | 0.970 | 0.970 |

**结论：hnsw_rs 胜出**。按计划删除 delta 区设计，生产路径（CLI/集成测试）已切换到 HnswRsIndex。HnswIndex（instant-distance）保留作对照。

## 执行中发现的关键问题（记入 p4-design.md 13.3）

1. **bincode 2 不支持 serde_json::Value**（serialize_any → AnyNotSupported）→ 快照用 DocumentDto 存 JSON 字符串；bincode 2 需显式开 `serde` feature
2. **hnsw_rs 的 DistDot 在 aarch64 有 `assert!(dot<=1)` 浮点断言**（自匹配 dot=1.0000002 会 panic）→ 自定义 DistDotClamped（clamp [-1,1]）
3. **crate 名是 `hnsw_rs`（下划线）**，crates.io 上没有 hnsw-rs
4. clap 短参冲突：--input/--index 都默认拿 -i，debug 构建直接 panic

## 其他要点

- NFR-04 冷启动：MVP 规模（≤200 篇）<1s ✅；万级重建 ~15s 不满足秒级（如实记录，届时用 hnswio/rkyv，不过度设计）
- 快照策略 D1（存原始数据+加载重建）落地验证：round-trip 三模式检索完全一致
- term_dict 按 TermId 升序导出（防漂移）+ content_hashes 按 hash 升序（确定性）
- T4-05（删除回滚）P1 已完成，本阶段只补测试

## 待办
- P5：语料、评测与调参（T5-01~T5-09）

## P5 设计文档 v1.1 → v1.2（用户要求：评测语料改用互联网开源数据）

- **v1.2 核心变更**：评测数据源从"手写 120 篇 + 自标注 40 条"切换为 **T2Ranking**（THUIR，SIGIR 2023）
- 选型核查（官方仓库实查）：**T2Ranking 胜出**——Apache-2.0（数据明示）、HF `THUIR/T2Ranking` 直下无注册墙、dev 24,832 query 带 4 级专业标注（TREC qrels 格式，每对 ≥3 人多数票）、论文基线 BM25 MRR@10=0.359 可做外部锚点。DuReader-retrieval 落选：需 aistudio 报名注册下载 + 数据许可未明示 + 809 万段落过重。mMARCO-zh（机翻）、Multi-CPR（假负例）落选
- 设计变化：标注者偏差（v1.1 最大风险）整体消除；相关性二元 → 4 级分级 NDCG（gain=2^g−1，单测锚点 0.90495）；合成压测语料废弃（12K 真实段落 ≈ 1 万 chunk 目标规模，一份数据两种口径）；新增 `t2_prep.rs` 转换器（固定种子，qrels 池内零假负例 + 哈希均匀负例补足 + 词汇重合度启发式四桶各 80 条）
- bench 变化：+`--rel-threshold`，删 `--pool`；320 query × 20 reps
- 决策点 9 个（D1 选型 / D3 分级 / D5 转换产物 commit 入库 ~10MB / D8 移除 instant-distance 等）

## P5 设计文档完成 v1.1（待用户确认后已升级，见上节）

- 产出 `docs/devel/p5-design.md` **v1.1**（发现仓库里已有一版 v1.0 待确认稿，基于代码实况修订升级而非重写）
- v1.0 的实质缺陷（v1.1 修正，见附录 C）：① 用 80 篇小语料测 NFR-02/03（目标值明确是 @1 万 chunk）→ 引入"两条评测线"：效果线（人工语料 120 篇）与规模线（合成 1 万 chunk，gitignore）；② 遗漏 ADR-007 的 tantivy P5 基线义务（NDCG@10 相对差 ≤5%）→ 补 `tests/eval_tantivy.rs`；③ 遗漏 R7 的 RRF k 敏感性 → 补 k∈{20,60,100}；④ `--k1/--b` 无落地路径（Searcher 内部 new Bm25Retriever 用默认参数）→ 补 `Searcher::with_bm25_params`
- 其他关键设计：指标手算基准单测（NDCG 二元 0.86038 锚点）；T5-04/05 顺序对调（终版对照必须用定稿参数）；bench 延迟禁用 CachedEmbedder + reps 20（800 样本）；T5-09 charabia 因 thirdparty.md(0.10 可裁剪) 与 v1.0 附录B(0.9 强依赖 lindera) 调研矛盾，改为三步验证裁决（dry-run 依赖树 / +1.90 check / make deny）
- 待用户确认 9 个决策点（D1 语料 120 篇 / D4 相关性维持二元 / D7 万级 NFR-04 如实标注 / D8 移除 instant-distance 等）
- docs/README.md 索引已同步（p4 状态更新 + p5 条目新增）

## P5 v1.2（T2Ranking）数据源变更同步刷新至全部相关文档

- `plan.md` → v1.2：头部变更行；现状表（demo 语料已备 30 篇定稿 / 评测数据集改 T2Ranking 装配）；并行工作流图；T1-14 描述（不再"50~200 篇采集"）；附录验证命令 `--queries data/t2-queries.jsonl`；进度表 T5-01/02 任务名
- `requirements-spec.md` → v1.2：**9.4 评测数据集要求整节重写**（T2Ranking 选型依据 + t2_prep 装配要求表 + 装配诚信约束）；9.2 指标定义（NDCG 分级 gain=2^rel−1、Recall/MRR 阈值化）；8.2 外部依赖两行；8.3 风险 P1 已缓解 / P2 已消除 / **新增 P6 评测子集装配偏差**；9.5 披露要求；附录 C v1.2 行
- `architecture-design.md` → v1.3：目录树 data/ 注释；10.2 CLI 示例文件名；11 章 P5 门槛行；14.2 v1.3 变更行。遵守"不复制需求定义、只引用编号"规则
- `thirdparty.md` → v1.1：5.2 非 crate 资产表新增 **T2Ranking（Apache-2.0）** 含引用义务（SIGIR 2023）
- `p1-design.md`（历史文档，加注不改写）：3 处"P5 需 50~200 篇真实语料"前瞻引用加"已被 2026-09-03 决策取代"注记，D6 决策点同
- `docs/README.md`：p5-design 条目描述更新；风险编号范围 P1~P4 → P1~P6
- 验证：全局 grep 无残留旧引用（data/queries.jsonl、30~50 条、50~200 篇、标注后交叉复核均清零，仅变更记录中的历史描述合法保留）
- 教训：macOS BSD grep 的多分支 `\|` 交替模式在本环境会静默返回空，必须用 `grep -E`

## P5 设计外部评审（DeepSeek）回应：p5-design.md v1.2 → v1.3

- 评审 16 条意见**全部核实并接受**（P0 两条均经源码验证属实）：
  - ① hnsw_rs 0.3.4 `Hnsw::new` 用 `StdRng::from_os_rng()` 无 seed API（registry 源码 hnsw.rs:328/343 已核）→ 图跨进程不确定；快照只存原始向量、图在 load 时重建（cli/main.rs rebuild_vector_index）。新增 R-P5-13 + `--runs 3` 抖动披露 + D7 重开 hnswio 条件分支（`Graph::dump`/`HnswIo::load_hnsw` 已核实存在，支持 mmap，图落盘可同时解 NFR-04 与确定性）
  - ② ef_construction=300/ef_search=200 硬编码自 P2 从未校准；P4 召回 0.970 测于随机向量 → 新增 8.6 向量路诊断（`--vector-index brute` 零内核改动——`Searcher::with_vector` 收 `&dyn VectorIndex`、BruteForceIndex 已实现 trait；ef_search {100,200,400} 校准；`HnswRsIndex::with_ef_search` 注入口）
  - P1 四条：符号检验（二项精确，单测锚点 18 对正 14 → 单侧 p≈0.0154）；网格表补 Recall/MRR 列+定稿不退化；主表 thr=1/thr=2 双列；段落→chunk 单 chunk 断言（防双计）
  - P2 勘误：样本数 6,000→6,400（P99 第 6,336 个）；NFR-03 口径=embed+save（build 不建图已核）；--json 提为验收必需；报告记录 git commit/HF revision/模型 id；o* 分布先行定阈值；桶 <30 条标"探索性"；抽样算法精确化（7K 最小堆或两遍，禁 Bernoulli）；Analyzer"双实现"措辞诚实化
- 新增决策点 **D10**（向量诊断与统计检验纳入范围）；D7 加 hnswio 条件分支；D8 与 R-P5-13 同批决策、vector_ab 改真实语料
- 教训：本机 BSD grep 无 -E 时 `\|` 交替模式静默返回空（同日第二次踩），一律用 grep -E

## P5 实施：bench 模块 + CLI + 转换器落地（T5-03a / T5-03b / T5-01-02a 完成）

- 确认 p5-design.md v1.3 后开工。四笔提交（a10475a / a0d8c31 / 37a908a / 2c422b1）：
  - **T5-03a** `core::bench`：metrics.rs（graded NDCG gain=2^g-1、阈值化 Recall/MRR、nearest-rank 分位数、二项精确符号检验 log2 空间，17 单测含手算锚点）+ judgments.rs（坏行带行号报错、grade 0..=3 校验、source 覆盖校验）；Searcher::with_bm25_params 与 HnswRsIndex::with_ef_search 双注入口；Error::InvalidInput 变体
  - **T5-01/02a** `t2_prep.rs`（example）：固定种子（SEED_QID/SEED_NEG/SEED_BUCKET）确定性装配、单遍流式扫 collection + 7K 最小堆精确取秩、MixedAnalyzer 分桶（o* 直方图）、段落→chunk 断言
  - **T5-03b** `cli bench` 子命令：三路对照双阈值列、分桶表（<30 标探索性）、符号检验、延迟 P50/P99、16 格 k1×b 网格、--vector-index brute 诊断开关、--runs N 图重建抖动披露、--json 输出
  - **docs**：p5-design.md v1.3 入库；.gitignore 忽略 /data/t2ranking/（3.4GB 原始数据，2,303,643 段落 + 表头）
- make fmt / lint / test（91 过 4 ignore）/ deny 全绿
- 踩坑记录：
  - clippy print_literal 与 to_string_in_format_args 链式互斥——正确解法是把字面量直接并入格式串（去掉占位符与参数），不要加 .to_string()
  - Edit 工具在本会话多次"报成功但未落盘"——改用 python3 脚本幂等替换 + assert 验证
  - tempfile 测试并行冲突 → 每测试唯一文件名
  - cargo run 后台任务的 cwd 是项目根，--t2ranking 相对路径曾导致下载/输出错位，一律用绝对路径
- collection.tsv 下载完成（3.4GB，24 分钟）；t2_prep 已在后台运行（task A7DraD），产出 t2-corpus.jsonl / t2-queries.jsonl 后待人工审阅分布 + BM25 sanity 对照论文锚点 MRR@10≈0.359

## P5 完成：语料装配、三路对照、调参、NFR、README、清理（T5-01/02b ~ T5-10）

- 剩余提交：ca60306（语料装配+sanity）/ fb227a0（三路+定稿）/ 6707f03（charabia）/ dae7c63（NFR+报告）/ 64c5c6c（README）/ cba12a5（D8）/ 2045a9f（docs收尾）
- **T5-01/02b**：t2_prep 两 panic 修复（o*=1.0 直方图越界、负例文本未并入）；装配 320 query/12K 段落，qrels 32562/32562 全命中；段落→chunk 断言不成立（4638/12000 多 chunk）→ 落实"每段落强制单 chunk"（Chunker::new(200_000,0)）；BM25 sanity MRR@10(thr1)=0.6394（外部锚点量级 0.3~0.8 内 ✅）
- **T5-05**：BM25 网格 16 格最优 k1=1.5/b=0.75（NDCG 0.4491，三项不退化）写入默认；ef_search 100/200/400 校准（近似误差<0.002）+ brute 基线隔离；RRF k 20/60/100 波动<0.005 维持 60；weights 诊断 (1,1.5) 甜点 0.5221 → RrfFusion::default() 定稿
- **T5-04 三路对照**（runs=3+符号检验+分桶）：hybrid MRR@10(1)=0.6922、Recall@10(1)=0.6829 全场最高，NDCG 0.5183 vs vector 0.5249（p=0.385 不显著，**未超越如实记录**）；exact/natural/mixed 桶 hybrid 显著优于 bm25（p≤0.036），paraphrase 桶 vector 碾压（0.4294 vs 0.3799）；tantivy 基线相对差 0.31%（阈值 5%）+ Top-10 重叠 97%
- **T5-09 charabia**：三步验证过（无 lindera/MSRV/deny）；实测全面落后自研（NDCG -5.9%），根因 kvariants 简→繁归一化+单字 CJK 保留 → 保留自研链
- **T5-06 NFR 实测**：NFR-02 延迟达标（bm25 P99 3.90/vector 8.37/hybrid 8.70ms）；**NFR-03 构建未达标**（12K chunk embed 233.7s+落盘 236.7s>120s，ort CPU 推理 ~51 条/s）；NFR-04 快照加载 53~107ms ✅ 但 HNSW 图重建 11.57s（D7）；NFR-05 向量 24.6MB 吻合理论 20MB、peak RSS 383MB
- **T5-07**：README（定位/快速上手/架构/评测摘要/数据引用/局限）+ search_basic.rs 示例（30 文档/2ms）+ demo 语料补 metadata.topic + 评测语料入库（.gitignore 显式放行）
- **T5-08/T5-10（D8）**：移除 instant-distance（hnsw.rs + ImmutableIndex + 依赖）；vector_ab.rs 改写真实语料对照（2000 段×100 query，Top-10 重合率 0.994）
- 阶段收尾：make fmt/lint/test（90 过）/deny 全绿 + charabia feature 复验

**关键坑（新增）**：
- `cargo run` 的 cwd 是 workspace 根（非 crate 目录）；`cargo test` 的 cwd 是 crate 目录。示例/测试定位数据文件要用 `env!("CARGO_MANIFEST_DIR")` 或相对仓库根 `../../data/`
- charabia 的 `chinese` feature 含 kvariants 简→繁归一化（"硕士"→"碩士"），这是 Meilisearch 简繁互搜设计，但简体语料上是负收益
- embed 批量吞吐 ~50 条/s（19.5ms/条），远慢于 P0 单条 1.58ms 直觉——ort CPU 推理 batch 256 是 NFR-03 未达标的根因，非超线性
- 前台 Bash 跑 build --vectors 会被 SIGKILL（sandbox 内存限制），后台 run_in_background 不受限

**P5 结论（交 P6 残留）**：D7 图持久化（hnswio）、假负例敏感度对照、paraphrase 桶 RRF 弱路稀释（自适应融合/reranker 兜底）、NFR-03 embed 加速（并行/量化/GPU/增量）
