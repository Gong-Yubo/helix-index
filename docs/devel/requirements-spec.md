# HelixIndex 需求分析说明书

### —— 面向 Agent 场景的通用检索引擎内核

| 项目   | 内容                   |
| ---- | -------------------- |
| 文档版本 | v1.20                |
| 创建日期 | 2026-09-02           |
| 状态   | **2026-09-16 V2 Step 8（读写并发）详细设计已出、待评审**（`v2-step8-design.md` **v0.1**）：① **NFR-14 由「草案」定口径** —— 可见性 = **`commit()` 后立即可查**（对齐 NFR-11）；读延迟 = **②a 相对判据**（**合并期 ÷ 静止期**的端到端 `took` P99 **≤ 1.2（拟）**，同机 / 同图 / 同 query 集）+ **②b 绝对护栏**（合并期 P99 **仍在 NFR-02 预算内**）；③ 不得以「重建索引中」为由拒绝查询 ⇒ 并补「**库内不存在该类错误码**」的可证伪断言（S8-T10 以 `grep` 断 `Error` 枚举无该变体）。② **NFR-11 补口径与拟值** —— 写延迟口径由「从累计 embed 耗时**推算**」（`eval-report.md` §8.11 的 1.14 s/批）改为 **`commit()` 的端到端 **直接计时**；**拟值 `P50 ≤ 1.0s / P99 ≤ 2.0s`**（`batch_size = 64`、CPU/FP32、**不含首次模型下载与加载**），**待 S8-08 标定后定稿**（同 NFR-13 / NFR-10 先例）。③ **FR-17 §5.3.5 的验收措辞**由「**写入后**立即可查」改写为「**`commit()` 后**立即可查」（**只改措辞、不改语义**，保留原措辞引文；「读能看到未 commit 的写」**明确不做**，取形见 `plan-v2.md` §4.0 **H5**）。④ **NFR-10 ③** 补设计锚点（复审触发条件 = **T7-26** 会话池落地时；挂起的 Q3 已**改述为 Q3′**：不同 `intra_threads` / 实例数是否改变 `embed_query` 向量**数值** —— ⚠️ fastembed **无 `sessions` 参数**，原措辞在实现面上不存在）。⚠️ **本版不修订任何既有指标数值**（NFR-02 ~ 13 的阈值 / 预算 / 时限一字未动）、**不新增 / 删除 FR 或 NFR**；架构侧随之 **v1.19**（**§14.6 新增 R49 ~ R54**）。**2026-09-16 V2.1 计划修订**：新增 **FR-34 / FR-35 / NFR-14 / NFR-15 草案（待评审，数值一律留白）**、**FR-18 状态同步为「已实现并收尾」**、**FR-17 补 Step 8 的任务号与 NFR-14**；⚠️ **不修订任何既有指标数值**。已定稿（**2026-09-16 V2 Step 7 收尾（S7-05，`v2-step7-design.md` v0.3）**：**NFR-12 定稿**（原「≤500ms（拟）」废弃 ⇒ **双口径**：CPU/FP32 上界 `R=20` 下 P50 ≤ 8.8s / P99 ≤ 10.9s、**仅精排开启时适用**；GPU/量化标「拟」待测）+ **NFR-05 回填精排内存实测**（增量 **2.64 GiB / 7.71×**）+ **NFR-06 更正精排 tie-break 机制句**（「保持输入序」→ **`chunk_id` 升序**，与 D-S7-07 一致）并补 S7-T9 读数 + **FR-18 验收注记同步**；⚠️ 无预算数值增减、不增删 FR/NFR。此前 **2026-09-14 V2 Step 7 设计评审收口（`v2-step7-design.md` v0.2）**：评审 4 项拍板（**D-S7-04 / D-S7-05 / D-S7-01 / D-S7-08**）**全部同意**、**F1~F6 全部采纳**（设计 v0.1 → v0.2）；**需求面只有两处事实性更正、无预算/结构变更**：① **NFR-12 的标定语料**由「（10 万级）」更正为 **T2Ranking 12K / 320 query**（「10 万级」是 NFR-13 / S5-04 先例的措辞残留）；② **NFR-12 的「≤ 500ms（拟）」补出处与联动** —— 该拟值 = **`R = 10` 档的量级上界**，而 `R = 20` 粗估 ≈1s 会超它 ⇒ **阈值与默认 `R` 必须联动、由 S7-04 一并回填**，**禁止各自独立引用**（评审 **F4**）。此前 2026-09-14 V2 Step 7 详细设计（`v2-step7-design.md` v0.1，待评审）：**NFR-12 补三部分口径**（**延迟判据 = 双列（均标「拟」）**：**端到端 `took` P99** + **精排段 `rerank_elapsed` P99 ≤ 500ms（拟）**——⚠️ **该拟值对应 `R = 10` 档的量级上界、与默认 `R` 联动**（设计 §4.2.2 补记），独立于 NFR-02；**质量判据** = hybrid MRR@10(1) 相对**同图**基线可测提升；**可观测** = `Metrics.rerank_window` / `rerank_elapsed` + `Explain.rerank_score.is_some()`）+ **NFR-05 补限定词「不含精排器」**（`model.onnx.data` 实测 **≈2,187MB（FP32）** 常驻，精排内存**单独立为架构 R44**、数值待 S7-04 回填）+ **NFR-06 补精排确定性注记**+ **NFR-07 补精排可观测字段（`Metrics.rerank_window` / `rerank_elapsed`）** + **FR-18 补设计锚点与模型来源更正**（`rozgo/bge-reranker-v2-m3`，**非** BAAI 官方）；⚠️ **NFR-12 的 A/B 必须在「同一张冻结图」上做**（P5 锚点 0.6922 自身带 ~0.6% 图漂移）；⚠️ **本版不新增任何 FR**。此前 **2026-09-13 V2 Step 6 收尾（S6-10）**：① **NFR-10 定稿 + 收窄** —— S6-08 实测 `QPS(4)/QPS(1)` = bm25 **2.98×** ✅ / hybrid 1.63× / vector 1.88×（`eval-report.md` §8.12 / **§8.13**）⇒ **×2.5 由「拟」定稿**；裁定（三条并记）：**② 的适用范围收窄为「不含查询侧编码的读路径」**、**NFR-10 整体保持「打开」**（编码侧并发登记为架构 **R43**，**复审触发条件 = 查询侧会话池落地时**，届时须先答挂起的 Q3）、并**更正措辞**（「读路径零共享可变状态」只对**索引读路径**成立，评审 P3-8）；② **NFR-05** 补口径限定词「**（检索路径，batch 1）**」（建库路径内存**单独立为架构 R41**，预算按 `batch × 序列长度` 给、数值待定）；③ **NFR-03 ②** 增量口径实测 **23.705s < 27.973s** ✅（余量 15.3%）。此前 2026-09-11 V2 Step 6 评审收口（PR #38）：NFR-10 口径改为方案 A（由「无法判定」变「可判定」）、NFR-03 ② 补单位注记**——评审对设计文档 7 条意见（F1~F7）全部采纳、并拍板 Q4~Q7：NFR-10 = 「`--threads 4` QPS ≥ `--threads 1` × **2.5**（逐位一致为**前置条件**；测量口径 = 预热丢弃首轮 + 顺序交错 + 报告运行范围）」，**数值标「拟」、待 S6-08 实测定稿**（同 Step 5 的 NFR-13 先例）；**NFR-03 ② 的 10% = 文档数**（chunk 数只作参考、不进判据）。此前 2026-09-11 V2 Step 6 详细设计回写：需求侧无变化，本行仅为版本面同步**——NFR-03 ② / NFR-05 / NFR-10 / NFR-11 的备注补「设计已出（`v2-step6-design.md` v0.1）」与设计锚点；**NFR-10 数值目标缺失**与 **NFR-03 ② 的 10% 单位**登记为设计文档 §9.2 Q4 / Q5，待评审拍板后另行修订。此前 2026-09-10 NFR-13 标定定稿：**NFR-13 由「拟 ≤20ms」改为双口径**——常规档位 ≤20ms / 降级字段档位 ≤35ms，数值来自 V2 Step 5 的 S5-04 标定（10 万级 A/B 实测，`eval-report.md` §8.9）；**这是 Step 5 唯一触碰 FR/NFR 的改动**。此前 2026-09-10 口径精确化：需求侧无变化，本行仅为版本面同步——`vector_shortfall` 的「精确路径恒为 0」修正为**条件式**（`allowed` 来自 `Index`、扫描枚举的是图中的点），仅触及 `architecture-design.md` §5.4 / §8.3 与实现侧措辞；2026-09-10 V2 Step 5 设计回写：NFR-13 补「端到端 P99（含过滤求值）」口径 + 拟 ≤20ms + 成本口径修正、NFR-07 补 Step 5 的可观测落地路径；此前 2026-09-08 状态校正：NFR-04 行改「已达标」、NFR-03 补首次全量实测落点；2026-09-07 V2 Step 3 落地校准：FR-31 原子快照验收注记落地；V2 计划复审回写：NFR-03 拆双口径、新增 NFR-13、NFR-11 口径修订、FR-31 升 Must、步骤编号按重排更新） |
| 项目定位 | 通用检索内核（场景层后接），单机 MVP |
| 技术栈  | Rust                 |
| 文件名   | `requirements-spec.md` |
| 配套文档 | `architecture-design.md`（架构设计说明书） |

---

## 1. 文档信息

### 1.1 版本与状态

| 版本   | 日期         | 状态  | 说明                          |
| ---- | ---------- | --- | --------------------------- |
| v1.0 | 2026-09-02 | 待评审 | 由 `archive/requirements-and-design_v1.0.md` v1.0 拆分重构而来 |
| v1.1 | 2026-09-02 | 已定稿 | 依据 `thirdparty.md` 回写：MSRV 1.90、NFR-09 许可合规 |
| v1.2 | 2026-09-03 | 已定稿 | 依据 `p5-design.md` 切换评测数据源 T2Ranking |
| v1.3 | 2026-09-03 | 已定稿 | P5 实测校准 NFR-02~05 |
| v1.4 | 2026-09-04 | 已定稿 | V2 需求回写：FR-26~33 / NFR-10~12、FR-15/16/17/18 修订（依据 `plan-v2.md`） |
| v1.5 | 2026-09-05 | 已定稿 | V2 Step 1 落地后回写：① FR-26/FR-27 描述改为**实现中立**（原写「`search_filter` 存活位图」属实现细节，且与最终定案的双路径不符，需求文档不该规定实现）；② NFR-02 补 V2 Step 1 重测（1 万 / 10 万级合成 fixture，无过滤路径未退化） |
| v1.6 | 2026-09-06 | 已定稿 | V2 Step 2（图持久化，ADR-A 方案 C）落地后回写：① **FR-29 落定**（实测数据入库）；② **NFR-04 刷新**——口径改为「完整冷启动」并补实测（≈100ms ✅）；③ **NFR-06 口径细化**——删除过时论据「HNSW seed 固定」（`StdRng::from_os_rng()` 无 seed API），改为「同一快照连续加载两次逐位一致」；④ **NFR-07 扩「降级可观测」** |
| v1.7 | 2026-09-07 | 已定稿 | **V2 计划复审回写 + 步骤重排**（复审结论已并入 `plan-v2.md`，索引见其 §附-3）：① **NFR-03 拆「首次全量 / 增量追加」双口径**（D-J8：原「1 万 chunk <120s」在不量化/不换模型前提下无已识别路径可达）；② **新增 NFR-13 低选择度过滤延迟**（D-J9：把 10 万级低选择度场景纳入口径，此前它落在所有 NFR 之外）；③ **NFR-11 口径修订**——`add` 后需 `commit()` 才可见（`search/index.rs:5-6`），原「增量写入后立即可查」与实现语义不符；④ **FR-31 优先级 Should → Must**（Q-C3 定级为「高（正确性）」）；⑤ NFR-10/11/12 与 FR-17 的「对应步骤」按 2026-09-07 重排更新（`plan-v2.md` §附-2 映射表） |
| v1.8 | 2026-09-07 | 已定稿 | **V2 Step 3（原子快照）落地校准**（依据 `v2-step3-design.md` v0.3）：FR-31 验收注记落地——「临时文件 + fsync + rename 原子替换」由 `storage/atomic.rs` 通用原语实现，崩溃不变式（save 序列任一注入点崩溃后 load 得旧或新，绝不 `SnapshotCorrupted`）经故障注入测试证明，fsync 代价实测 +0.027~0.035s（`eval-report.md` §8.7） |
| v1.9 | 2026-09-08 | 已定稿 | **状态校正（无指标变化）**：NFR-04 行由「未达秒级，V2.0 解决」的将来时改为「✅ 已达标」（V2 Step 2 实测 ≈100ms，详注保留在 §6.1 下方注记）；NFR-03 补首次全量口径实测落点（P5 236.7s / P6 225.5s，均 < 240s ✅） |
| v1.10 | 2026-09-10 | 已定稿 | **① V2 Step 4 落地回写补记**（`#32` / `9e2211e` 已落地但漏记版本行）：**FR-30 墓碑物理回收标记「✅ 已实现」**——`compact` / `compact_and_save`（唯一落盘入口）+ `tombstone_stats` / `CompactionReport` + CLI `helix compact`；10K churn 0.3×5 实测 graph+data **84.6→33.7MB**；⚠️ **ID 会重编号**，跨 compact 的持久引用改用 `source` / `content_hash`（架构 §7.5.3 / R27）。**② V2 Step 5 设计回写**（依据 `v2-step5-design.md` v0.3，D-S5-01~09 全部拍板）：**NFR-13 补口径**——**端到端 P99（含过滤求值）**、拟 ≤20ms（数值待 S5-04 标定）；**成本口径修正**——D-J9 的「约 0.05ms」只覆盖「对候选算距离」一段，**不含 `O(N)` 遍历定位候选**（1~10ms，方案 A 的主要成本）；**NFR-07 补口径落地路径**——`Metrics` 进 `SearchResponse` + per-lane 耗时 + `vector_route` + 修两条早退 `took` 恒 0 |
| v1.11 | 2026-09-10 | 已定稿 | **口径精确化：需求侧无变化，本行仅为版本面同步**（**补记**：此前只落在文末附录 C，§1.1 本表漏行，本次一并补齐）——`architecture-design.md` §5.4 / §8.3 把「`vector_shortfall` 在精确路径下恒为 0」修正为**条件式**（`allowed` 来自 `Index`、扫描枚举的是**图里的点**，两独立来源）；口径本身不受影响 |
| v1.12 | 2026-09-10 | 已定稿 | **NFR-13 标定定稿（V2 Step 5 / S5-04；补记**：同 v1.11，此前只落附录 C）：预算由「拟 ≤20ms」单值改为**双口径**——常规档位（字段索引命中）**≤ 20ms**（10 万级实测最差 7.06ms）、降级字段档位 **≤ 35ms**（最差 22.84ms）；阈值 1024 → **8192**（阈值是实现常量，不属需求面） |
| v1.13 | 2026-09-11 | 已定稿 | **V2 Step 6 详细设计回写：需求侧无变化，本行仅为版本面同步**（依据 `v2-step6-design.md` v0.1，D-S6-01~09 待评审）——**NFR-03 ② / NFR-05 / NFR-10 / NFR-11 的备注补「设计已出」状态与设计文档锚点**（此前只有"V2.0 Step 6 实测"的将来时）。⚠️ **本版不修订任何指标**：① NFR-03 ① 的 240s 与 ② 的 `× 变更比 × 1.2` 一字未动；② **NFR-10 的数值目标仍然缺失**（口径全文只有"多线程并发检索吞吐与正确性"，**无法判定达标**）⇒ 已登记为 `v2-step6-design.md` §9.2 **Q4**，待评审在「相对提升（近线性）判据 / 绝对 QPS / 只记录不判定」三方案中拍板，**本轮不擅改**；③ NFR-03 ② 的「10% 以什么为单位」登记为 **Q5**（建议：文档数），同上待拍板 |
| v1.14 | 2026-09-11 | 已定稿 | **NFR-10 口径修订为方案 A + NFR-03 ② 补单位注记（V2 Step 6 评审收口 / PR #38）**—— 评审在「相对提升近线性判据 / 绝对 QPS / 只记录不判定」三方案中**拍板方案 A**（设计 `v2-step6-design.md` §4.6.3 / D-S6-08）：**NFR-10 由「只有定性描述、无任何数字 ⇒ 无法判定」改为双部分口径**——① **前置条件（正确性）**：各线程 `hits`（`chunk_id` + `score`）序列**逐位等于**单线程；② **吞吐判据**：`--threads 4` QPS ≥ `--threads 1` × **2.5**（允许 40% 折损）；并**写明测量口径**（预热丢弃首轮 + 顺序交错 + 报告运行范围），避免被不同口径的重测打脸。⚠️ ×2.5 为**「拟」值**：基线 QPS 由 **S6-08** 实测，标定后定稿（同 Step 5 的 NFR-13 先例：v1.10 落「拟 ≤20ms」、v1.12 定稿）。**NFR-03 ② 的「10% 」单位拍板为「文档数」**（原文即「追加 10% 文档」），**chunk 数只作参考指标、不进判据**。**这是 Step 6 唯一触碰 FR/NFR 的改动**；其余 6 条评审意见（F1/F2/F5/F6/F7 及 §2.6 的 `rayon` 补记）只改设计文档与 CHANGELOG |
| v1.15 | 2026-09-13 | 已定稿 | **V2 Step 6 收尾（S6-10）：NFR-10 定稿并收窄适用范围 + NFR-05 补口径限定词** —— ① **NFR-10**：S6-08 实测 `QPS(4)/QPS(1)` = bm25 **2.98×** ✅ / hybrid **1.63×** / vector **1.88×**（`eval-report.md` §8.12 / **§8.13**）⇒ **×2.5 由「拟」定稿**（同 Step 5 的 NFR-13 先例：v1.10 落「拟」、v1.12 定稿）。**裁定三条并记**：㈠ **② 的适用范围收窄为「不含查询侧编码的读路径」**（bm25 即该路径的实测证据；根因 = 查询侧编码被 `Mutex<TextEmbedding>` 单 session 互斥串行化，架构 **R43**）；㈡ **NFR-10 整体保持「打开」、不关闭** —— 编码侧并发（vector / hybrid）**如实记为未达标项**，**不是** D-S6-08 已否决的「只记录不判定」；㈢ **复审触发条件 = 查询侧会话池（每 worker 独立 session）落地时**，届时须**先答挂起的 Q3**（不同 `sessions` / `intra_threads` 是否改变向量数值 —— 若改变会破坏 ① 的逐位一致）再重测。② **NFR-05**：补口径限定词「**（检索路径，batch 1）**」；**建库路径**（batch 64 × 长文本）峰值 RSS 实测 **2.32~3.33 GB**、与 372MB **差一个数量级** ⇒ **单独立为架构 R41**（预算形态按 `batch × 序列长度` 给、**数值待定**），**不并入** 372MB 基线。③ **NFR-03 ②** 增量口径实测 **23.705s < 27.973s** ✅（余量 15.3%，`eval-report.md` §8.11）。④ **更正 NFR-10 备注措辞**（评审 P3-8）：**「读路径零共享可变状态」只对「索引读路径」成立**。⚠️ **本版不新增任何 FR**；架构侧随之升 **v1.14**（§14.4 扩为 R36~R43） |
| v1.16 | 2026-09-14 | 待评审 | **V2 Step 7（精排）详细设计回写**（依据 `v2-step7-design.md` v0.1，D-S7-01~10 待评审）—— ① **NFR-12 由「一行、无阈值、无测量口径」补齐为三部分**（**延迟判据** = rerank **增量** P99 **≤ 500ms（拟）**、**独立口径不并入 NFR-02**；**质量判据** = hybrid **MRR@10(1)** 相对**同图**基线**可测提升**；**可观测** = `Metrics.rerank_window` / `rerank_elapsed` + `Explain.rerank_score.is_some()`，**不得静默**）——**「500ms」标「拟」（⚠️ 对应 `R = 10` 档上界、**与默认 `R` 联动**，见设计 §4.2.2 补记）、待 **S7-04** 标定回填（语料 = **T2Ranking 12K / 320 query**，设计 §4.6.1）**（同 NFR-13 先例：v1.10 落「拟」→ v1.12 定稿）。② ⚠️ **写明 NFR-12 的 A/B 必须在「同一张冻结图」上做**（`--index` 读快照 + `--runs 1`）：P5 锚点 `hybrid MRR@10(1) = 0.6922` **自身带 ~0.6% 图漂移**（2026-09-14 复现 **0.68815**；**bm25 三项逐位复现 Δ=0** 作对照 ⇒ 唯一变量是未播种的 HNSW 图）。③ **NFR-05 补口径限定词「不含精排器」**——`rozgo/bge-reranker-v2-m3` 的 `model.onnx.data` 实测 **≈2,187MB（FP32，sha256 `84b66c78…8945`）**、加载后常驻 ⇒ 精排内存**单独立为架构 R44**（形态按「模型参数 + 单 batch 激活」给、**数值待 S7-04 回填**），**不并入** 372MB 基线。④ **NFR-06 补精排确定性注记**（精排是 score 的二次单调变换、**不改 `chunk_id` 序**；⚠️ `PaddingStrategy::BatchLongest` 使**跨 batch 组成**的分数**不可逐位复现** ⇒ 判据仍是「同一快照两次加载逐位一致」）。⑤ **NFR-07 补精排可观测字段**（`Metrics.rerank_window` / `rerank_elapsed`）。⑥ **FR-18 补设计锚点 + 模型来源更正**（**`rozgo/bge-reranker-v2-m3`，非 BAAI 官方** —— 官方库**没有 ONNX 导出**）。⚠️ **本版不新增任何 FR**；架构侧随之升 **v1.15**（§14.5 新增 **R44~R48**） |
| v1.17 | 2026-09-14 | 待评审 | **V2 Step 7 设计评审收口 —— 需求侧只有两处事实性更正，无预算 / 结构变更**（设计升 **v0.2**；评审 4 项拍板全部同意、F1~F6 全部采纳）：① **NFR-12 的标定语料更正** —— 原写「（10 万级）」，实为 **T2Ranking 12K / 320 query**（设计 §4.6.1 / 附录 B 的协议；「10 万级」是 NFR-13 / S5-04 先例的措辞残留，**F3**）；② **NFR-12 的「≤ 500ms（拟）」补出处与联动** —— 该拟值对应 **`R = 10` 档的量级上界**（设计 §4.2.2 粗估 ≈0.5s），而 **`R = 20` 粗估 ≈1s 会超它** ⇒ **阈值与默认 `R` 必须联动、由 S7-04 一并回填**，**不得各自独立引用**（**F4**）；③ 头部状态的 NFR-12 摘要由「单列」同步为「**双列**」。⚠️ **本版不修订任何预算数值、不新增 / 删除 FR-NFR**；架构侧随之升 **v1.16**（纯版本面同步） |
| v1.18 | 2026-09-16 | 待评审 | **V2 Step 7 收尾（S7-05，设计 `v2-step7-design.md` v0.3）**：① **NFR-12 定稿** —— 原「≤ 500ms（拟）」**废弃**（实测最小档 `R=10` 的 P99 = **5220 ms**、差 **10.4×**）⇒ 改为**双口径**（**CPU/FP32 上界**：默认 `R=20` 下 P50 ≤ 8.8 s / P99 ≤ 10.9 s；**GPU/量化 = 待测**，标「拟」）；并写明**适用范围 = 仅显式开启精排时**（D-S7-04 默认关）与**与 `R` 联动**（`R` 定稿 **20**，D-S7-01）；② **NFR-05** 回填精排内存实测（**增量 2.64 GiB / 7.71×**，`eval-report.md` §8.14.10）；③ **NFR-06** 更正精排 tie-break 机制句（「保持输入序」→ **`chunk_id` 升序**，与 D-S7-07 及实装一致）+ 补 S7-T9 实测读数（单条 vs 批内 logit 相同）；④ **FR-18** 验收注记同步。⚠️ **无预算数值增减、不新增/删除 FR-NFR**；架构侧随之升 **v1.17** |
| v1.19 | 2026-09-16 | 待评审 | **V2.1 计划修订（需求面）** —— 依据 `plan-v2.md` **§附-4** 的 V2.1 计划审视（15 条结论）补齐**需求编号空缺与口径缺口**：① **新增 FR-34 prefilter / 排序键索引**（高基数数值字段：基数保护 `terms + numbers ≤ 1024` ⇒ 毫秒时间戳 / 雪花 ID **约 512 篇**即永久降级、该字段过滤回落 O(N) 全扫 ⇒ **Q-I1 对最常见的真实过滤场景收益为 0**）；**承接架构 R11 / R13 的「V2.1 复议 prefilter」**，⚠️ **是否投由 T7-27 的 spike 判定**（判据 = `Metrics.vector_shortfall` 的分布）。② **新增 FR-35 融合策略自适应**（原 Q-R1；⚠️ **与 FR-21「多 query 融合接口」不同** —— 本条是**单 query 内**的融合权重）。③ **新增 NFR-14 读写并发下的读延迟与可见性**（Step 8 此前**只有 FR-17 §5.3.5 的四条定性验收、无编号 NFR**）：可见性 = **`commit()` 后立即可查**、读延迟 = 合并期间持续检索无失败无超时且 P99 仍在 NFR-02 预算内、不得以「重建索引中」为由拒绝查询；⚠️ **数值与测量口径由 `v2-step8-design.md` 定稿**。④ **新增 NFR-15 自适应融合的质量判据**（Step 9 此前**无任何 FR / NFR 编号**）：在**分桶**上优于固定权重；⚠️ 指标 / 阈值 / **统计口径**须设计期定值 —— Step 7 的教训是**配对符号检验不显著**（最小 p = 0.1553）⇒ 须预先声明「按幅度读、不声称显著」或给出可行样本量。⑤ **FR-18 状态同步**（§5.2 摘要行此前停在「设计已出、待评审」，与已实现矛盾）；**FR-17** 补 Step 8 任务号（**T7-25 → T7-06 / T7-26**）与 NFR-14。⚠️ **本版不修订任何既有指标数值**（NFR-02~13 的阈值 / 预算 / 时限一字未动）；架构侧随之升 **v1.18** |
| v1.20 | 2026-09-16 | 待评审 | **V2 Step 8（读写并发）详细设计回写**（依据 `v2-step8-design.md` **v0.1**，D-S8-01 ~ 12 待评审、**4 条需拍板**：D-S8-01 / D-S8-06 / D-S8-09 / D-S8-11）—— ① **NFR-14 定口径**（原为「草案、待评审」）：可见性 = **`commit()` 后立即可查**（对齐 NFR-11，并同步改写 **FR-17 §5.3.5** 的措辞）；读延迟 = **②a 相对判据**（**合并期 ÷ 静止期**的端到端 `took` P99 **≤ 1.2（拟）**，同机 / 同图 / 同 query 集）+ **②b 绝对护栏**（合并期 P99 **仍在 NFR-02 预算内**）—— ⚠️ **相对为主、绝对为护栏**（避免把「分段的基线变化」与「合并的干扰」混在一起读）；③ 不得以「重建索引中」为由拒绝查询 ⇒ 设计把「**库内不存在该类错误码**」变成**可证伪断言**（S8-T10 以 `grep` 断 `Error` 枚举无该变体）。② **NFR-11 补口径与拟值**：口径由「**推算**」改为 **`commit()` 端到端 wall time 直接计时**；**拟值** `P50 ≤ 1.0s / P99 ≤ 2.0s`（`batch_size = 64`、CPU/FP32、**不含首次模型下载与加载**），**待 S8-08 标定后定稿**（同 NFR-13 / NFR-10 先例：先落「拟」、标定后定稿）。③ **FR-17 §5.3.5 措辞改写**：「写入后立即可查」→「**`commit()` 后立即可查**」（**只改措辞、不改语义**；「读能看到未 commit 的写」**明确不做**；**保留原措辞引文** —— 「被推翻的结论也写下来」纪律）。④ **NFR-10 ③** 补设计锚点（复审触发条件 = **T7-26** 会话池落地时，届时须先答**改述后的 Q3′**：不同 `intra_threads` / 实例数是否改变 `embed_query` 向量**数值**）。⚠️ **不修订任何既有指标数值**、**不新增或删除 FR / NFR**；架构侧随之 **v1.19**（**§14.6 新增 R49 ~ R54**） |

### 1.2 读者与阅读建议

| 读者              | 建议阅读章节                |
| --------------- | --------------------- |
| 项目决策者 / 评审人     | 第 2、5、6、7、9 章         |
| 场景层接入方（知识库 / 记忆） | 第 3、4、5.2、9 章         |
| 实现者             | 全文，随后转 `architecture-design.md`   |

### 1.3 与其他文档的关系

```
需求分析说明书（本文档）
    │  定义：做什么 / 为什么 / 怎么验收
    │  FR-xx / NFR-xx 编号的唯一权威来源
    │  术语表的唯一定义来源
    ▼
架构设计说明书（architecture-design.md）
    定义：怎么做 / 为什么这么做
    只引用需求编号，不重复定义需求
```

**引用规则**：架构文档中出现的 `FR-xx` / `NFR-xx` 一律指向本文档。需求变更时只改本文档，架构文档通过编号保持同步，避免两处定义漂移。

### 1.4 修订记录

见文末「附录 C 变更记录」。

---

## 2. 项目背景与目标

### 2.1 背景与问题陈述

Agent 应用（RAG 知识库、长期记忆、代码助手）的核心瓶颈往往不在大模型本身，而在**检索**：召回错了，后面生成得再好也是幻觉。

市面上现成的检索方案（Elasticsearch、Milvus、各类向量库）各有定位，但它们要么是给人用的搜索引擎，要么是纯向量数据库，**缺少一层专门针对 Agent 作为调用方而设计的内核**。这个缺口不是功能缺口，而是接口与质量属性层面的缺口——具体差异见第 4 章。

### 2.2 项目目标

| 目标 | 说明                                               |
| -- | ------------------------------------------------ |
| G1 | 实现 BM25 关键词检索与向量语义检索双路召回，并完成融合                   |
| G2 | 内核全部抽象为 trait，分词器 / 嵌入模型 / 向量索引 / 融合策略 / 精排器均可替换 |
| G3 | 接口设计上充分照顾 Agent 调用方的特殊性（见第 4 章）                  |
| G4 | 单机万级文档下跑通全链路，效果与延迟可量化                            |
| G5 | 架构为后续扩展（Rerank、增量、分布式）留出明确位置                     |

### 2.3 项目定位与边界

**一句话定位**：通用检索引擎内核，不做场景假设，场景层（知识库 RAG / Agent 记忆 / 代码检索）后续接入。

由此产生的边界划分：

- ✅ 内核负责：结构化输出、可解释性、防空召回、低延迟、幂等写入、元数据过滤
- ❌ 内核不负责：知识库的 schema、记忆的时序衰减公式、代码检索的符号关系

> 这条边界是硬约束。内核一旦内置领域逻辑，场景层就只能"将就"，最终退化为又一个专用引擎。

### 2.4 成功标准

- [ ] 在示例中文语料上，混合检索的 Recall@10 与 MRR@10 **优于** 单独的 BM25 与单独的向量检索
- [ ] 单次混合检索 P99 延迟（不含模型冷启动）< 20ms @ 1 万 chunk
- [ ] 索引快照加载时间 < 2s，显著优于重建索引
- [ ] 全部核心 trait 均有 ≥2 个实现或明确的替换路径

---

## 3. 术语与缩略语

> 本文档是术语的**唯一定义来源**，架构文档直接使用，不重复定义。

| 术语            | 含义                                          |
| ------------- | ------------------------------------------- |
| Document      | 摄入的原始文档，一个文档可切分为多个 Chunk                    |
| Chunk         | 检索与返回的最小单位，通常 200~500 字                     |
| Term          | 分词后归一化得到的索引词                                |
| Posting       | 倒排表中一条记录：`(chunk_id, term_freq, positions)` |
| 倒排索引          | Term → Posting 列表 的映射                       |
| 正排存储          | ChunkId → 原文、元数据 的映射                        |
| df            | document frequency，包含某 Term 的 Chunk 数量      |
| avgdl         | 平均 Chunk 长度（以 Term 数计）                      |
| 召回（Retrieval） | 从全量中快速找出候选集，重速度                             |
| 精排（Rerank）    | 对候选集精细打分重排，重精度                              |
| 融合（Fusion）    | 把多路召回结果合并为统一排序                              |
| RRF           | Reciprocal Rank Fusion，倒数排名融合               |
| Lane          | 一路独立的召回通道（如 BM25 lane、向量 lane）              |

---

## 4. 调用方特征分析：Agent 场景的特殊性

> 本章是全部需求的来源。**如果这个引擎和 Elasticsearch 没有区别，那它就不该存在。**

### 4.1 前提性张力：通用内核 vs Agent 场景

"通用内核"与"Agent 场景"之间存在张力：通用意味着不做场景假设，Agent 场景意味着要为 Agent 做优化。

本项目的解法是：**把 Agent 场景的特殊性翻译为「接口约束」与「质量属性约束」，而不是翻译为「领域逻辑」。**

- ✅ 翻译成接口约束：结构化输出、可解释性、防空召回、低延迟、幂等写入、元数据过滤
- ❌ 不翻译成领域逻辑：不内置知识库 schema、不内置记忆衰减公式、不内置符号关系

这样场景层（知识库 / 记忆 / 代码）才能真正接进来，而不是被内核的假设绑死。

### 4.2 变化一：查询的发出方从人类变成 LLM

|    | 人类查询                   | Agent 查询                                  |
| -- | ---------------------- | ----------------------------------------- |
| 形态 | 短、关键词化（"rust bm25 实现"） | 完整自然语言问句（"在 Rust 里怎么实现一个支持中文的 BM25 检索？"）  |
| 长度 | 2~5 词                  | 10~40 词，含大量虚词                             |
| 质量 | 意图明确                   | 可能含**指代**（"它的默认值是多少"）、可能含**幻觉词**（模型编造的术语） |

**工程后果**

- 长 query 直接喂 BM25，虚词会稀释信号
- 含幻觉词时该词 `df = 0`。若用 AND 语义 → **整体零召回**，这是 Agent 场景最典型的翻车方式
- 含指代时，语义向量会漂移，向量路也可能失效

**设计应对**

- FR-04：BM25 **默认 OR 语义**，禁止 AND。单个 term 未命中只损失该 term 的贡献，不影响整体
- FR-13：空结果必须携带 `EmptyReason`（是库里没文档？还是词全没命中？还是被过滤掉了？），让 Agent 能据此决定"改写 query / 换数据源 / 直接回答"
- FR-21：支持多 query 输入后融合，为后续的 HyDE、multi-query 改写留出接口（内核不做改写，只做承载）

### 4.3 变化二：检索结果是 LLM 的输入，不是给人看的列表

人可以翻页、扫标题、自行判断 relevance；Agent 是 Top-K 直接进 prompt，**一次机会，没有翻页这个交互**。

**工程后果**

- **前几条的精确率** 比 "有没有涵盖全部相关文档" 更关键
- **重复内容极度有害**：同一句话出现在 3 个 chunk 里 → 浪费 3 倍 token，且污染上下文、干扰注意力
- 结果必须**可溯源**，否则 Agent 无法标注引用，用户无法验证

**设计应对**

- FR-12：`Hit` 必须携带 `doc_id / source / char_start / char_end / metadata`，并提供 `to_context_block()` 直接产出可拼进 prompt 的文本块
- FR-24（v2）：结果去重，MMR 或同文档相邻 chunk 合并
- FR-25（v2）：按 token budget 裁剪，防止撑爆上下文窗口

### 4.4 变化三：检索位于 Agent 循环的关键路径，且被高频调用

一次用户提问，Agent 可能跑 5~20 轮，每轮调 1~3 次检索 → **单次会话几十次调用**。

人类搜索等 200ms 无感；Agent 场景下 50 次 × 100ms = 5s，直接体现在端到端延迟上，用户能明确感知。

**工程后果**

- **P99 比均值重要**。查询路径上不允许有隐藏的 O(n) 扫描或磁盘 IO
- Embedding 是最大延迟源：本地 bge-small 单条约 5~15ms，远程 API 则 50~200ms

**设计应对**

- NFR-02 明确延迟上限；两路召回并行执行、全内存索引、查询路径零磁盘 IO
- FR-20：query embedding 缓存（Agent 多轮循环中大量 query 是重复的）
- NFR-07：内核暴露 `SearchResponse.took`，让上层能观测与告警

### 4.5 变化四：Agent 需要能"自查"检索质量

人看到结果不对会自己改 query。Agent 需要**信号**来判断"这次召回靠谱吗"，才能决定是改写 query、换工具、还是直接回答。

**一个高价值的诊断信号**：BM25 命中但向量路未命中，通常意味着"词汇匹配但语义不相关"（如 query 里出现的词恰好是文档里的高频噪音词）；反之，向量路命中但 BM25 零命中，意味着"语义相关但用词完全不同"。

**设计应对**

- FR-13：每条 `Hit` 携带 `Explain`，包含各 lane 的 rank 与 score、`matched_terms`、融合分
- 上层可据此构造策略：两路都命中 → 高置信；仅一路命中 → 降低置信或触发重试

### 4.6 变化五：写入模式不同

传统搜索系统的索引由离线管道批量构建。Agent 场景下，Agent 自己会往里写（新记忆、新经验、刚抓取的网页），写入是**零散、持续、与主流程异步**的。

**工程后果**

- 必须幂等（同一个文档反复灌入不能产生重复结果）
- 增量写入不能阻塞读

**设计应对**

- FR-15：doc 级 upsert，按 `content_hash` 或 `source` 去重
- FR-17：主索引 + delta 区，delta 异步合并，读不阻塞

### 4.7 约束推导汇总

| Agent 场景特征           | 对内核的要求             | 落到需求          |
| -------------------- | ------------------ | ------------- |
| query 由 LLM 生成，质量不稳定 | OR 语义、防空召回、显式空原因   | FR-04 / FR-13 |
| 结果直接进 prompt，无翻页     | 结构化 + 可溯源 + 去重     | FR-12 / FR-24 |
| 高频调用，关键路径            | 低 P99、并行、缓存、零 IO   | NFR-02 / FR-20 |
| 需要自查检索质量             | 可解释性、双路命中信号        | FR-13         |
| Agent 自己写入           | 幂等 upsert、非阻塞增量    | FR-15 / FR-17 |
| 多场景共享内核              | 全 trait 抽象，不内置领域逻辑 | G2 / 2.3 节    |
| 需要按 scope 限定范围       | 元数据过滤              | FR-14         |

---

## 5. 功能性需求

### 5.1 需求总览

优先级采用 MoSCoW：**Must** 当期版本必做 / **Should** 当期版本尽量做 / **Could** 后续版本 / **Won't** 本期不做。V1 与 V2 的版本归属见条目说明与 `plan-v2.md`。

| 能力域    | 需求编号                          | Must | Should | Could |
| ------ | ----------------------------- | ---- | ------ | ----- |
| 摄入与分块  | FR-01                         | 1    |        |       |
| 文本分析   | FR-02                         | 1    |        |       |
| 嵌入     | FR-05、FR-06                   | 2    | FR-07  |       |
| 索引     | FR-03、FR-08、FR-15、FR-16、FR-17 | 5    |        |       |
| 召回     | FR-04、FR-09                   | 2    |        |       |
| 融合与排序  | FR-10、FR-18                   | 2    | FR-11  |       |
| 输出与可解释 | FR-12、FR-13                   | 2    | FR-14  |       |
| 工程与交付  | FR-22、FR-23                   | 2    | FR-19、FR-20 | FR-21、FR-24、FR-25 |

**第一版必做（Must）共 18 条**：FR-01 ~ FR-06、FR-08 ~ FR-10、FR-12、FR-13、FR-15 ~ FR-18、FR-22、FR-23。

**V2 新增需求（对应 `plan-v2.md`，2026-09-04 回写；2026-09-07 复审后更新）**：FR-26 ~ FR-33、NFR-10 ~ NFR-13。

- Must（V2.0）：FR-26（向量软删除）、FR-27（过滤下推）、FR-29（图持久化）、**FR-31（原子快照，2026-09-07 由 Should 升）**
- Should：FR-28（增量构建）、FR-30（墓碑物理回收）、FR-32（命名空间隔离）、NFR-13（低选择度过滤延迟）
- Could（V2.1）：FR-33（时间衰减打分钩子）
- 原 Could 的 FR-21/24/25（多 query / MMR / token budget）在 V2 **收编**落地。

### 5.2 需求条目

| 编号    | 需求                   | 优先级    | 说明                                       |
| ----- | -------------------- | ------ | ---------------------------------------- |
| FR-01 | 文档摄入与分块              | Must   | 支持按固定长度 + 重叠切分；`Document` → `Vec<Chunk>` |
| FR-02 | 中英混合分词               | Must   | 中文分词 + 拉丁文本按正则/空白切分                     |
| FR-03 | 倒排索引构建               | Must   | term dict + postings，手写实现                |
| FR-04 | BM25 检索              | Must   | OR 语义，参数可调                               |
| FR-05 | `Embedder` 抽象        | Must   | trait 定义                                 |
| FR-06 | 本地 embedding 实现      | Must   | fastembed + bge-small-zh-v1.5            |
| FR-07 | 远程 embedding 实现      | Should | HTTP API，feature flag 隔离                 |
| FR-08 | 向量索引                 | Must   | HNSW                                     |
| FR-09 | 向量检索                 | Must   | Top-K 相似度检索                              |
| FR-10 | RRF 融合               | Must   | 默认融合策略                                   |
| FR-11 | 加权归一融合               | Should | 备选策略，需规避归一化陷阱                            |
| FR-12 | 结构化结果返回              | Must   | 含出处、偏移、元数据、上下文块生成                        |
| FR-13 | 检索可解释性               | Must   | `Explain` + `EmptyReason`                |
| FR-14 | 元数据过滤                | Should | tag 等值匹配 + 数值范围                          |
| FR-15 | 幂等 upsert 与删除        | Must   | content_hash / source 去重；墓碑删除（V2 扩展至向量侧软删除，见 FR-26） |
| FR-16 | 索引二进制快照              | Must   | save / load（V2 补原子性与图持久化，见 FR-29/FR-31） |
| FR-17 | 增量写入                 | Must   | 主索引 + delta 区（**V1 已交付主索引增量；读不阻塞写的完整版归 V2.1**） |
| FR-18 | Reranker 接入           | Must   | trait 抽象 + NoOp（V1）；**V2.0 接入 `bge-reranker-v2-m3`**（**已实现并收尾（2026-09-16，设计 `v2-step7-design.md` v0.3；实现 PR #52 / #53 / #55 / #56 + 收尾 S7-05 → `62acb37`）**；⚠️ **模型来源更正**：fastembed 6.0.2 的 `RerankerModel::BGERerankerV2M3` 指向 **`rozgo/bge-reranker-v2-m3`**，**非 BAAI 官方**——BAAI 官方库**没有 ONNX 导出**） |
| FR-19 | 批量摄入并行化              | Should | rayon                                    |
| FR-20 | query embedding 缓存   | Should | LRU                                      |
| FR-21 | 多 query 融合接口         | Could  | 为查询改写留口子                                 |
| FR-22 | CLI                  | Must   | 建索引 / 检索 / 三种模式对比                        |
| FR-23 | 示例中文语料               | Must   | `data/` 下自带小样本                           |
| FR-24 | 结果去重（MMR）            | Could  | v2                                       |
| FR-25 | token budget 裁剪      | Could  | v2                                       |
| FR-26 | 向量软删除 + 存活过滤        | Must   | 删除语义扩展到向量侧：已删文档的向量**不得占用 Top-K 名额**，且该保证须跨快照持续有效 |
| FR-27 | 过滤下推 + 字段索引          | Must   | 元数据过滤下推到索引遍历，替换全表扫描；每 query 的过滤成本须与语料规模解耦 |
| FR-28 | 增量构建                 | Should | 只 embed 新增文档（content_hash 跳过）              |
| FR-29 | 图持久化                 | Must   | HNSW 图落盘/加载，冷启动达秒级。**✅ V2 Step 2 已实现**（ADR-A 方案 C：图 = 快照的派生缓存，`FORMAT_VERSION` 保持 2；sidecar 三件套 + manifest 作唯一原子发布点）。12K 实测：完整冷启动 ≈ 100ms ✅、图 dump 43.5ms、磁盘增量 1.6× |
| FR-30 | 墓碑物理回收（compaction）   | Should | 长期膨胀治理：回收向量图 + raw_vectors + 快照。**✅ V2 Step 4 已实现**（D-S4-01：**按存活集重新物化 + ID 重编号**，`FORMAT_VERSION` 保持 2）：`SearchIndex::compact`（纯内存）/ `compact_and_save`（**唯一落盘入口**，复用 Step 3 `atomic_write`）+ `tombstone_stats` / `CompactionReport` 可观测 + CLI `helix compact`（`--dry-run` / `--output` / `--json`）；落盘入口唯一 ⇒ 「重建图后必须重发 manifest」由既有 `save` 链路**自动满足**。10K 合成语料 churn 0.3×5 实测：graph+data **84.6→33.7MB**（-60%）、图 `nb_point` 25000→10000（== 存活且有向量的 chunk 数）、compact 后 reload `Loaded`（非 `Rebuilt`）冷启动 91~99ms（`eval-report.md` §8.8）。⚠️ **ID 会被重编号**：跨 compact 的持久引用请用 `source` / `content_hash`（D-S4-09 三处声明；架构 §7.5.3 / R27）。**手动触发为主，自动 compaction 默认关**（D-S4-02；`save` 墓碑占比 ≥20% 且总量 ≥1024 时打告警） |
| FR-31 | 原子快照                 | **Must**（2026-09-07 由 Should 升） | 临时文件 + fsync + rename 原子替换，防崩溃损坏。✅ **已落地（V2 Step 3）**：`storage/atomic.rs` 通用原语 `atomic_write`（tmp + flush + `sync_all` + rename + fsync 父目录），崩溃不变式「save 序列任一注入点崩溃后，load 要么旧快照要么新快照，绝不 `SnapshotCorrupted`」由故障注入测试证明（`v2-step3-design.md` §7 T3~T6）；fsync 代价实测 +0.027~0.035s（`eval-report.md` §8.7） |
| FR-32 | 命名空间隔离               | Should | 检索级命名空间（≠权限/多租户，后者范围外）                 |
| FR-33 | 时间衰减打分钩子             | Could  | 可插拔 recency boost 机制（策略放场景层）             |
| **FR-34** | **prefilter / 排序键索引（高基数字段）**（**草案，待评审**；2026-09-16 新增）：字段索引有基数保护（`terms + numbers ≤ 1024`），毫秒时间戳 / 雪花 ID **约 512 篇**即永久降级、该字段过滤回落 O(N) 全扫 ⇒ 要求「高基数字段的过滤成本与语料规模解耦」。⚠️ **是否投由 T7-27 的 spike 判定**（判据 = `Metrics.vector_shortfall` 的分布） | Should | 强化 FR-27；**承接架构 R11 / R13** 的「V2.1 复议 prefilter」 |
| **FR-35** | **融合策略自适应**（**草案，待评审**；2026-09-16 新增）：融合权重按桶（paraphrase / exact / 长尾）自适应，修复弱路 BM25 的稀释 | Should | 新增；⚠️ **与 FR-21「多 query 融合接口」不同** —— 本条是**单 query 内**的融合权重（原 Q-R1） |

### 5.3 关键需求详细规约

以下 6 条是本项目的差异化需求，单列规约以避免实现时被简化掉。

#### 5.3.1 FR-04：BM25 强制 OR 语义

| 项    | 规约                                                       |
| ---- | -------------------------------------------------------- |
| 行为   | query 中多个 term 之间为 OR。未命中的 term 贡献 0 分，其余 term 正常累加      |
| 禁止   | 第一版不提供 AND / 布尔查询语法                                      |
| 理由   | Agent 的 query 由 LLM 生成，可能含幻觉词。AND 语义下单个 term 的 `df=0` 会导致整体零召回，且**没有任何错误提示**，是最隐蔽的失败模式 |
| 验收   | 构造含 1 个不存在词的 query，检索仍需返回其余 term 的命中结果，且 `Explain.matched_terms` 中不含该词 |

#### 5.3.2 FR-12：结构化结果返回

| 项    | 规约                                                                            |
| ---- | ----------------------------------------------------------------------------- |
| 必带字段 | `chunk_id`、`doc_id`、`score`、`text`、`source`、`metadata`、`explain`              |
| 溯源字段 | `source`（文件路径/URL/标题）、`char_start`、`char_end`（原文字符偏移）                          |
| 上下文块 | 提供 `to_context_block()`，直接产出带出处标注、可拼进 prompt 的文本                               |
| 理由   | Agent 无法翻页，且必须能标注引用供用户验证                                                       |
| 验收   | 任一条 `Hit` 能反查回原文，偏移量精确；`to_context_block()` 输出含 source 标注                       |

#### 5.3.3 FR-13：检索可解释性与空结果原因

| 项      | 规约                                                                                 |
| ------ | ---------------------------------------------------------------------------------- |
| `Explain` | 每条 Hit 携带：`matched_terms`、各 lane 的 `score` 与 `rank`、`fused_score`                   |
| `EmptyReason` | 空结果时必须给出原因，取值：`NoDocuments`（索引为空）/ `AllTermsUnmatched`（词全部未命中，提示 query 可能含幻觉词）/ `FilteredOut`（有候选但被 filter 全部过滤，提示放宽 scope） |
| 诊断价值   | 上层可据此构造策略：两路都命中 → 高置信；仅一路命中 → 降低置信或触发重试                                            |
| 验收     | 三种空结果场景各能返回对应 `EmptyReason`；`--explain` 模式下每条结果的分 lane rank/score 完整可查              |

#### 5.3.4 FR-15：幂等 upsert 与删除

| 项    | 规约                                             |
| ---- | ---------------------------------------------- |
| 去重键  | `content_hash`（默认）或 `source`，二选一可配置            |
| 语义   | 同一文档重复 upsert 不产生重复 chunk；upsert 已存在文档时先删后插    |
| 删除   | 墓碑标记，异步物理清理；删除后统计量须与全量重建一致（V2 扩展：向量侧软删除 + 存活过滤，见 FR-26） |
| 理由   | Agent 会反复灌入同一份内容（重新抓取的网页、重复记忆）                 |
| 验收   | 同一文档 upsert 两次，chunk 数不翻倍，检索结果不变；删除后统计量无漂移     |

#### 5.3.5 FR-17：增量写入不可阻塞读

> 交付状态：V1 已交付「主索引增量写入」；「读不阻塞写」的 delta 分段完整版归 V2.1（`plan-v2.md` **Step 8**，2026-09-07 重排前为 Step 7）。
>
> ⚠️ **2026-09-16（V2.1 计划修订）**：Step 8 已补任务号 —— **T7-25（硬前置：修架构 R34 的递归读锁）→ T7-06（delta 分段）/ T7-26（查询侧会话池）**，跟踪 issue **#58**；验收新增 **NFR-14**（草案）。
>
> ⚠️ **口径冲突（Step 8 设计期须择一）**：本表「验收」列写「**写入后**立即可查」，而 **NFR-11**（Step 6 定稿）已改为「**`commit()` 后**立即可查」（`search/index.rs` 的 `add` → `commit` 语义，对齐 Lucene/tantivy）。取形见 `plan-v2.md` §4.0 **H5**：**可见性 = `commit()` 后可见，「读能看到未 commit 的写」明确不做** ⇒ 本表「写入」应读作「**`commit()` 之后**」，措辞待 Step 8 设计期一并更正（**不改语义**）。
>
> ✅ **2026-09-16（V2 Step 8 详细设计已出，`v2-step8-design.md` v0.1）—— 口径冲突已择一**：**取 NFR-11**，即**可见性 = `commit()` 后**（「读能看到 **未 commit 的写**」**明确不做**，取形见 `plan-v2.md` §4.0 **H5** 与设计 §0 附加行）。本表「验收」列的「写入后立即可查」**已按需求面改写为「`commit()` 后立即可查」**（随本版 **v1.20** 落地，**只改措辞、不改语义**；**原措辞按「被推翻的结论也写下来」纪律保留于上方**）。⚠️ 另：本条的**验收可证伪化**落在设计 §1.3 第 7 条（`add` 后未 `commit` ⇒ **查不到**；`commit` 后 ⇒ **立即查到**，双向断言 + 反向自证，S8-T7），**测量口径与拟值由 NFR-14 承载**。

| 项    | 规约                                                           |
| ---- | ------------------------------------------------------------ |
| 行为   | 新增文档进入 delta 区，立即对检索可见；delta 在后台异步合并进主索引                     |
| 禁止   | 合并期间不得阻塞读请求；不得以"重建索引中"为由拒绝查询                                 |
| 理由   | Agent 写入是零散、持续、与主流程异步的，任何阻塞都会直接卡住 Agent 循环                   |
| 验收   | **`commit()` 后立即可查**（⚠️ **2026-09-16 措辞改写**：原写「写入后立即可查」，见本节注记 —— **只改措辞、不改语义**）；合并过程中持续发起检索，无失败、无超时（延迟变化须在 NFR-02 范围内）                |

#### 5.3.6 FR-18：Reranker 接入

| 项    | 规约                                                  |
| ---- | --------------------------------------------------- |
| 交付（V1） | 定义 `Reranker` trait + `NoOpReranker` 实现，接入查询流程但不改变排序 |
| 交付（V2.0） | 接入 `bge-reranker-v2-m3`（fastembed `TextRerank`），候选窗口从 `take(k)` 放开到 `candidate_k`/可配 R |
| 理由   | V1 保证查询链路中已有精排位置；V2.0 落地真实精排以提升首条精确率（MRR） |
| 验收   | `NoOpReranker` 与移除精排环节结果一致（V1）；接入后 hybrid MRR@10(1) 相对基线可测提升，精排延迟满足 NFR-12（V2.0；**2026-09-16 定稿**：**双口径**且**仅精排开启时适用**，见 NFR-12 行）。<br>**2026-09-14（Step 7 设计）收紧口径**：② 的「相对基线」**必须钉在「同一张冻结图」上**（P5 锚点自身带 ~0.6% 图漂移，见 NFR-12 备注与设计 §3.1）；① 的「移除精排环节」在本设计中**有具体实现**= `NoOpReranker`（`crates/core/src/rerank/noop.rs`，`take(top_n)` 直通），既是 V1 交付、也是 Step 7 的**逃生舱** |

> **2026-09-14 V2 Step 7 详细设计已出**（`v2-step7-design.md` v0.1，待评审）：交付形态 = `Reranker` trait 保持（`crates/core/src/rerank/mod.rs`，`fn rerank(&self, query, hits, top_n)`）＋ **`LocalReranker`**（fastembed `TextRerank` + `RerankerModel::BGERerankerV2M3`）＋ `NoOpReranker` 保留；参数面 `Config::reranker`（`crates/core/src/search/config.rs`）**已就位**（V1 遗留），Step 7 **零签名破坏**。⚠️ 设计期发现（详见设计 §2）：候选窗口 `candidate_window` **需新增**，且 `fused` 在精排前先 `take(k)`（`crates/core/src/query/searcher.rs` 的融合后截断处）会**静默截断**候选池 ⇒ **R > 3k 时窗口被夹到 3k**（`candidate_k = k.saturating_mul(3).max(10)`，同文件的 `candidate_k` 计算处）。⚠️ **行号沿革（2026-09-16 更正）**：`plan-v2.md` §4.0 与 issue `#23` 曾记 `:196`，2026-09-14 更正为 `:286`；**实装后再度漂移**（实测 `take(take_n)` = `:337`、`candidate_k` = `:149`、`rerank(...)` = `:375`）⇒ 按架构 §14.5 锚点规则**改符号指代** |

---

## 6. 非功能性需求

### 6.1 规模与性能

| 编号     | 类别   | 指标                                               | 备注                                 |
| ------ | ---- | ------------------------------------------------ | ---------------------------------- |
| NFR-01 | 规模   | 1 万 chunk（MVP 目标），10 万（架构上限）                     | 单机内存；P5 实测 12K chunk 构建+检索全程无错 ✅            |
| NFR-02 | 查询延迟 | BM25 < 5ms；向量 < 10ms；混合 P99 < 20ms               | P5 实测（release，6400 样本/模式）：bm25 P50=1.13/P99=3.90 ✅；vector P50=4.26/P99=8.37 ✅；hybrid P50=4.41/P99=8.70 ✅。**V2 Step 1 重测**（合成 fixture，release）：1 万级 bm25 0.42 / vector 3.54 / hybrid 3.46；**10 万级 bm25 2.43 / vector 5.81 / hybrid 3.46** —— 无过滤路径未退化 ✅。⚠️ **带过滤路径未纳入 NFR-02**：10 万级上选择度 ≤1% 时 vector P99 达 41~158ms（超限额 4~16×），见架构文档 **R18**；2026-09-07 起该场景由 **NFR-13** 单独纳入口径，不再只是「已知取舍」） |
| NFR-03 | 索引构建 | **双口径（2026-09-07 修订，D-J8）**：① **首次全量** 1 万 chunk 含 embedding < **240s**；② **增量追加** 10% 文档 < 首次全量 × 10% × 1.2 | 原单口径「< 120s」**不可达**：P6 重测 225.5s（embed 209.9s，12K）；根因 ort CPU 推理 ~51 条/s。已排除的三条路径：量化（D-J6，fastembed 6.0.2 无中文量化变体）、调批次（32/64/128/256 → 62.6/59.1/54.4/51.6 条/s，差异 <20%）、并行建图（只占 4%）⇒ 在「不换模型」前提下无已识别路径可补 1.6× 差距。**首次全量口径实测达标 ✅**（P5 236.7s / P6 225.5s，均 < 240s）；增量口径由 FR-28（增量构建）落地，见 `plan-v2.md` **Step 6**。**2026-09-11**：Step 6 详细设计已出（`v2-step6-design.md` v0.1）—— ① 口径 T7-09（多 session / CoreML EP）**只承诺 S 级 spike**，产出「投 / 不投」结论，**不承诺**把 225.5s 压到 120s（embed 占 93%，不换模型则上界收益 <7%）；② ② 口径由 **T7-11（`helix build --index` 追加）** 落地，骨架已在（`content_hashes` 入快照 ⇒ load 后仍能按 `content_hash` 短路跳过 embed）；**2026-09-11 评审收口（Q5 已拍板）：10% 的单位 = 文档数**（`NFR-03 ②` 原文即「追加 10% 文档」），**chunk 数只作参考指标、不进判据** |
| NFR-04 | 冷启动  | 快照 + 图加载 < 2s                                     | **✅ 已达标（V2 Step 2 / ADR-A，2026-09-06）**：完整冷启动 ≈100ms（实测 82~100ms，快照 57~77ms + 图 23~37ms），余量 20×；图 dump 43.5ms。⚠️ 达标依赖图 sidecar 命中，降级路径仍 ~10s（配 NFR-07「降级不得静默」）；磁盘代价 1.6×。P5 时代「图重建 11.57s（D7）」的历史口径见下方注记 |
| NFR-05 | 内存   | 1 万 chunk 向量部分约 20MB（512 维 × 4B）                 | P5 实测：向量 24.6MB（12K）✅ 吻合；**峰值 RSS 383MB / P6 重测 372MB —— 口径 = 检索路径（batch 1，**不含精排器**）**（主导为模型 + ort 运行时，非向量）。<br>**2026-09-11（Step 6 设计）**：⚠️ T7-09 若投 **E2（多 session）**，N 份 session 的 arena / 工作区会**抬高峰值** ⇒ 峰值项必须重测；spike 决策门已含「峰值 RSS 增量 ≤ 20%」。<br>**2026-09-13 结案（S6-10）**：① **T7-09 判「不投」**（E2 增益 +11.7% / +16.0%，而峰值 RSS **+96.4% / +289.2%** ⇒ 两侧门槛都不过）⇒ 多 session 的抬升**不发生**，架构 **R36** 随「不投」消解；② 🔴 **本行补口径限定词「（检索路径，batch 1）」**（Q8 方案 C）—— **建库路径**（batch 64 × 长文本）峰值 RSS 实测 **2.32~3.33 GB**，与 372MB **差一个数量级**，不加限定词会让人「按 372MB 规划内存」而在建库时被 OOM kill。建库路径的内存预算**单独立为架构 R41**（形态按 `batch × 序列长度` 给、**不按语料规模给**；`eval-report.md` §8.10.4 三点实验已钉死「RSS 与**语料规模解耦**、1 batch 后饱和」），**数值待定**；本行**不把建库路径并入**这条历史基线（并入会让「372MB」的含义变混、反而降低可判定性）。<br>**2026-09-14（Step 7 设计）**：⚠️ **精排器会独立抬高「检索路径」的峰值** —— `rozgo/bge-reranker-v2-m3` 的 `model.onnx.data` 实测 **≈2,187MB（FP32，sha256 `84b66c78…8945`）**、**加载后常驻**（`TextRerank` 持有 session）⇒ 本行 **372MB 明确不含精排器**；精排内存预算**单独立为架构 R44**（形态按「模型参数 + 单 batch 激活」给、**数值已回填（2026-09-16，S7-05 收尾）**：检索路径 / 冻结图 / 含模型加载 / 3 次取中位 ⇒ 峰值 RSS 由「精排关」的 **403.7 MiB** 升到「精排开（`R = 20`）」的 **3110.5 MiB**（min 2959.1 / max 3488.9）⇒ **增量 2.64 GiB、7.71×**，且该增量是模型文件体积（2.12 GiB）的 **1.25×**（ONNX Runtime 运行时 + 单 batch 激活 + tokenizer）；读数见 `eval-report.md` §8.14.10），**不并入** 372MB 基线（理由同 R41：并入会让「372MB」含义变混、反而降低可判定性）。设计锚点 `v2-step7-design.md` 附录 C / D-S7-10 |
| NFR-10 | 并发读吞吐 | **三部分（2026-09-13 修订，V2 Step 6 收尾）**：① **前置条件（正确性）** —— `Searcher` 跨线程并发检索，各线程的 `hits`（`chunk_id` + `score`）序列**逐位等于**单线程；② **吞吐判据（适用范围：不含查询侧编码的读路径）** —— `--threads 4` 的 QPS ≥ `--threads 1` × **2.5**（近线性，允许 40% 折损）；③ **编码侧并发（保持「打开」，未纳入 ②）** —— 含**查询侧编码**的端到端路径（vector / hybrid）并发吞吐**当前不达标**（4 线程 1.88× / 1.63×），根因 = 查询侧编码被**单 session 互斥**串行化（架构 **R43**）。**测量口径**：预热丢弃首轮 + 顺序交错（1/2/4/8/1/2/4/8）+ 报告运行范围（核数 / 是否插电 / 是否在 CI）。⚠️ 并发下 per-query 延迟**含排队**，**不得与 NFR-02（单线程口径）横比** | V2.0 **Step 6** 实测（`plan-v2.md`；前置：Step 5 的 T7-23 把 `Metrics` 暴露出来）。**2026-09-11 口径修订**：原口径全文只有「多线程并发检索吞吐与正确性」的定性描述、**无任何数字 ⇒ 当天无法判定达标**；评审在「相对提升（近线性）/ 绝对 QPS / 只记录不判定」三方案中**拍板方案 A**（设计文档 §4.6.3 / D-S6-08）——否决绝对 QPS（与机器强耦合，违背「性能数字不具跨机可引用性」的既定立场）与「只记录」（会使本口径永不关闭）。采集点 = `helix bench --threads N`（设计 §4.6）。<br>**2026-09-13 S6-08 实测**（12K / 320 queries / `--reps 20` / 档位 `1,2,4,8`；10 核 / 插电 / 非 CI；`eval-report.md` §8.12 为**唯一权威数据源**）：`QPS(4)/QPS(1)` = **bm25 2.98× ✅ / hybrid 1.63× ❌ / vector 1.88× ❌** ⇒ ×2.5 这个「拟」值**至此定稿**（不再标「拟」）。<br>**2026-09-13 S6-10 裁定（三条并记）**：㈠ ② 的适用范围**收窄**为「**不含查询侧编码的读路径**」——bm25 即该路径的实测证据（2.98×，余量 **19%**）；㈡ **NFR-10 整体保持「打开」、不关闭**——③ 是**如实记录的未达标项**，不是 D-S6-08 已否决的「只记录不判定」；㈢ **复审触发条件 = 查询侧会话池（每 worker 独立 session）落地时** —— 届时须**先答挂起的 Q3**（不同 `sessions`/`intra_threads` 是否改变向量数值；若改变会破坏 ① 的逐位一致），再把 ③ 并入 ② 重测。<br>⚠️ **措辞更正（评审 P3-8）**：本行原末尾写「读路径**零**共享可变状态 ⇒ 近线性是强先验」——该先验**只对索引读路径成立**（bm25 为证）；**端到端**的 vector / hybrid 还含**查询侧编码**（`crates/core/src/embed/local.rs:35` 的 `Mutex<TextEmbedding>`），**不受此先验覆盖**。**2026-09-16（Step 8 设计已出，`v2-step8-design.md` v0.1）**：③ 的出口已落为 **T7-26（查询侧会话池）**，形态 = `Vec<Mutex<TextEmbedding>>` 池（⚠️ fastembed **无 `sessions` 参数** —— `InitOptions` 只有 `intra_threads` ⇒ 原措辞「不同 `sessions`」在实现面上不存在；**挂起的 Q3 已改述为 Q3′**：不同 `intra_threads`（及实例数）是否改变 `embed_query` 的向量**数值** —— 若改变则池化**破坏 ① 的逐位一致前置** ⇒ **必须判「不投」**）。⚠️ **投 / 不投由 spike S8-S2 判定、不预先承诺**（Step 6 的 E2 教训）；**若判「不投」，本行 ③ 如实记为未达标、R43 保持打开** —— **不允许用「设计已给出出口」代替实测**。⚠️ **默认 `embed_sessions = 1`** ⇒ **不开就不付**（架构 **R54**） |
| NFR-11 | 增量可见性与写延迟 | **口径修订（2026-09-07）**：`commit()` 后立即可查；写延迟有界（= flush 一批的耗时） | ⚠️ 原表述「增量写入后立即可查」与实现语义不符：`add` 之后必须 `commit()` 才对检索可见（`search/index.rs:5-6`，对齐 Lucene/tantivy）。V2.0 **Step 6** 实测（plan-v2.md）。**2026-09-11（Step 6 设计）**：落地路径 = `helix build --index` 追加（设计 §4.5）；写延迟口径 = 「`load` → `add` → `commit` → 可查」的端到端耗时，测试 S6-T10 同时覆盖「追加后 `save` → `load` 往返、同快照两次加载逐位一致」（NFR-06 不破）。**2026-09-13 收尾（S6-10）结案**：① 落地路径已由 **S6-07 / PR #43** 实现（`helix build --index` 追加）；写延迟**旁证** = `eval-report.md` §8.11 的 `21.412 s / 18.75 批 ≈ 1.14 s/批`（⚠️ 该值是**从累计 embed 耗时推算**、**不是直接计时**）；② 🔴 **S6-T10 原设计承诺、实现期漏做**（S6-10 自查发现）⇒ **已补齐**（`crates/core/tests/step6_incremental_build.rs`，含「同快照两次加载逐位一致」与「往返不丢顺序」两条 + 非空反向自证）；③ NFR-11 的**写延迟目标值仍未定**（当前只有旁证，无判据）——**不标「已达标」**，留待后续按需标定。**2026-09-16（Step 8 设计已出，`v2-step8-design.md` v0.1）—— 口径已定、拟值已给**：① **口径** = `load` → `add` → **`commit()`** → 可查 的**端到端 wall time**，且**必须直接计时**（不再是「从累计 embed 耗时推算」——§8.11 的 1.14 s/批 是本行唯一的旁证，**且是推算值、禁止当实测引用**）；② **拟值** = **`P50 ≤ 1.0s / P99 ≤ 2.0s`**（`batch_size = 64`、CPU/FP32、**不含首次模型下载与加载**），**待 S8-08 标定后定稿**（同 NFR-13 / NFR-10 先例：先落「拟」→ 标定后定稿，D-S8-10）；⚠️ **本行仍不标「已达标」** —— 拟值不是实测 |
| NFR-12 | 精排延迟 | **三部分（2026-09-16 **定稿**，S7-05 收尾）**：① **延迟判据（双列；**CPU / FP32 口径已定稿**，GPU / 量化标「拟」）** —— 在**同一张冻结图**上（`--index` 读快照 + `--runs 1`）做「`--rerank-window` **不传**（= 关闭，控制组 A）」vs「`--rerank-window <R>`」的 A/B，**两条轴分列报告**：㈠ **端到端 `took` P50 / P99**（口径 = 一次 `search` 全链路）；㈡ **精排段 `rerank_elapsed` P50 / P99**（口径 = **仅 `rerank()` 调用本身**，**不含候选回捞、也不含 `explain` 组装**——后者由 D-S7-06 推迟到最后 ≤ k 条上做；**定稿 = 双口径**（2026-09-16，S7-04 实测）：**㈠ CPU / FP32 上界** —— 默认 `R = 20` 下 **P50 ≤ 8.8 s、P99 ≤ 10.9 s**（实测 **8.78 / 10.86 s**；M5 10 核 / AC 插电 / 非 CI / 模型已缓存；测量期间另有 ~370% CPU 的无关负载 ⇒ **读作上界**）；**㈡ GPU / 量化模型 = 待测**（无实测 ⇒ **只留形态、不给数字**，标「拟」）。⚠️ **适用范围**：精排**默认关**（D-S7-04）⇒ 本条**只在显式传 `--rerank-window` 时成立**；⚠️ **与 `R` 联动**（`R` 定稿 **20**，D-S7-01；实测成本 ≈**0.43~0.48 s/文档**、与 `R` 近似线性）。🔴 **原「≤ 500ms」（拟，对应 `R = 10` 档）已废弃**：实测 `R = 10` 的 P99 = **5220 ms**（差 **10.4×**）⇒ CPU-only / FP32 / 未量化下**不可达**（`eval-report.md` §8.14.6）。**独立口径、不并入 NFR-02**（NFR-02 只管 bm25 / vector / hybrid 三个阶段各自的延迟预算，精排是**其后的独立阶段**，且精排开启后 NFR-02 的 20ms **名义失效** ⇒ 必须另立口径）；② **质量判据（与 FR-18 验收同源）** —— 精排开启后 hybrid **MRR@10(1)** 相对**同图**基线**可测提升**（默认 **`R = 20`** 定稿，D-S7-01；**S7-04 实测**：hybrid `ΔMRR` = **+0.0384**，其中 **87.1% 来自「窗口」放开**、拐点在 `R = 20`（`20→50` 的边际降到首段的 **6.6%**）——⚠️ **分层 60 子集**口径、**不可与全量 320 横比**；⚠️ **配对符号检验不显著**（二项精确单侧，最小 p = **0.1553**）⇒ 按**幅度**读、**不声称统计显著**；⚠️ 同一子集上 **`vector` 在 `R = 50` 反而下降** ⇒「窗口越大越好」不成立。决策门见 `v2-step7-design.md` §4.6、读数与局限见 `eval-report.md` §8.14.4 / §8.14.5 / §8.14.9）；③ **可观测（NFR-07 延伸）** —— 精排段的耗时与窗口**必须进 `SearchResponse::metrics`**（`rerank_elapsed` / `rerank_window`，对齐 `bm25_elapsed` / `vector_elapsed` 的既有纪律），且「`score` 已被精排替换」必须**有唯一信号**（`Explain.rerank_score.is_some()`，D-S7-05）。⚠️ **「未启用精排」不是运行时降级**：`local-rerank` feature 未编译时 `LocalReranker` **编译期不存在**（同 Step 6 D-S6-05 的硬失败纪律）⇒ 本 Step **不设**运行时降级状态字段（设计 §4.4.5 的有意取舍）。✅ **已于 2026-09-16（S7-05 收尾）定稿**：原「≤ 500ms（拟）」**废弃**（实测差 **10.4×**）⇒ 改为上面的**双口径**；标定语料 = **T2Ranking 12K**、**分层 60 子集**（⚠️ 原「全量 320」协议按实测成本复算 ≈**262 小时**、本机不可行 ⇒ 缩减为分层 60；**理由与「不可横比」后果**见 `v2-step7-design.md` §4.6.5） | V2.0 **Step 7** 落地（`plan-v2.md`）。**H3 的取形**：设计把「精排候选窗口」做成**可配 `R`、默认 20（拟）**（D-S7-01）——**不再把窗口钉死为某个魔数**，标定由 S7-04 回填。**2026-09-14 设计期口径成形**：① 原口径只有「rerank P99（独立口径）」**一行、无阈值、无测量口径、无质量判据 ⇒ 当天无法判定达标**；② ⚠️ **必须在冻结图上做 A/B**：P5 锚点 `hybrid MRR@10(1) = 0.6922` **自身带 ~0.6% 图漂移**（2026-09-14 复现 **0.68815**；**bm25 三项逐位复现 Δ=0** 作对照 ⇒ 唯一变量是未播种的 HNSW 图）⇒ 若每次重跑都重建图，A/B 的差会被图漂移淹没、**不可归因**（设计 §3.1 / S7-T11 自证）。③ 采集点 = `helix bench --rerank-window <R>` 的 **`took` 与 `rerank_elapsed` 两张直方图**（端到端与精排段**分列**）（设计 §4.5 / §4.6）；harness 报数**必标运行范围**（核数 / 是否插电 / 是否 CI / 是否含模型冷加载）。④ ⚠️ **候选池联动**（⚠️ **以下为 2026-09-14 设计期口径**；实装已满足，见末句）：设计期 `candidate_k = k.saturating_mul(3).max(10)`（`crates/core/src/query/searcher.rs` 的 `candidate_k` 计算处）既是各 lane 的召回目标、又是融合输出的**上限**，而编排层在精排前先 `take(k)`（同文件的融合后截断处）⇒ **`R > 3k` 时实际窗口被静默夹到 `3k`**（设计「发现 A」）；⇒ **要求**：窗口必须与之联动，否则 R=100 在 k=10 时退化成 30。✅ **实装已满足**：`candidate_k = k.saturating_mul(3).max(window).max(10)`、融合后截断改为 `take(take_n)`（均在 `searcher.rs`；⚠️ 设计期用的**本仓行号** `:118` / `:286` 实装后已漂移（实测 `:149` / `:337`）⇒ 按架构 §14.5 锚点规则改**符号指代**） |
| **NFR-13** | **低选择度过滤延迟** | 10 万级语料、选择度 ≤1% 档位的**端到端**过滤检索 P99 有界（**含过滤求值**，非仅向量路）。**双口径（2026-09-10 S5-04 标定定稿，D-S5-04）**：① **常规档位（字段索引命中）≤ 20ms**；② **降级字段档位 ≤ 35ms**（其过滤求值自身 ~8.8ms，属 D-S5-09 已接受成本，不计入向量路） | **新增（2026-09-07，D-J9）。✅ 已实施并标定定稿（V2.0 Step 5 / S5-04，2026-09-10）**。此前该场景**不在任何 NFR 口径内**：10 万级 sel-0.1% 档 vector P99 达 158ms（超 NFR-02 限额 16×），见架构 **R18**。方案：`allowed ≤ 阈值`（**8192**，S5-04 定稿）时绕开 ANN、直接精确扫描。**实测**（10 万级，全表见 `eval-report.md` §8.9）：常规档位最差 **7.06ms**（`sel-0.1%` hybrid，余量 2.8×）；降级字段档位最差 **22.84ms**（`ts-range-degraded` hybrid，其中过滤求值 8.77ms、向量路仅 3.15ms）；对照**兜底前**同档为 **112.69 / 223.24ms**。⚠️ **成本口径修正（2026-09-10，Step 5 设计 v0.3 §4.2.4）**：D-J9 写的「约 0.05ms」只覆盖「对 ~100 个候选算距离」一段，**不含 `O(N)` 遍历定位候选** ⇒ 端到端口径必须计入。⚠️ 该口径**只覆盖低选择度子集**（`allowed > 8192` 仍回路径 B，R18/R13/R11/R17 均不得标「已解决」） |
| **NFR-14** | **读写并发下的读延迟与可见性** | **（2026-09-16 定口径、拟值待标定**；原为「草案、待评审」，2026-09-16 新增，替代 Step 8 此前只有的「FR-17 §5.3.5 四条定性验收」）三部分：① **可见性** = **`commit()` 后立即可查**（对齐 NFR-11）；② **读延迟** = 合并期间持续发起检索**无失败、无超时**，且 P99 **仍在 NFR-02 预算内**；③ 不得以「重建索引中」为由拒绝查询。⚠️ 数值与测量口径由 `v2-step8-design.md` 定稿（先落「拟」、标定后定稿，同 NFR-13 / NFR-10 先例） | ⚠️ **Step 8 此前无编号 NFR**（`plan-v2.md` §附-4 P0 缺口）；⚠️ **FR-17 §5.3.5 的「写入后立即可查」与 NFR-11 的「`commit()` 后立即可查」口径不一致** ⇒ Step 8 设计期须一并对齐（取形：可见性 = commit 后，**「读能看到未 commit 的写」明确不做**，`plan-v2.md` §4.0 H5）。**2026-09-16（Step 8 设计已出，`v2-step8-design.md` v0.1）—— 本行口径已定（由「草案」升为「定口径」）**：① 可见性 = **`commit()` 后立即可查**（已同步改写 **FR-17 §5.3.5** 的措辞）；② 读延迟 = **②a 相对判据**（**合并期 ÷ 静止期**的端到端 `took` P99 **≤ 1.2（拟）**，**同机 / 同图 / 同 query 集**）+ **②b 绝对护栏**（合并期 P99 **仍在 NFR-02 预算内**）—— ⚠️ **相对为主、绝对为护栏**：避免把「分段的基线变化」与「合并的干扰」混在一起读（设计 Q7）；③ 不得以「重建索引中」为由拒绝查询 ⇒ 设计把「**库内不存在该类错误码**」变成**可证伪断言**（S8-T10 以 `grep` 断言 `Error` 枚举无该变体）。⚠️ **②a 的 1.2 标「拟」**，待 **S8-09** 标定后定稿；读数落 `eval-report.md` **§8.15**（S8-T11 采集） |
| **NFR-15** | **自适应融合的质量判据** | **（草案，待评审**；2026-09-16 新增，**Step 9 此前无任何 FR / NFR 编号**）在**分桶**上优于固定权重。⚠️ 指标（建议 hybrid MRR@10(1) 为主）/ 阈值 / **统计口径**须在设计期定值 —— Step 7 的教训是**配对符号检验不显著**（最小 p = **0.1553**）⇒ 须**预先声明**「按幅度读、不声称显著」或给出可行样本量 | ⚠️ 与原表其余判据同族：**A/B 必须钉在同一张冻结图**上（`--index` 读快照 + `--runs 1`） |

### 6.2 确定性、可观测、兼容性、合规

| 编号     | 类别  | 指标                                               | 备注                                 |
| ------ | --- | ------------------------------------------------ | ---------------------------------- |
| NFR-06 | 确定性 | 相同 query + 相同索引 → 完全相同结果                         | **V2 Step 2 口径细化**：图持久化（FR-29）把拓扑冻结进 sidecar，故「**同一快照连续加载两次**」的 Top-10（chunk_id + score）**逐位一致** ✅。<br>⚠️ 口径**不是**「两次建库结果一致」——`hnsw_rs` 用 OS 熵（`StdRng::from_os_rng()` 无 seed API），两次建库拓扑本就不同。**原备注「HNSW seed 固定」是过时论据，已删除**。<br>另：tie-break by chunk_id（`chunk_id` 升序，`crates/core/src/query/searcher.rs` 融合段）。<br>**2026-09-14（Step 7 设计）补精排锚点**：精排**不新增不确定性来源**——它只对候选窗口内 hits 的 `score` 做**二次单调变换**（模型对 (query, doc) 的打分是确定性前向），**改用 `chunk_id` 升序**（⚠️ **2026-09-16 更正**，S7-05 收尾：原写「不改 `chunk_id` 序（同分时稳定排序保持输入序）」—— 机制描述与 **D-S7-07 及实装相反**；实装在 `crates/core/src/rerank/scoring.rs` 的 `apply_scores` **刻意不沿用** `fastembed` 的 stable sort，并列时按 **`chunk_id` 升序**。**本口径的结论不受影响**：重排仍是**逐位确定**的）⇒ 本口径的判据仍是「**同一快照两次加载 Top-10 逐位一致**」。⚠️ **不得把判据升级为「跨 batch 组成仍逐位一致」**：`PaddingStrategy::BatchLongest`（`fastembed` `src/common.rs`）使 padding 长度随**同 batch 内最长文本**变化 ⇒ 同一文档在不同 batch 组成下分数**可有微小差异**；故 A/B 必须**固定 batch 组成**（同一冻结图 + 同一 `--runs 1` 单次建库 + 同一 `batch_size`）。`max_length` 截断（**512** token，`RerankInitOptions`）同理属**输入侧**确定性、不破本口径。<br>✅ **2026-09-16 实测（S7-T9 / P3，S7-05 收尾补收读数）**：同一 `(query, doc)` 的**原始 logit 在「单条」与「批内」两种组成下相同**（`2.2603965` = `2.2603965`，`eval-report.md` §8.14.11）。⚠️ 但这是**单一样例、单一批组成**的读数 ⇒ **不构成**「跨 batch 组成恒逐位一致」的证明，故上一条「**不得把判据升级**」**仍然成立** |
| NFR-07 | 可观测 | 每次检索输出 `took`、各 lane 耗时、候选数；**降级不得静默**                      | **V2 Step 2 扩口径**：原只有检索侧字段。「降级可观测」是延伸——图 sidecar 不可用时必须**显式**告知（否则 NFR-04 会静默失效而不自知）。实现为 `GraphStatus { Loaded, Rebuilt(reason), NotApplicable }`，CLI / `SearchIndex::graph_status()` 均可读。<br>**V2 Step 5 补口径落地（2026-09-10，设计已定 D-S5-05~08，待 S5-05~07 实现）**：`query::Metrics` 目前只在 `search_parts` 内聚合并经 `tracing` 输出，**外部拿不到实例**（无法单测 / bench 采集）⇒ 改为进 `SearchResponse`（D-S5-05），并补 per-lane 耗时（D-S5-06）、`vector_route` 路径可见性（D-S5-07）、修两条早退路径 `took` 恒 0（D-S5-08）。<br>**2026-09-14（Step 7 设计）补精排可观测面**：`SearchResponse::metrics` 追加两个字段——`rerank_elapsed`（精排阶段**自身**耗时，与 `bm25_elapsed` / `vector_elapsed` 并列，供 NFR-12 采集）与 `rerank_window`（**精排里实际拿到多少候选**；`== k` ⟺ 窗口**没被放开**，是「放开是否真的生效」的直接证据）。**「不得静默」在本 Step 的落地**是 `Explain.rerank_score.is_some()`（`score` 已被替换的**唯一**信号）+ `local-rerank` feature 未启用时 `LocalReranker` **编译期不存在**（而非运行时静默退化）。⚠️ **本 Step 不设运行时降级状态字段**——这是设计的有意取舍（`v2-step7-design.md` §4.4.5）。字段既不进 `Config::fingerprint()`（不影响快照兼容）、也不改 `Hit` 结构（`Hit.score` 的语义变化由 **D-S7-05** 待评审拍板）。落地见 `v2-step7-design.md` §4.3 / §4.4 |
| NFR-08 | 兼容性 | Rust **1.90+**，2021 edition                      | MSRV 由依赖决定（`bincode` 1.85 / `clap` 1.85 / `smol_str` 1.89 / `roaring` 1.90），经 `rust-toolchain.toml` 固定。原定 1.80 已不可行，见 ADR-008 |
| NFR-09 | 许可合规 | 全部直接依赖与传递依赖均为宽松许可；出现 GPL/LGPL/AGPL 系列即视为不达标 | 由 `cargo-deny` 白名单自动校验（ADR-008）       |

> ⚠️ NFR-02 ~ NFR-05 已由 P5 实测校准（`eval-report.md` 第 8 节）：NFR-02/04/05 达标，
> **NFR-03 构建未达标**（P6 重测 225.5s > 120s，embed 吞吐限制）⇒ **2026-09-07 已按 D-J8
> 拆为「首次全量 / 增量追加」双口径**，首次全量目标调整为 240s（见上表）。
> NFR-04 的「完整冷启动未达秒级（图重建 11.57s，D7）」已由 **V2 Step 2 图持久化解决**：
> 冷启动 ≈ 100ms（快照 57~77ms + 图 23~37ms，除「<2s」外一律给范围），达标；图 dump 仅 43.5ms。
> ⚠️ 代价是磁盘 +60%（52.3MB → ≈84MB，1.6×），且**达标依赖图 sidecar 命中**——
> 降级路径仍是 ~10s，故必须与 NFR-07 的「降级不得静默」配套阅读。

### 6.3 质量属性优先级

当质量属性冲突时，按以下顺序取舍：

| 序 | 质量属性        | 取舍说明                                     |
| - | ----------- | ---------------------------------------- |
| 1 | **正确性 / 确定性** | 结果不对比慢更糟；同输入必须同输出，否则无法调试与回归              |
| 2 | **首条精确率（MRR）** | Agent 无翻页，第一条错了后面全白搭；优先于召回率              |
| 3 | **延迟 P99**  | 在 NFR-02 上限内越高越好，但超出上限即视为不达标             |
| 4 | **召回率（Recall）** | 重要，但低于前三者                                |
| 5 | **资源占用 / 索引体积** | 最后考虑，MVP 阶段不为体积牺牲正确性                     |

---

## 7. 明确不做（Out of Scope）

以下内容**第一版明确不做**，避免范围蔓延：

| 不做                                  | 原因                         |
| ----------------------------------- | -------------------------- |
| 分布式 / 分片 / 副本                       | 单机 MVP 优先，架构留位             |
| HTTP / gRPC 服务层                     | 先以库 + CLI 形态交付             |
| PDF / Word / HTML 文档解析              | 属于摄入层，非检索内核职责              |
| 查询改写 / HyDE / 多轮对话管理                | Agent 应用层职责，内核只留多 query 接口 |
| 权限 / 多租户隔离（鉴权、租户边界、访问控制）             | 范围外——内核只提供命名空间过滤机制（FR-32），鉴权/策略归场景层 |
| 图检索 / 关系推理                          | 不在本期范围                     |
| posting 压缩（varint / roaring bitmap） | MVP 用朴素 `Vec<u32>`，P4 优化项  |
| 查询语法（布尔、短语、通配符）                     | MVP 只用 OR 语义，位置信息先存好备用     |

---

## 8. 假设、依赖与约束

### 8.1 已定约束

| 约束       | 内容                                        |
| -------- | ----------------------------------------- |
| 语言       | Rust **1.90+** / 2021 edition（NFR-08，由依赖 MSRV 决定） |
| 项目许可     | `MIT OR Apache-2.0` 双许可（Rust 生态惯例，与上游依赖兼容度最高） |
| 依赖许可     | 仅允许宽松许可（MIT / Apache-2.0 / BSD-3-Clause / BSL-1.0 / CC0-1.0 / Zlib / ISC / Unicode-DFS-2016），由 `cargo-deny` 强制（NFR-09） |
| 依赖锁定     | `Cargo.lock` **必须入库**（`fastembed` 精确锁定 `ort =2.0.0-rc.13`，不锁定则构建不可复现） |
| 规模形态     | 单机 MVP，万级文档，内存索引                          |
| 交付形态     | library crate + CLI + 示例中文语料              |
| 场景假设     | 不做场景假设，内核通用（见 2.3）                        |
| BM25 实现  | 倒排索引与打分**完全手写**，不引入 tantivy              |
| 持久化      | 内存索引 + 二进制快照，不引入嵌入式 KV                    |
| Embedding | 默认本地模型，远程 API 作为可选 feature                |

> 以上约束来自项目立项时的决策，变更需回到本文档修改并同步架构文档。

### 8.2 外部依赖

| 依赖项         | 状态                                                 | 阻塞阶段   |
| ----------- | -------------------------------------------------- | ------ |
| Rust 工具链    | ✅ **已安装**：stable 1.98.0；项目经 `rust-toolchain.toml` pin **1.90.0**（NFR-08）。原"未安装"为误判——探测跑在非登录 shell，`~/.cargo/bin` 不在 PATH，详见 `p0-design.md` 12.1 | 无 |
| ONNX 模型下载   | ✅ **P0 已验证通过**：`Xenova/bge-small-zh-v1.5`，96 MB，直连 49s 完成（未用镜像）。⚠️ 该仓在 HF 未声明 License，见 8.3 | 无 |
| 示例中文语料      | ✅ 已备 30 篇 demo（`data/corpus.jsonl`，FR-23 / README 演示用） | 无 |
| 评测数据集       | ⬜ 待装配：**开源 T2Ranking 数据集转换**（Apache-2.0，SIGIR 2023；HF 直下无注册墙），转换产物 `data/t2-corpus.jsonl`（~12K 段落）+ `data/t2-queries.jsonl`（~320 条），见 9.4 与 `p5-design.md` | P5（T5-01/T5-02） |
| crates.io 访问 | ✅ **P0 已验证**：直连可用（`index.crates.io` RTT 0.37s），镜像配置已写入 `~/.cargo/config.toml` 但默认注释 | 无 |

### 8.3 项目风险（非技术风险）

| #  | 风险                    | 影响                        | 应对                                       |
| -- | --------------------- | ------------------------- | ---------------------------------------- |
| P1 | ~~示例语料规模太小~~                            | ~~评测结果无统计意义，混合检索的优势无法证实~~      | **已缓解（2026-09-03）**：评测语料改用 T2Ranking 转换（~12K 段落 + ~320 查询，见 9.4），规模与真实性均远超原方案           |
| P2 | ~~评测标注质量差~~                           | ~~指标失真，调参调向错误方向~~            | **已消除（2026-09-03）**：T2Ranking 为专业团队标注（4 级相关性，每对 ≥3 人多数票），不再自标注，标注者偏差整体消除           |
| P3 | embedding 模型与语料域不匹配  | 向量路效果差，混合检索可能不优于 BM25     | 先验证单路效果再谈融合；如实记录不达预期的实验结果（见 9.5）        |
| P4 | ~~Rust 工具链与依赖安装阻塞~~ | ~~全部编码工作停滞~~                | **P0 已解除**：工具链已装、依赖全部可编译、模型可下载             |
| P5 | **模型仓未声明 License**   | 合规审查时被质疑（**非法律风险**：上游 `BAAI/bge-small-zh-v1.5` 为 MIT，Xenova 为公开转换仓） | **已决策（2026-09-02）：接受，采用选项 A**。fastembed 实际拉 `Xenova/bge-small-zh-v1.5`，该仓 HF `license` 字段为空，按"沿用上游 MIT"接受。**残留风险**：仅为推断，无正式声明。**复核时机**：P2 实现 `Embedder`（T2-02）时复查；若届时要求严格合规，回落路径为选项 B（自导 ONNX）或 C（换声明了 License 的模型）。详见 `p0-design.md` 12.5 |
| P6 | **评测子集装配引入偏差**（2026-09-03 新增） | 指标绝对值失真（随机负例潜在假负例、启发式分桶不完美、真实搜索查询 ≠ Agent 生成查询） | ① 转换器固定种子可复现；② 装配局限在评测报告中强制披露（见 9.4/9.5）；③ 与论文基线仅作量级对照；④ Agent 查询代理偏差作为已知局限记录，不在 v1 修复 |

---

## 9. 验收与评测要求

### 9.1 验收方式

| 层级    | 内容                            | 对应架构文档章节    |
| ----- | ----------------------------- | ----------- |
| 单元测试  | 算法正确性、接口一致性、统计量维护；**含与 `tantivy` 的 BM25 排序对照** | 架构文档 12.1、ADR-007 |
| 集成测试  | 端到端链路（摄入 → 提交 → 快照 → 加载 → 检索） | 架构文档 12.2   |
| 效果评测  | 本章 9.2 ~ 9.5，在 P5 阶段执行         | `helix bench` |
| 依赖合规  | 全部依赖在 License 白名单内（NFR-09）     | 架构文档 ADR-008；`make deny` |

### 9.2 评测指标定义

| 指标           | 定义            | 用途                         |
| ------------ | ------------- | -------------------------- |
| Recall@10    | 前 10 条中相关文档占比（相关性 ≥ 阈值计为相关，阈值可调） | 衡量召回能力                     |
| MRR@10       | 首个相关文档排名倒数的均值（阈值同上） | 衡量"第一条对不对"，**Agent 场景更重要** |
| NDCG@10      | 折损累积增益（**分级增益 gain = 2^rel − 1**，rel ∈ 0~3） | 综合排序质量                     |
| P50 / P99 延迟 | 端到端检索耗时       | NFR-02 / NFR-03 验证          |

> 相关性等级来自 T2Ranking 的 4 级专业标注（见 9.4）。分级 NDCG 用完整等级信息；Recall / MRR 阈值化以兼容二元口径，阈值经 `helix bench --rel-threshold` 可调。

### 9.3 强制对照实验

**必须做，否则无法证明混合检索的价值。**

| 配置           | 预期                        |
| ------------ | ------------------------- |
| BM25 only    | 术语查询好，改写查询差               |
| Vector only  | 改写查询好，术语查询差               |
| Hybrid (RRF) | **两类查询都不差，整体最优** ← 需被数据证实 |

### 9.4 评测数据集要求

**数据源**：开源数据集 **T2Ranking**（清华 THUIR，SIGIR 2023；License **Apache-2.0**；HuggingFace 直接下载无注册墙）。选择依据：真实中文搜索查询 + 真实网页段落、4 级专业相关性标注（每对 ≥3 人多数票）——**彻底消除"实现者自写语料、自标注"的偏差**。候选对比（DuReader-retrieval / mMARCO-zh / Multi-CPR）与选型过程见 `p5-design.md` 第 3 章。

**装配要求**：经转换器 `t2_prep.rs`（**固定种子，可复现**）从 dev 集装配两份产物：

| 产物                      | 规模                | 要求                                                         |
| ----------------------- | ----------------- | ---------------------------------------------------------- |
| `data/t2-corpus.jsonl`  | ~12K 段落           | qrels 池内段落全集 + 均匀采样负例补足；真实中文网页段落，词频自然分布；规模对齐 NFR"1 万 chunk"口径 |
| `data/t2-queries.jsonl` | ~320 条查询，**四类各 ~80 条** | 词汇重合度启发式分桶（不依赖任何检索器输出）；携带 4 级相关性标注（T2Ranking qrels） |

四类查询与预期占优一路：

| 查询类型                    | 预期占优的一路   |
| ----------------------- | --------- |
| 精确术语匹配                  | BM25      |
| 同义改写                    | 向量        |
| 中英混合术语                  | 混合        |
| 长句自然语言问句（Agent 典型输入）    | 混合        |

**装配诚信约束**：随机负例存在潜在假负例、分桶为启发式近似——两者必须在评测报告中披露（见 9.5）；与 T2Ranking 论文公布的基线数字（如 BM25 MRR@10=0.359）仅作量级对照，不构成可比基准（子集 ≠ 原全集）。

### 9.5 数据诚信要求

**参数调优**：对 `k1 ∈ {1.0, 1.2, 1.5, 2.0}` × `b ∈ {0.3, 0.5, 0.75, 0.9}` 做网格搜索，以 NDCG@10（分级）为目标选优。

> 如调优后混合检索未能优于最优单路，需**如实记录并分析原因**（常见于：子集装配偏差、embedding 模型与语料域不匹配、真实查询与 Agent 生成查询的差异），**不得粉饰数据**。数据集装配的固有局限（随机负例、启发式分桶）必须一并披露。

---

## 附录 A 需求确认记录

| 决策点      | 结论                                            |
| -------- | --------------------------------------------- |
| 场景定位     | 通用检索引擎内核，场景层后接                                |
| 技术栈      | Rust                                          |
| 第一版能力    | BM25 + 向量 + 混合融合；Rerank 不在第一版但留位              |
| 规模形态     | 单机 MVP，万级文档，架构留扩展位                            |
| 向量索引     | 先集成成熟 crate，抽象成 trait，后续可换自研                  |
| Embedding | trait 抽象，本地默认、远程可选，feature flag 切换            |
| 中文分词     | 中文分词器 + 英文 tokenizer 混合                       |
| BM25      | 完全手写倒排索引                                      |
| 持久化      | 内存索引 + 二进制快照                                  |
| 交付形态     | library crate + CLI + 示例数据集                   |

## 附录 B 参考资料

- Robertson & Zaragoza, *The Probabilistic Relevance Framework: BM25 and Beyond*, 2009 — BM25 理论基础
- Malkov & Yashunin, *Efficient and Robust Approximate Nearest Neighbor Search Using HNSW*, 2016 — HNSW 原论文
- Cormack, Clarke & Büttcher, *Reciprocal Rank Fusion Outperforms Condorcet and Individual Rank Learning Methods*, SIGIR 2009 — RRF 与 k=60 的出处
- BAAI/bge-small-zh-v1.5 模型卡 — 查询侧 instruction 前缀的权威说明

## 附录 C 变更记录

| 版本   | 日期         | 变更                                                |
| ---- | ---------- | ------------------------------------------------- |
| v1.0 | 2026-09-02 | 由 `archive/requirements-and-design_v1.0.md` v1.0 拆分而来。承接第 1、2、3、4、11.3、13 章内容；新增「文档信息」「关键需求详细规约」「质量属性优先级」「项目风险」四节 |
| v1.1 | 2026-09-02 | 依据 `thirdparty.md` 调研结论回写：① NFR-08 的 MSRV 由 1.80+ 上调至 **1.90+**（实测主流依赖 MSRV 已突破 1.80）；② 新增 **NFR-09 许可合规**（依赖 License 白名单，由 `cargo-deny` 强制）；③ 8.1 已定约束新增「项目许可 `MIT OR Apache-2.0`」「依赖许可白名单」「`Cargo.lock` 必须入库」三条 |
| v1.2 | 2026-09-03 | 依据 `p5-design.md` v1.2（评测数据源决策）：① 9.4 重写——评测数据集由"人工撰写 30~50 条自标注"改为 **T2Ranking 开源数据集转换**（Apache-2.0，~12K 段落 + ~320 查询，4 级专业标注），并新增装配诚信约束；② 9.2 指标定义更新（NDCG 分级 gain=2^rel−1，Recall/MRR 阈值化）；③ 8.2 外部依赖与 8.3 风险 P1/P2 同步（P1 已缓解、P2 已消除，新增 P6 评测子集装配偏差）；④ 9.5 数据诚信要求补装配局限披露 |
| v1.3 | 2026-09-03 | P5 实测校准（`eval-report.md`）：① NFR-02~05 的"待实测"替换为实测值——NFR-02 延迟达标（bm25 P99=3.90 / vector 8.37 / hybrid 8.70ms）、NFR-03 构建**未达标**（236.7s>120s，embed 吞吐 ~51 条/s）、NFR-04 快照加载 53~107ms 达标但图重建 11.57s（D7）、NFR-05 向量 24.6MB 吻合理论；② 附实测环境（macOS aarch64 / release / 12K chunk / 6400 样本） |
| v1.4 | 2026-09-04 | V2 需求回写（依据 `plan-v2.md`，已消化 GLM-5.3 评审）：① 新增 FR-26~33（向量软删除/过滤下推/增量构建/图持久化/墓碑回收/原子快照/命名空间隔离/时间衰减钩子）与 NFR-10~12（并发读吞吐/增量可见性/精排延迟）；② 修订 FR-15（向量侧删除）、FR-16（原子性+图持久化）、FR-17（标注部分交付，完整版 V2.1）、FR-18（V2.0 接入 bge-reranker-v2-m3）；③ NFR-03 备注刷新 P6 重测 225.5s、NFR-04 明确「快照+图加载」口径、NFR-05 补 P6 重测 372MB；④ §7「Rerank 模型接入」移出不做清单、「权限/多租户」改为范围外声明 |
| v1.5 | 2026-09-05 | ① FR-26/FR-27 描述改为实现中立——原 FR-26 写「`search_filter` 存活位图」把实现细节写进了需求文档，且与最终定案（存活位图作单一真源 + 谓词下推 + 无过滤走普通 `search()` 的双路径）不符。需求文档只规定**可验收的行为**（不占名额、跨快照有效、成本与语料规模解耦）；② NFR-02 备注补 V2 Step 1 重测数据（1 万级 0.42/3.54/3.46 ms；10 万级 2.43/5.81/3.46 ms），并标注带过滤 vector 路涨 2.3~3.3× 属已知取舍 |
| v1.6 | 2026-09-06 | V2 Step 2（图持久化）落地回写，依据 `v2-step2-design.md` v0.3（ADR-A 已拍板）与 12K 实测：① **FR-29 落定**——补实现方案与实测（完整冷启动 ≈100ms、图 dump 43.5ms、磁盘增量 1.6×）；② **NFR-04 刷新**——指标口径由「快照 + 图加载」收紧为「**完整冷启动**（快照加载 + 图加载 + 位图/字段索引重建）」，实测 76.9+23.3≈100ms **达标**（限额 2s），并补降级路径对照（~10s）说明达标**依赖图 sidecar 命中**；③ **NFR-06 口径细化**——原备注「HNSW seed 固定」是**过时论据**（`hnsw_rs` 用 `StdRng::from_os_rng()`，无 seed API，两次建库拓扑本就不同），改为「图持久化冻结拓扑 ⇒ 同一快照连续加载两次逐位一致」；④ **NFR-07 扩口径**——新增「降级不得静默」，落地为 `GraphStatus { Loaded, Rebuilt(reason), NotApplicable }`；⑤ 6.1 表下小结同步（NFR-04 已达标，代价是磁盘 +60%） |
| v1.7 | 2026-09-07 | **V2 计划复审回写 + 步骤重排**（复审结论已并入 `plan-v2.md` §附-3），依据 2026-09-07 全量复审与用户拍板（D-J8~D-J11）：① **NFR-03 拆双口径**——原「1 万 chunk 含 embedding <120s」在「不量化（D-J6）+ 不换模型」前提下无已识别路径可达（实测 225.5s，差 1.6×；量化/调批次/并行建图三条路均已排除），改为「首次全量 <240s + 增量追加 < 首次 × 变更比 × 1.2」；② **新增 NFR-13（低选择度过滤延迟）**——10 万级 sel-0.1% 档 vector P99 158ms 此前落在所有 NFR 之外，现纳入口径并对应 `plan-v2` Step 5（T7-22 暴力兜底）；③ **NFR-11 口径修订**——`add` 后需 `commit()` 才可见（`search/index.rs:5-6`），原「增量写入后立即可查」与实现语义不符；④ **FR-31 由 Should 升 Must**——Q-C3 定级「高（正确性）」，且现状（`storage/snapshot.rs:88` 直写无 fsync）崩溃即丢索引；⑤ FR-17/NFR-10/NFR-11/NFR-12 的「对应步骤」按重排编号更新（Step 7→8、Step 4→6、Step 6→7，映射见 `plan-v2.md` §附-2） |
| v1.8 | 2026-09-07 | **V2 Step 3（原子快照）落地校准**（依据 `v2-step3-design.md` v0.3，D-S3-01~07 拍板）：**FR-31 验收注记落地**——「临时文件 + fsync + rename 原子替换」由 `storage/atomic.rs` 通用原语 `atomic_write` 实现（快照与 manifest 共用一份实现，`FORMAT_VERSION` 保持 2），崩溃不变式（save 序列任一注入点崩溃后 load 得旧或新，绝不 `SnapshotCorrupted`）经故障注入测试 S3-T3~T6 证明；fsync 代价实测 +0.027~0.035s（`eval-report.md` §8.7），不触碰任何 NFR |
| v1.9 | 2026-09-08 | **状态校正（无指标变化，本表补记）**：NFR-04 行由「未达秒级，V2.0 解决」的将来时改为「✅ 已达标」（V2 Step 2 实测 ≈100ms，详注保留在 §6.1 下方注记）；NFR-03 补首次全量口径实测落点（P5 236.7s / P6 225.5s，均 < 240s ✅） |
| v1.10 | 2026-09-10 | **① V2 Step 4 落地回写补记**（`#32` / `9e2211e` 已落地但漏记版本行）：**FR-30 墓碑物理回收标记「✅ 已实现」**——`SearchIndex::compact`（纯内存）/ `compact_and_save`（唯一落盘入口，复用 Step 3 `atomic_write`）+ `tombstone_stats` / `CompactionReport` + CLI `helix compact`（`--dry-run` / `--output` / `--json`）；10K 合成语料 churn 0.3×5 实测 graph+data **84.6→33.7MB**（-60%）、compact 后 reload `Loaded`、冷启动 91~99ms（`eval-report.md` §8.8）；⚠️ **ID 会被重编号** ⇒ 跨 compact 的持久引用请用 `source` / `content_hash`（D-S4-09；架构 §7.5.3 / R27）。**② V2 Step 5 设计回写**（依据 `v2-step5-design.md` v0.3，D-S5-01~09 全部拍板）：**NFR-13 补口径**——**端到端 P99（含过滤求值）**、拟 **≤20ms**（与 NFR-02 同量级；数值待 S5-04 标定回填）；**成本口径修正**——D-J9 的「约 0.05ms」只覆盖「对 ~100 个候选算距离」一段，**不含 `O(N)` 遍历定位候选**（1~10ms，方案 A 的主要成本，§4.2.4）；**NFR-07 补口径落地路径**——`query::Metrics` 进 `SearchResponse`（D-S5-05）+ per-lane 耗时（D-S5-06）+ `vector_route`（D-S5-07）+ 修两条早退路径 `took` 恒 0（D-S5-08） |
| v1.11 | 2026-09-10 | **口径精确化：需求侧无变化，本行仅为版本面同步**。`architecture-design.md` §5.4 / §8.3 把「`vector_shortfall` 在精确路径下恒为 0」修正为**条件式**——`allowed` 来自 `Index`、扫描枚举的是**图里的点**（两个独立来源），图滞后于索引时该值仍 > 0；并把该条件升级为信号：`Exact` + 缺口 `> 0` ⟺ **图未覆盖全部 allowed chunk**（`Exact` + 缺口 `== 0` 才说明「这一档没走 ANN」）。**未触碰任何 FR / NFR**（NFR-13 的 P99 口径与预算不变），故本表只作版本面记录 |
| v1.12 | 2026-09-10 | **NFR-13 标定定稿（V2 Step 5 / S5-04，本 Step 唯一触碰 FR/NFR 的改动）**：预算由「拟 **≤20ms** 单值」改为**双口径**——**① 常规档位（字段索引命中）≤ 20ms**（10 万级实测最差 **7.06ms**，余量 2.8× ⇒ **原拟值被证实**）；**② 降级字段档位 ≤ 35ms**（实测最差 **22.84ms**，其中过滤求值自身 8.77ms 属 D-S5-09 已接受、不归本 Step）。依据：10 万级 A/B 标定（同一快照，`--brute-fallback off` vs 强制精确），对照兜底前 **112.69 / 223.24ms**；全表见 `eval-report.md` §8.9。同时把阈值 1024 定稿为 **8192**（两端点实测 + 归一插值交叉点 ≈ 9700）——阈值是**实现常量**、不属需求面，故只在架构 §14.3 R33 与设计文档 §4.3/§4.9 记录 |
| v1.13 | 2026-09-11 | **V2 Step 6 详细设计回写（需求侧无变化，本行仅为版本面同步）**（依据 `v2-step6-design.md` v0.1，D-S6-01~09 待评审）：① **NFR-03** 补 Step 6 的落地拆解——① 口径（首次全量 <240s）**只做"别弄坏"**（T7-09 只承诺 **S 级 spike**，产出"投 / 不投"结论；embed 占构建耗时 **93%** ⇒ 不换模型则上界收益 <7%，故**不承诺**压到 120s），② 口径（增量追加）由 **T7-11（`helix build --index`）** 落地，骨架已在（`content_hashes` 入快照 ⇒ `load` 后仍能按 hash 短路跳过 embed）；⚠️「10% 的单位」未决 ⇒ 设计 §9.2 **Q5**；② **NFR-05** 补「E2 多 session 会抬高峰值 ⇒ 须重测」+ 决策门「增量 ≤ 20%」；③ **NFR-10** 标注 ⚠️ **数值目标缺失 ⇒ 今天无法判定达标**（三方案列设计 §9.2 **Q4**，待评审拍板）；④ **NFR-11** 补写延迟的具体口径（`load → add → commit → 可查`）与 S6-T10 的 NFR-06 交叉覆盖；⑤ **§1.1 版本表补齐 v1.11 / v1.12 两行**——此前它们只落在本附录，属**定义面漂移**（v1.11/v1.12 改的是架构侧口径与 NFR-13 数值，**需求侧均无变化**），本次一并补记为版本面同步。**未修订任何 FR / NFR 指标**：阈值、预算、时限一字未动 |
| v1.14 | 2026-09-11 | **NFR-10 口径修订为方案 A + NFR-03 ② 补单位注记（V2 Step 6 评审收口 / PR #38）**—— 评审对 `v2-step6-design.md` 的 7 条意见（F1~F7）**全部采纳**，并拍板 **Q4~Q7**：① **NFR-10**（Q4）由「只有定性描述、无任何数字」改为**双部分口径**（前置条件 = 各线程 `hits` 逐位等于单线程；吞吐判据 = `--threads 4` QPS ≥ `--threads 1` × **2.5**）+ **写明测量口径**（预热丢弃首轮 / 顺序交错 / 报告运行范围）；**×2.5 标「拟」，待 S6-08 实测定稿**（同 Step 5 的 NFR-13 先例）；② **NFR-03 ②**（Q5）的 10% 单位 = **文档数**，chunk 数只作参考、不进判据；③ **Q6** = 方案 A（`default_embedder` 硬失败，不需额外兼容开关）；④ **Q7** = 沿用既有 `--no-graph-persist`、不新增逃生舱。**未触碰任何其它 FR / NFR** |
| v1.15 | 2026-09-13 | **V2 Step 6 收尾（S6-10）：NFR-10 定稿并收窄适用范围 + NFR-05 补口径限定词** —— ① **NFR-10**：S6-08 实测 `QPS(4)/QPS(1)` = bm25 **2.98×** ✅ / hybrid **1.63×** / vector **1.88×**（`eval-report.md` §8.12 / §8.13）⇒ **×2.5 由「拟」定稿**；**裁定三条**：㈠ **② 的适用范围收窄为「不含查询侧编码的读路径」**、㈡ **NFR-10 整体保持「打开」不关闭**（编码侧并发登记为架构 **R43**，**不是**「只记录不判定」）、㈢ **复审触发条件 = 查询侧会话池落地时**（届时须先答挂起的 **Q3**）；② **NFR-05** 补限定词「**（检索路径，batch 1）**」，**建库路径内存单独立为架构 R41**（预算按 `batch × 序列长度` 给、数值待定）；③ **NFR-03 ②** 增量口径实测 **23.705s < 27.973s** ✅（余量 15.3%）；④ **更正 NFR-10 备注措辞**（评审 P3-8：「读路径零共享可变状态」只对**索引读路径**成立）。⚠️ **不新增任何 FR**；架构侧随之 **v1.14** |
| v1.16 | 2026-09-14 | **V2 Step 7（精排）详细设计回写 + NFR-12 口径成形**（依据 `v2-step7-design.md` v0.1，D-S7-01~10 待评审）。**本 Step 唯一触碰 FR/NFR 的改动 = NFR-12（补口径）+ NFR-05（补限定词）+ FR-18（补锚点/更正来源）**：① **NFR-12 三部分化**——原口径只有「rerank P99（独立口径，不并入 NFR-02）」**一行、无阈值、无测量口径、无质量判据 ⇒ 无法判定达标**；本版补：**延迟判据（双列）**（**端到端 `took` P99** + **精排段 `rerank_elapsed` P99 ≤ 500ms（拟）**，采集点 = `helix bench --rerank-window <R>` 的 **`took` 与 `rerank_elapsed` 两张直方图**，端到端与精排段**分列**）、**质量判据**（hybrid **MRR@10(1)** 相对同图基线**可测提升**）、**可观测**（`Metrics.rerank_window` / `rerank_elapsed` + `Explain.rerank_score.is_some()`，**不得静默**）。**「500ms」为「拟」值**（对应 `R = 10` 档上界、**与默认 `R` 联动**，见设计 §4.2.2 补记），由 **S7-04** 标定回填定稿（同 Step 5 的 NFR-13 / Step 6 的 NFR-10 先例）。② ⚠️ **NFR-12 的 A/B 必须在「同一张冻结图」上做**——**前置 ① 复现结论**：P5 锚点 `hybrid MRR@10(1) = 0.6922` **自身带 ~0.6% 图漂移**（2026-09-14 复现 **0.68815**，**bm25 三项逐位复现 Δ=0** 作对照 ⇒ 唯一变量是未播种的 HNSW 图）⇒ 不读 `--index` 快照则 A/B **不可归因**（设计 §3.1 列为**第一条设计约束**；S7-T11 为自证测试）。③ **NFR-05 补口径限定词「不含精排器」**——`rozgo/bge-reranker-v2-m3` 的 `model.onnx.data` 实测 **≈2,187MB（FP32）**、加载后常驻；精排内存**单独立为架构 R44**（数值待 S7-04 回填），**不并入** 372MB 基线。④ **NFR-06 补精排确定性注记**（score 单调变换、`chunk_id` 序不变；⚠️ `BatchLongest` ⇒ **跨 batch 组成**分数不可逐位复现，判据仍是「同一快照两次加载逐位一致」）。⑤ **NFR-07 补精排可观测字段**（`Metrics.rerank_window` / `rerank_elapsed`）。⑥ **FR-18 补设计锚点 + 模型来源更正**（**`rozgo/bge-reranker-v2-m3`，非 BAAI 官方**）。**H3 的取形 = 可配 `R`、默认 20（拟）**（D-S7-01，数值由 S7-04 标定）。⚠️ **不新增任何 FR**；架构侧随之升 **v1.15**（§14.5 新增 **R44~R48**） |
| v1.17 | 2026-09-14 | **V2 Step 7 设计评审收口：需求侧只有两处事实性更正**（设计升 **v0.2**；评审 **4 项拍板全部同意**、**F1~F6 全部采纳**）—— ① **NFR-12 的标定语料更正**：「（10 万级）」→ **T2Ranking 12K / 320 query**（设计 §4.6.1 / 附录 B 的协议；「10 万级」是 **NFR-13 / S5-04 先例**的措辞残留，评审 **F3**）；② **NFR-12 的「≤ 500ms（拟）」补出处与联动** —— 该拟值 = **`R = 10` 档的量级上界**（设计 §4.2.2 粗估 ≈0.5s），而 **`R = 20` 粗估 ≈1s 会超它** ⇒ **阈值与默认 `R` 必须联动、由 S7-04 一并回填**，**禁止把两个「拟」各自独立引用**（评审 **F4**）；③ 头部 / §1.1 的 NFR-12 摘要由「单列」同步为「**双列**」口径（原先只有头部摘要停在单列）。⚠️ **无预算数值 / 结构 / 编号变更**；架构侧随之升 **v1.16**（纯版本面同步） |
| v1.18 | 2026-09-16 | **V2 Step 7 收尾（S7-05，设计 `v2-step7-design.md` v0.3）**：① **NFR-12 定稿** —— 原「≤ 500ms（拟）」**废弃**（实测 `R=10` 档 P99 = **5220 ms**、差 **10.4×**）⇒ **双口径**（**CPU/FP32 上界**：默认 `R=20` 下 P50 ≤ 8.8 s / P99 ≤ 10.9 s；**GPU/量化待测**，标「拟」），并写明**适用范围 = 仅显式开启精排时**、**与 `R` 联动**（`R` 定稿 **20**）；② **NFR-05** 回填精排内存实测（**增量 2.64 GiB / 7.71×**）；③ **NFR-06** 更正精排 tie-break 机制句（→ **`chunk_id` 升序**）+ 补 S7-T9 读数；④ **FR-18** 验收注记同步。⚠️ 无预算数值增减、不增删 FR/NFR；架构侧随之升 **v1.17** |
| v1.19 | 2026-09-16 | **V2.1 计划修订（需求面）** —— 依据 `plan-v2.md` **§附-4** 的 15 条审视结论，补齐 V2.1 的**编号空缺与口径缺口**：**新增 FR-34**（prefilter / 排序键索引，高基数数值字段；承接架构 R11 / R13，是否投由 T7-27 的 spike 判定）、**FR-35**（融合策略自适应，原 Q-R1）、**NFR-14**（读写并发下的读延迟与可见性 —— Step 8 此前只有 FR-17 §5.3.5 的定性验收）、**NFR-15**（自适应融合的质量判据 —— Step 9 此前无任何编号）；**四条全为「草案、待评审」，数值一律留白**（待 `v2-step8/9-design.md` 定稿）。另修：**FR-18 状态**由「设计已出、待评审」同步为「已实现并收尾」（§5.2 摘要行漂移）、**FR-17** 补 Step 8 任务号（T7-25 / T7-06 / T7-26）与 NFR-14。⚠️ **不修订任何既有指标数值**；架构侧随之升 **v1.18** |
| v1.20 | 2026-09-16 | **V2 Step 8（读写并发）详细设计回写**（依据 `v2-step8-design.md` **v0.1**，D-S8-01 ~ 12 待评审、**4 条需拍板**）—— **本 Step 触碰需求面三处，全部是「补口径 / 改措辞」，不含任何预算数值增减**：① **NFR-14 由草案定口径** —— 可见性 = **`commit()` 后立即可查**；读延迟 = **②a 相对（合并期 ÷ 静止期 `took` P99 ≤ 1.2，拟）** + **②b 绝对护栏（仍在 NFR-02 预算内）**；并补「库内不得存在『重建索引中』类错误码」的**可证伪断言**；② **NFR-11 补口径与拟值** —— 口径由「从累计 embed 耗时**推算**」改为 **`commit()` 端到端直接计时**，**拟值 `P50 ≤ 1.0s / P99 ≤ 2.0s`**（`batch_size = 64`、CPU/FP32、不含首次模型下载与加载），**待 S8-08 标定后定稿**；③ **FR-17 §5.3.5 措辞改写** —— 「写入后立即可查」→「**`commit()` 后立即可查**」（**只改措辞、不改语义**，保留原措辞引文；「读能看到未 commit 的写」**明确不做**）；④ **NFR-10 ③** 补设计锚点 —— 复发触发条件 = **T7-26**（查询侧会话池）落地时，挂起的 **Q3 已改述为 Q3′**（不同 `intra_threads` / 实例数是否改变 `embed_query` 的向量**数值** —— ⚠️ fastembed **无 `sessions` 参数**，原措辞在实现面上不存在）。⚠️ **NFR-02 ~ 13 的阈值 / 预算 / 时限一字未动**、**不新增或删除 FR / NFR**；架构侧随之 **v1.19**（**§14.6 新增 R49 ~ R54**） |
