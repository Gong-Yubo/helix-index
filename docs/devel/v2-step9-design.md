# HelixIndex V2 · Step 9 详细设计（相关性深化：自适应融合 + 评测资产纪律）

| 项 | 内容 |
| --- | --- |
| 版本 | **v0.10（2026-09-26，PR9-3：T7-16 / T7-15 实现期定形 ⇒ 见 §4.16）** —— v0.1（首版）→ v0.2 → … → v0.7（§4.13：spike 读数 + 判「不投」）→ v0.8（§4.14）→ v0.9（§4.15）→ **v0.10**（§4.16：R58 拍板记录 / Q9-4 定形 / 生成器载体与两处偏差 / T7-15 读数与解读边界）；**逐条处置见 §4.8 ~ §4.12 / §4.14 / §4.15**，**spike 读数见 §4.13**，**T7 资产见 §4.16** |
| 日期 | 2026-09-23（v0.1~v0.3）/ 2026-09-24（v0.4）/ 2026-09-25（v0.5~v0.9）/ 2026-09-26（v0.10） |
| 状态 | **设计已过三轮评审**（第 1 轮「通过 + 3×P4」、第 2 轮「**有条件通过 + 1×P3 + 6×P4**」、第 3 轮「**通过 + 3×P4**」，逐条处置见 §4.8 / §4.9）。**PR9-1 已实现并合并**（PR #72 ⇒ squash `06e8432`；实现期定形见 §4.10，其两轮评审响应见 §4.11 / §4.12）。**PR9-2 已跑完 spike S9-S1（结论 = 判「不投」）**（PR #73；读数与结论见 **§4.13**，其第 1 轮评审响应见 **§4.14**、**第 2 轮 ✅ 复审通过（0×新意见）见 §4.15**）。**PR9-3（S9-06：T7-16 / T7-15）已实现**（实现期定形见 **§4.16**；⚠️ T7-16 的**真实资产待 LLM 端点就绪后产出** —— 见 4.16 ①）。v0.1~v0.3 为**纯设计**（`.rs` 零改动）；**v0.4 起含实现期定形记录**（`.rs` 改动由 PR #72 承载，本文仍不直接改代码；**PR9-2 零 `.rs` 改动**）。 |
| 上游 | `plan-v2.md` **v0.25** §4 Step 9 / §4.0 **H7** / §5（**FR-35** / **NFR-15** 草案）/ §6「V2.1 门槛」/ §附-4（P0-4、P0-5、P1-10）；`requirements-spec.md` **v1.24**（§附-1：**FR-35** / **NFR-15**）；`architecture-design.md` **v1.24**（**§5.6** `FusionStrategy` / **§5.7** `Reranker` 的 provided 方法先例 / **§14.7** = 本步新增风险 R55~R58）；`eval-report.md` **§3.3**（分桶读数）/ **§3.4**（图抖动）/ **§8.15**；issue **#4**（本步入口）/ **#71**（本步跟踪，2026-09-24 建） |
| 锚点体例 | ⚠️ 跨文档引用一律 **「§ 锚点 + 文档版本 + 行号」双钉**（第 1 轮评审 **P4-3**）：行号**只作辅助**、且必须标注它是**哪个文档版本的时点**（本文自己会挪行 ⇒ 纯行号锚点合并即漂移）。🔴 **两条硬纪律**（第 3 轮评审 **P4-7** 的教训）：① **版本标签与行号必须同一时点**（本轮曾标 `v1.24` 却给了 `v1.23` 时点的行号 ⇒ 标签本身是假的）；② **行号必须在「本 PR 全部编辑完成后的最终 head」上量** —— 若本 PR 自身会在锚点**之上**插入内容（如给架构 §5.6 加 25 行 + 1 版本行），中途量到的行号必然漂（本轮实测 **+26**） |
| 范围 | ① **T7-14** 自适应融合（**FR-35**；判据 **NFR-15**）；② **T7-15** 假负例敏感度对照（**只作对照**）；③ **T7-16** Agent query 评测集（**外部 LLM API** 生成 + 自查；**只作对照、不替换主基线** —— **H7** 已拍板）；④ 本步的**评测资产纪律**与**判据口径**定值 |
| 非范围 | **不做**权重网格搜索（**D-S9-08**）；**不改** `FusionStrategy::fuse` 的签名（**D-S9-01**）；**不把「桶标签」引入生产路径**（**D-S9-02**）；**不改** Step 7 的窗口机制（`candidate_window` / `R`，**R45** 原样）；**不做** Step 10 的后处理（MMR / token budget / 多 query 融合）；**不做** 精排器的改动（`Reranker` 一字不动）；**不替换**主基线（**H7**） |
| 交付物 | ① 本文（设计）；② 自适应融合实现（`FusionStrategy` 的 **provided 方法** + `Config::adaptive_fusion` 默认 **false**）；③ 评测载体（脚本侧**跨运行配对** + 早停/冻结图纪律）；④ 两个评测资产与生成脚本（T7-16 / T7-15）；⑤ 四处定义面回写 + `eval-report.md` **§8.16** 读数；⑥ 风险 **R55 ~ R58**（→ 架构 **§14.7**） |

---

## 决策速览（D-S9-01 ~ D-S9-10）

| 编号 | 决策点 | 建议 | 定值来源 |
| --- | --- | --- | --- |
| **D-S9-01** | 自适应融合的**落点形态** | **`FusionStrategy` 新增 trait provided 方法**（默认实现 = 今天的行为）⇒ **既有实现零改动、默认逐位一致**；**不改** `fuse` 签名 | **§2.4 N3** + `Reranker::candidate_window` 先例（`rerank/mod.rs:56-85`） |
| **D-S9-02** | 自适应的**信号**从哪来 | **只允许**「检索自身可观测的量」；**禁止**读 qrels / 桶标签 | **§2.4 N1**（桶标签含标注 ⇒ 泄漏 + 生产不可用） |
| **D-S9-03** | 启用面 | `Config::adaptive_fusion`（**默认 `false`**）+ `SearchIndexBuilder::adaptive_fusion(bool)` ⇒ **零行为变化** | 照 `embed_sessions` 先例（`S8-08`） |
| **D-S9-04** | 判据口径 | 主指标**分桶 `MRR@10(1)`**（只看名次 ⇒ 对 **R46** 的 `Hit.score` 语义免疫）+ `NDCG@10` 为辅；**基线 = 固定权重 `(1.0, 1.5)`**；**同一冻结图**；**`candidate_k` 两侧相同**；**精排默认关** | `plan-v2.md` §6 验收门槛的 **Step 9** 行（**v0.26 时点 `:648`**）+ `eval-report.md` §3.3 / §3.4 |
| **D-S9-05** | **统计口径** | **预先声明「按幅度读、不声称显著」**；报每桶 `n` + 同向幅度；**不写「显著」二字** | **已由用户 2026-09-23 拍板**（Step 7 教训：配对符号检验最小 p = 0.1553） |
| **D-S9-06** | **H7：评测资产能否替换主基线** | **只作对照、不替换**（主基线 = T2Ranking 12K / 320 query） | **已由用户 2026-09-23 拍板** |
| **D-S9-07** | T7-16 的 **LLM 通道** | **外部 LLM API**，形态 **provider-agnostic**：`HELIX_LLM_BASE_URL` / `HELIX_LLM_API_KEY` / `HELIX_LLM_MODEL` 三环境变量 + OpenAI 兼容 `POST /chat/completions`；**密钥不入库**；⚠️ **语料外发须用户确认**（见 **R58**） | **已由用户 2026-09-23 拍板** + §2.3（仓内零 LLM 先例） |
| **D-S9-08** | 防过拟合 | **不做权重网格搜索**；自适应规则取「**零参数**」或「**单阈值**」形态；标定只做**阈值灵敏度曲线**，不取最优点 | §2.4 **N5**（80 条/桶的样本量约束） |
| **D-S9-09** | 判据的**评测载体** | **脚本侧跨运行配对**（两次 `helix bench` + 按 `qid` 配对 `per_query` JSON）；**不改** `bench` 的 `--modes` | §2.3（`--modes` 表达不了「hybrid@A vs hybrid@B」） |
| **D-S9-10** | 「先 spike、再投」 | **T7-14 不预先承诺收益**：spike **S9-S1** 出数据再判「投 / 不投」；判「不投」则只交付**工具 + 结论**（同 Step 6 的 E1/E2/E3、Step 8 的 S8-S2 先例） | `plan-v2` 反模式纪律 + **D-S9-08** |

---

## 1. 目标与验收

### 1.1 要解决的问题

| 问题 | 证据（位置） | 严重度 |
| --- | --- | --- |
| **Q-R1** paraphrase 桶 hybrid 被**弱路 BM25 稀释** | `eval-report.md` §3.3（标题行 `:48`，该文件**当前版本**）：paraphrase 桶（n=80）NDCG@10 —— bm25 **0.2306** / vector **0.4294** / hybrid **0.3799**；hybrid vs vector 的逐 query 胜负 = **25 / 11 / 44（p = 1.0）** ⇒ hybrid 在该桶被拖到 vector 之下 | 高（MRR 口径；来源 `plan-v2.md` §3 需求来源表的 **Q-R1** 行，**v0.26 时点 `:87`**） |
| **Q-R3** 假负例敏感度对照未做 | `eval-report.md` §10「残留 P6」第 2 条；`plan-v2.md` §3 **Q-R3** 行（**v0.26 时点 `:89`**） | 低 |
| **Q-R4** 评测 query 来自人类日志，缺 Agent query | `eval-report.md` §9「已知偏差」第 3 条；`plan-v2.md` §3 **Q-R4** 行（**v0.26 时点 `:90`**） | 中 |

### 1.2 对应需求

| 需求 | 本 Step 的落点 |
| --- | --- |
| **FR-35** 融合策略自适应（`requirements-spec.md` §附-1 FR 表 **FR-35** 行，**v1.24 时点 `:309`**；**草案**） | §4.1 ~ §4.3：**按 query 可观测特征**动态决定两路权重；⚠️ 本文更正了该条的措辞「按桶（paraphrase / exact / 长尾）自适应」—— **见 §2.4 N1 与 §6.3**：生产侧**不得**「按桶」（桶含标注） |
| **NFR-15** 自适应融合的质量判据（`requirements-spec.md` §附-1 NFR 表 **NFR-15** 行，**v1.24 时点 `:400`**；**草案**） | §4.5 + §1.3：**指标 / 阈值 / 统计口径**三条在本节定值（D-S9-04 / D-S9-05） |
| **H7**（`plan-v2.md` §4.0「开工前必须澄清的设计决策」表，**v0.26 时点 `:117`**） | **D-S9-06**：只作对照、不替换 ⇒ P5 锚点 / Step 7 的 `R = 20` 标定 / NFR-12 保持可比 |
| **R46**（`Hit.score` 语义随开关而变） | 判据**只看名次**（MRR@10 / NDCG@10 都是 rank 的函数）⇒ 结构性免疫；⚠️ 但**必须钉死精排开关**（精排会改名次） |

### 1.3 验收标准（**可证伪**，编号化）

> 全部口径见 §4.5：**同一冻结图**（`--index` 读快照）+ **`--runs 1`** + **精排默认关** + **`candidate_k` 两侧相同** + **`k = 10`** + **主基线不替换**。判据只报**幅度与同向**，**不声称显著**（D-S9-05）。

| 编号 | 判据 | 通过条件 | 备注 |
| --- | --- | --- | --- |
| **S9-1a**（主） | paraphrase 桶：自适应 hybrid 的 `MRR@10(1)` **优于固定权重 hybrid** | `ΔMRR ≥ +0.01` | 对应 **NFR-15** 的字面口径（「在分桶上优于固定权重」） |
| **S9-1b**（辅） | paraphrase 桶：自适应 hybrid **追平 vector 单路** | `MRR_adaptive ≥ MRR_vector − 0.0024` | 来自 issue **#4** 的验收建议；⚠️ **上界结构性可达** —— 若自适应把 `w_bm25` 压到 `0`，该桶 hybrid **≡ vector 单路**（`rrf.rs:61-67`），故本条**不是碰运气**。⚠️ `0.0024` 是**政策容差**（口径见 S9-1c，与 S9-1c / G2 **同一个数** —— 第 2 轮评审 **P4-5**：原用 `0.002` 无出处，已统一） |
| **S9-1c**（防回归） | exact / natural / mixed 三桶：自适应 hybrid **不得回退** | `ΔMRR ≥ −0.0024` | 防「为修一桶而伤三桶」。⚠️ **口径 = 政策容差（交易余量），不是噪声界**（第 2 轮评审 **P4-5**）：§4.5 协议把两臂钉在**同一冻结图 + `--runs 1`**、且检索是确定性的 ⇒ 臂间 Δ 是**精确值、不含图漂移成分** ⇒ 这个数回答的是「**为修 paraphrase 愿意付多大的别桶代价**」—— 这是**取舍声明**，不是测量误差。**量级取自实测**（`eval-report.md` §3.4：图漂移极差 ≤ 0.0024）只为 ① 不凭空造数、② 为将来**跨图复跑 / 跨报告对齐**留余量 |
| **S9-1d**（全局） | 全量 320 query：自适应 hybrid **不低于**固定权重 hybrid | `ΔMRR ≥ 0` | 「全局结论保持：hybrid MRR/Recall 仍全场最高」（issue #4） |
| **S9-2**（逐位一致） | **关闭**该开关时与今天**逐位一致** | 同快照、同 query、同参数的 `hits` **逐位相同**（`chunk_id` 序 + `score`） | 同 `S8-08` 的「默认 1 ⇒ 零行为变化」纪律 |
| **S9-3**（防泄漏，**源码级**） | 自适应**不读**相关性数据 | `FusionCtx` 的字段**逐个**可从「召回结果 / query / 索引统计」构造出来；**编译期**断言该类型不含 `relevance` / `qrels` / `bucket` 语义字段；依赖注入面（`searcher` 的入参）不含相关性数据 | **本 Step 最重要的一条**（§2.4 N1） |
| **S9-4**（可观测） | `Metrics` 暴露「本次用了哪组权重 / 走了哪个分支」 | 新增字段在 `SearchResponse.metrics` 可断言（照 `Metrics.rerank_window` 先例，`metrics.rs:119`） | 见 §6.2 的破坏性说明 |
| **S9-5**（T7-16） | Agent query 集**产出并在库内冻结** | 资产 + 生成脚本 + prompt/模型记录**同时入库**；**只作对照**读数落 `eval-report.md` §8.16 | **只作对照**（D-S9-06） |
| **S9-6**（T7-15） | 假负例敏感度对照**产出** | 「仅 qrels 语料」重建臂 vs 主基线臂的 Δ 读数落 §8.16；**只作对照** | 结论**不外推**（`eval-report.md` §9） |

⚠️ **判「不投」是合法结论**（D-S9-10）：若 **S9-S1** 的阈值灵敏度曲线表明「不存在一个阈值使 S9-1a 成立且 S9-1c 不破」，则 **T7-14 降级为「交付工具 + 如实记录未达标」**，`NFR-15` 保持「打开」并写明复审触发条件（照 `S8-09` 对 `NFR-14` 的处置口径 —— **三条并记**：口径定稿 + 另立未决 + 复审触发条件）。

---

## 2. 现状与问题定位（源码级）

### 2.1 融合的接口面

| 事实 | 位置 |
| --- | --- |
| trait 只有两个方法，**签名里没有查询上下文**：`fn name()`、`fn fuse(&self, lanes: &[LaneResults], k: usize) -> Vec<(ChunkId, Score)>` | `fusion/mod.rs:21-28` |
| `LaneResults = Vec<(ChunkId, Score)>`（**不带 lane 名**） | `fusion/mod.rs:18` |
| `RrfFusion { k: f32, weights: Vec<f32> }`；默认 `k = 60`、`weights = [1.0, 1.5]`（P5 定稿） | `fusion/rrf.rs:19-22`、`:46-50` |
| **权重是构造期注入的**，`fuse` 内按 `lane_idx` 取：`self.weights.get(lane_idx)` | `fusion/rrf.rs:61-62` |
| 贡献式 `w / (k + rank + 1)`；排序 = `fused_score` 降序、**同分按 `chunk_id` 升序**（确定性 NFR-06）；末尾 `truncate(k)` | `fusion/rrf.rs:65`、`:72-77` |
| 融合**不回捞正文**（硬约束：一旦回捞，换策略就不能独立测） | `fusion/mod.rs:3-7` |
| 编排层的融合调用点：**两路召回之后**、回捞之前 —— `parts.fusion.fuse(&lanes, candidate_k)` | `query/searcher.rs:374` |
| `lanes` 的构造：**`bm25_lane` 与 `vector_lane` 各自 `Some` 就 push**（**空 lane 也会被 push** —— 见 §4.4 的 I9-2） | `query/searcher.rs:362-367` |
| `candidate_k = max(3k, window, 10)`；**窗口必须在召回之前算**（顺序是硬要求） | `query/searcher.rs:171-184` |
| 一个 `QueryExecutor` 持 **一个** `Box<dyn FusionStrategy>`（`bench` 每 mode × 每 run 装配一次） | `query/searcher.rs:725-741`、`:767-770` |
| `to_lane` **不过滤**分数（`Scored` → `(chunk_id, score)` 原样） | `query/searcher.rs:863-865` |

### 2.2 **分桶的定义**与判据载体

| 事实 | 位置 |
| --- | --- |
| 分桶常量：`OSTAR_THRESHOLD = 0.5` / `NATURAL_MIN_CHARS = 16` / `PER_BUCKET = 80` / `SEED_BUCKET` | `examples/t2_prep.rs:57` / `:58` / `:53` / `:48` |
| 分桶规则：含 ASCII 字母数字 ⇒ `mixed`；否则 `chars ≥ 16` ⇒ `natural`；**否则**算 `o*`：**遍历 `qrels[qid]` 里 `grade ≥ 1` 的段落**，取 `max(\|q_terms ∩ p_terms\| / \|q_terms\|)`，`o* ≥ 0.5` ⇒ `exact`，否则 `paraphrase` | `examples/t2_prep.rs:193-231` |
| 🔴 **`exact`/`paraphrase` 的划分用到了 qrels**（`grade ≥ 1` 的段落）⇒ **桶标签含标注信息** | 同上（`t2_prep.rs:206-230`） |
| 标签**物化在评测资产里**：`data/t2-queries.jsonl` 字段 = `qid` / `query` / `relevance` / **`type`（桶标签）**；共 **320 条**（四类各 80） | `data/t2-queries.jsonl` |
| 判据的执行载体 = `helix bench` 的「分桶（NDCG@10）」表（按 `j.qtype` 聚合 `agg_type`）+ JSON 的 `buckets` / `per_query`（逐条带 `qid` / `type` / 三个指标） | `cli/src/bench.rs:1218-1240`、`:2156-2200` |
| 「预期占优」是**声明式的假设表**（`exact → bm25`、`paraphrase → vector`），**已被 P5 实测推翻一多半** | `cli/src/bench.rs:61-67` + `eval-report.md` §3.3 |
| 逐桶符号检验（**hybrid vs 单路**、per-query NDCG delta、二项精确单侧） | `cli/src/bench.rs:525-532`、`:1246-1260` |

### 2.3 评测工具的能力与**缺口**

| 能 | 位置 |
| --- | --- |
| **固定权重**的对照臂**今天就能跑**：`--rrf-k` / `--rrf-weights`（后者约束「两个非负数」） | `cli/src/bench.rs:111`、`:116`、`:313-325` |
| **冻结图**：`--index` 读快照（与 `--input` 二选一） | `cli/src/bench.rs:73`、`:800` |
| 逐 query 结果落 JSON（可做**脚本侧配对**） | `cli/src/bench.rs:2166-2180` |

| 不能（**本步要绕开或补齐**） | 说明 |
| --- | --- |
| `--modes` 只认 mode 名（`bm25` / `vector` / `hybrid`）⇒ **表达不了「hybrid@权重 A vs hybrid@权重 B」的同轮对照** | ⇒ **D-S9-09**：两次运行 + 脚本按 `qid` 配对；⚠️ 两次运行**必须同一冻结图**（否则带上 HNSW 的 `OsRng` 图漂移） |
| 既有符号检验是 **hybrid vs 单路**，**不是**「两个 hybrid 配置之间」的逐桶配对 | ⇒ 配对与逐桶聚合在**脚本侧**做（本步**不改** `bench`） |
| 仓内**零 LLM 通道先例**：`grep -rnE "openai\|api_key\|OPENAI\|deepseek\|qwen\|LLM_\|llm_"` 在 `*.rs` / `*.sh` / `*.toml` / `*.py` 上 **0 命中** | ⇒ **D-S9-07**（外部 API + 环境变量 + provider-agnostic） |

### 2.4 🔴 设计期新发现

> 本节是本文价值的主体：**用源码推翻计划里的想当然**。

**N1 🔴 「桶」不是纯 query 特征 —— 它含标注信息（`t2_prep.rs:206-230`）**

`exact` / `paraphrase` 的划分 = 「query 词项在**标注为相关（`grade ≥ 1`）的段落**里的最大覆盖率是否 ≥ 0.5」。
⇒ 两个后果：

1. **生产不可用**：线上没有 qrels ⇒ 这个「桶」在生产环境**算不出来**；
2. 🔴 **标签泄漏**：若实现按桶标签调权，等价于**把答案喂进排序**（「这条 query 的答案段落不怎么含 query 词 ⇒ 所以压 BM25」）⇒ 在评测集上**必然**「优于固定权重」，但结论**既不可信也不可投产**。
   ⇒ 这正好命中 **NFR-15 的字面口径**（「在**分桶**上优于固定权重」）⇒ **必须在设计期就把两件事分开**：
   **桶 = 评测分组（可含标注）**；**自适应信号 = 检索自身可观测的量（禁含标注）**。⇒ **D-S9-02**，判据 **S9-3**。
   ⚠️ 顺带：**FR-35 的原文措辞「融合权重按桶（paraphrase / exact / 长尾）自适应」需更正**（见 §6.3 与 §5 的 D-S9-02）。

**N2 🔴 架构 §5.6 的 `FusionStrategy` 形态与实现漂移**（`architecture-design.md` **§5.6**；旧伪码位于 **v1.22 时点 `:492-501`**）

架构写的是：

```rust
fn fuse(&self, lanes: &[(&'static str, Vec<ScoredHit>)], weights: &[f32]) -> Vec<ScoredHit>;
```

实际是：`fn fuse(&self, lanes: &[LaneResults], k: usize) -> Vec<(ChunkId, Score)>`（`fusion/mod.rs:28`）。三处不一致：
① 形参里**没有** `weights`（权重在**构造期**注入，`rrf.rs:27-30`）；② 类型名 `ScoredHit` **不存在**（实际 `LaneResults` / `(ChunkId, Score)`）；③ 实际有 `k` 形参、架构里没有。
⇒ **回写架构时按实现更正**（本文 §6.4 已列）。

**N3 🔴 `fuse` 没有查询上下文 ⇒ FR-35 在「不动接口」的前提下**结构性做不到****

自适应 = 「同一个策略实例、不同 query 用不同权重」。而 `fuse(&self, lanes, k)` 的入参只有候选池，**拿不到任何 query 特征**；`searcher.rs:374` 也**只传这两个**。
⇒ 三条路：

| 路 | 形态 | 代价 |
| --- | --- | --- |
| A | 在 `searcher` 内**每次查询新建** `RrfFusion::new(k, 算出的 weights)` | 可行但**丢了可解释性**（策略退化成一次性参数）、且**换策略就得改编排层** |
| B | **改** `fuse` 签名（加 `ctx`） | **破坏性**：下游自定义 `FusionStrategy` 实现全部要改 |
| **C** ✅ | trait **provided 方法**（默认实现 = 今天的行为，返回 `None` ⇒ 用原权重） | **非破坏**：既有实现**一行不改**、默认**逐位一致** |

⇒ **取 C**（**D-S9-01**），依据是**同仓已有先例**：`Reranker::candidate_window`（`rerank/mod.rs:56-85`）—— 它同样是「给 trait 加一个默认零行为的方法」，并在 rustdoc 里明写「① 既有实现一行不改；② 未装精排器时与之前**逐位一致**」。

**N4 ⚠️ 判据载体表达不了「A/B 两个 hybrid 配置」（见 §2.3）** ⇒ **D-S9-09**（脚本侧配对），并把「必须同一冻结图」写成硬纪律。

**N5 ⚠️ 80 条/桶的样本量 ⇒「搜出来的最优权重」不可信（量化理由）**

paraphrase 桶只有 **80** 条，而该桶的**路间差距**（vector − bm25）= 0.4294 − 0.2306 = **0.1988**、**图漂移极差** = 0.0024（§3.4）⇒ 噪声/信号 ≈ **1.2%**，单看「能不能区分方向」是够的；但**在一个 80 点的集合上搜权重**会出现「最优值随样本抖动」：任何**多参数**或**多阈值**的搜索都能把噪声拟合进去，且**无法用同一样本自证**（Step 7 已在 320 条上吃到「配对符号检验不显著、p 最小 0.1553」的教训）。
⇒ **D-S9-08**：自适应规则**只允许**「零参数」或「单阈值」形态；标定只出**灵敏度曲线**（扫描阈值看判据是否在**一段区间**内成立），**不取最优点**。

**N6 ⚠️ `plan-v2` 的「T7-16 是 T7-14 的**前置**」（§附-4 P1-10）在源码事实上**不成立****

§附-4 的推理是「『在分桶上优于固定权重』需要**分桶依据**，而现 qrels 来自人类日志（Q-R4）」。**但**：
`data/t2-queries.jsonl` 的 **`type` 字段就是桶标签**（四类各 80、已物化）、`bench` 已按它出分桶表（`bench.rs:1223`）⇒ **T7-14 的判据今天就能算**，**不需要**等 T7-16。
⇒ 依赖关系应更正为：**T7-14 与 T7-15 / T7-16 独立并行**；T7-16 的定位是**外部效度增量**（「人类 query 作为 Agent query 的代理」这一已知偏差，`eval-report.md` §9），**不是** T7-14 的硬前置。
⚠️ 且 **T7-16 的产物若没有 qrels，就既算不出桶、也评不了分** ⇒ 它必须**自带标注**（弱标签，见 R57）⇒ 更不适合当 `NFR-15` 的主判据（**D-S9-04** 把主判据钉在**既有 320 条**上）。

---

## 3. 设计约束（既有事实，本文不重新论证）

1. **RRF 只用名次、免疫量纲**（`rrf.rs:1-4`）⇒ 自适应**只需要改权重**，不需要碰分数归一化。
2. **融合不回捞正文**（`fusion/mod.rs:3-7`）⇒ 自适应信号**不能依赖正文**（只能依赖 `lanes` + query + 索引统计）。
3. **确定性**（NFR-06）：结果排序必须确定（同分按 `chunk_id` 升序）；自适应**不得引入随机**。
4. **逐位一致前置**（NFR-10 ①）：关闭开关时与今天**逐位一致**。
5. **评测资产纪律**（`plan-v2.md` §4 Step 9 的「评测资产纪律（新增，必读）」条，**v0.26 时点 `:554`**）：新造资产**只作对照、不替换主基线**（**H7** / **D-S9-06**）。
6. **结论不外推**（`eval-report.md` §9）：仅对「T2Ranking 中文网络语料 + bge-small-zh-v1.5 + 12K 段落」成立。
7. **A/B 必须同一冻结图**（`plan-v2` V2.1 开工纪律）：`--index` 读快照 + `--runs 1`。
8. **延迟类读数不具跨机可引用性**，报数必标运行范围（`architecture-design.md` §14.6 末的「读数落点 + 报数纪律」段，**v1.24 时点 `:1713`**）。

---

## 4. 详细设计

### 4.1 形态（D-S9-01）

```rust
/// 融合上下文：**只含检索自身可观测的量**（I9-1）。
///
/// ⚠️ **字段白名单**（第 1 轮评审 **P4-1**）：新增任何字段**必须**同步进 `S9-T3`
/// 的白名单断言 —— 该判据的形状是「字段集合 **==** 白名单」⇒ 加了字段却不改单测
/// ⇒ **单测当场红** ⇒ 「PR9-1 临时加字段」不可能静默违约。
pub struct FusionCtx<'a> {
    /// 各路候选的**统计摘要**（不含正文、不含相关性数据）
    pub lanes: &'a [LaneStats],
    /// query 侧可观测特征（词项数 / 是否含 ASCII 词 / 字符数）
    pub query_terms: usize,
}

/// 单路候选的统计摘要。
///
/// ⚠️ **空态定义**（第 1 轮评审 **P4-1**）：`lane.is_empty()` ⇒ `len == 0`、
/// `top_score == None` / `last_score == None` —— **不用 `0.0` 作哨兵**：
/// BM25 无上界、`0.0` 是**合法分值** ⇒ 哨兵会让「空 lane」与「非空但全零分 lane」
/// 不可区分（后者正是 **Q9-2** 要核的场景）。
pub struct LaneStats {
    pub len: usize,
    pub top_score: Option<f32>,
    pub last_score: Option<f32>,
}

pub trait FusionStrategy: Send + Sync {
    fn name(&self) -> &'static str;
    fn fuse(&self, lanes: &[LaneResults], k: usize) -> Vec<(ChunkId, Score)>;

    /// 【Step 9 / FR-35 新增，**provided**】按 query 侧信号**覆盖本次的权重**（**决策**）。
    ///
    /// 默认实现返回 `None` ⇒ 沿用策略自身的权重 ⇒ **既有实现一行不改**、**逐位一致**。
    /// ⚠️ 返回 `Some(w)` 时，`w` 必须与 `lanes` **一一对应**（含**空 lane** 的槽位，见 I9-2）。
    ///
    /// 🔴 **它只是「决策」钩子，`w` 的应用走 [`Self::fuse_adaptive`]**：编排层只持
    /// `&dyn FusionStrategy`、而 `fuse` 读的是策略**私有**字段（`rrf.rs:62`）
    /// ⇒ 编排层**没有任何合法通道**把 `w` 送进融合计算（排除法见 `fuse_adaptive` 的文档）。
    /// ⇒ 可观测上报（`Metrics::fusion_weights`）与等价性判据（`S9-T6`）挂**这个**方法。
    fn weights_override(&self, _ctx: &FusionCtx<'_>) -> Option<Vec<f32>> {
        None
    }

    /// 【Step 9 / FR-35 新增，**provided**】`weights_override` 的**应用通道**（**闭合机制**）。
    ///
    /// 默认实现 = `self.fuse(lanes, k)` ⇒ **既有实现一行不改**、**默认逐位一致**。
    /// `RrfFusion` 覆写：先 `self.weights_override(ctx)` 决策 → `Some(w)` 则以 `w` 跑 RRF
    /// （抽一个**带权重的私有 helper**，与 `fuse` 共用同一段数学 ⇒ 不会出现两份 RRF）
    /// → `None` 则退回 `self.fuse(lanes, k)`。
    ///
    /// 🔴 **为什么必须再多这一个方法**（第 2 轮评审 **P3-1**，建议随本设计补齐）：
    /// 只加 `weights_override` 时，「`Some(w)` 时**用 `w`**」这句话**没有落点** ——
    /// 编排层**消费不了**它的返回值。四条替代路**逐条堵死**：
    /// ① 改 `fuse` 签名加 `ctx` = **N3-B**，破坏性（下游自定义实现全要改）；
    /// ② 每查询重建 `RrfFusion::new(k, w)` —— 编排层只持 `&dyn`（做不到），且 = N3 已否决的路 A；
    /// ③ 策略内部可变性 —— 破坏 `Send + Sync` 与并发**逐位一致**（违反 NFR-10 ①）；
    /// ④ 编排层自己拿 `lanes` 重算 RRF —— 绕开策略抽象，且对 `WeightedFusion` 是**错的**。
    ///
    /// ⚠️ 与 `Reranker::candidate_window` 先例的**差别**：后者的返回值（`usize`）
    /// 编排层**自己就能消费**（进 `candidate_k`）⇒ 一个方法即闭环；
    /// `weights_override` 的返回值编排层**消费不了** ⇒ 必须再补一个「消费方」。
    fn fuse_adaptive(
        &self,
        lanes: &[LaneResults],
        k: usize,
        _ctx: &FusionCtx<'_>,
    ) -> Vec<(ChunkId, Score)> {
        self.fuse(lanes, k)
    }
}
```

- `LaneStats`（设计期定形为**最小集**）：`len` / `top_score` / `last_score`（都是 `lanes` 已有的量）；
  ⚠️ **空态**见上面的类型文档（**P4-1**）。
- **编排层**（`searcher.rs:374` 附近）的**两条显式规则**（第 2 轮评审 **P3-1**）：
  1. **`Config::adaptive_fusion == false`** ⇒ **不调用 `fuse_adaptive`、不构造 `FusionCtx`**
     （仍走今天的 `parts.fusion.fuse(&lanes, candidate_k)`）⇒ **S9-2 的逐位一致是结构性的**，
     而不是靠「调用者记得别传」的约定；
  2. **`== true`** ⇒ 构造 `FusionCtx` → 调 `fuse_adaptive(&lanes, candidate_k, &ctx)`；
     `w` 的**决策与应用都在策略内部** ⇒ 编排层**不需要、也不能**消费 `w`（融合侧的口径）。
  3. **可观测上报**（第 3 轮评审 **P4-9**；规则 2 的**必要补充**）：开关开时，编排层
     **除调 `fuse_adaptive` 外、另调一次 `weights_override(&ctx)` 专用于填 `Metrics`**
     （`fusion_weights` / `fusion_signal`）。
     - **为什么不是「复用 `fuse_adaptive` 内部的决策结果」**：`weights_override` 是 `&self` 的
       **纯函数**（确定性、无内部状态）⇒ 对同一个 `ctx` 再调一次**必然得到同一个 `w`**
       ⇒ 上报值与实际用于融合的权重**不可能不一致**；
     - ⚠️ **为什么不改 `fuse_adaptive` 的返回签名**（如返回 `(hits, Option<Vec<f32>>)`）：
       那会让它与 `fuse` **不再同形** ⇒ 破坏「默认 = `self.fuse`」的转发、**也破坏 S9-T6 的等价性判据**；
     - ⚠️ **为什么不让策略把 `w` 写进某个共享位置**：需内部可变性 ⇒ 破坏 `Send + Sync`
       与并发逐位一致（NFR-10 ①，同 §4.1 排除法 ③）。
- ⚠️ **不改 `fuse` 的签名**（N3）；⚠️ **不新增 `FusionStrategy` 实现**（`AdaptiveRrfFusion`
  这类实现**走不通** —— 它的 `fuse` 拿不到 `ctx`）。

### 4.2 自适应规则（两级；**零参数 / 单阈值**，D-S9-08）

| 级 | 触发条件 | 动作 | 是否需要标定 |
| --- | --- | --- | --- |
| **tier 1**（**零参数、确定性**） | **某 lane 为空**（`len == 0`） | 把该 lane 的权重置 **0**（**不**从 `lanes` 里删 —— I9-2） | ❌ 不需要（`len == 0` 是确定事实） |
| **tier 2**（**单阈值**） | 信号 `s < θ`（信号 = 「BM25 路的弱路程度」，**候选**见 §4.3） | `w_bm25` 按**单参数**单调映射降到 `0`（或档位 `{1.0, 0.5, 0.0}`） | ✅ **spike S9-S1**（决策门见 §4.5） |

⚠️ 「某 lane 为空」在 Hybrid 下**可达**：`lanes` 是「两个 `Some` 就 push」（`searcher.rs:362-367`），而 lane 自身可以是**空 vec**（`to_lane` 不过滤、召回可以 0 命中）。
⚠️ **实现期须核实**（**Q9-2**）：BM25 是否会产生 **`score == 0` 的非空 lane**（`to_lane` 不过滤，但上游是否已过滤未核）—— 若会，则 tier 1 可扩为「`len == 0` **或** `top_score ≤ 0`」。**本文不拍这个前提**。

🔴 **tier 1 单独不改变排序**（第 2 轮评审 **P4-4**，本文原未点破）：RRF **只用名次**（`w / (k + rank + 1)`），而 `len == 0` 的 lane 在 `fuse` 内**零次迭代**（`rrf.rs:57-67`）⇒ 它**本来就零贡献** ⇒ 「把它的权重置 0」**不改变任何排序与分值**，开不开 tier 1 的 `hits` **逐位相同**。⇒ tier 1 的真实价值只有三条：① `Metrics.fusion_weights` 的**可观测语义**（报 `Some([0.0, w])` vs `None`）；② 为 **Q9-2** 的「**非空但全零分 lane**」扩展定形 —— 那才是有排序效应的场景；③ I9-2「置 0、**不得删槽**」的安全实现模板。
⚠️ 连锁修正：**`S9-T7` 的断言必须落在可观测面**（见 §7）—— 只断言「结果 ≡ 单路」在**不开 tier 1** 时也成立 ⇒ 区分不了 tier 1 是否生效。

### 4.3 tier 2 的候选信号（**都是检索自身可观测的量**）

| 候选 | 构造 | **对应的 ctx 字段 / 编排层预算出的量**（**P4-1**） | 优点 | 风险 |
| --- | --- | --- | --- | --- |
| **s1** 两路 top-k **重合率** | `\|BM25 top-k ∩ Vector top-k\| / k` | ⚠️ **现最小集装不下** ⇒ 需新增 `LaneStats.chunk_ids`（该路的 `chunk_id` 切片；**只传切片、不拷数据**）+ 同步进 `S9-T3` 白名单 | 纯检索侧、无需标注、与「词面 vs 语义」分歧直接相关 | 对 `k` 敏感（须与判据的 `k` 钉死）；新增字段 ⇒ 过 I9-1 自检 |
| **s2** BM25 路的**内部分布** | `top_score / (top_score − last_score)` 之类 | ✅ **已在最小集**（`top_score` / `last_score`） | 只用一路 | BM25 无上界 ⇒ 绝对量不可比；形态须先实测 |
| **s3** query 词项的**索引侧覆盖** | 用 `Query` 的词项去 `Index` 查 `df(t)` / 命中词项数占比 | ⚠️ 需新增 `FusionCtx.query_df`（编排层在**召回前**查 `Index::doc_freq`，`retriever/bm25.rs:107`）+ 同步进 `S9-T3` 白名单 | **最接近「词面重合」的代理**且**不依赖 qrels** | 多一次索引侧查询（成本 O(词项数)，与 §2.1 的「不回捞正文」不冲突——只查词典） |
| **s4** query 侧字符特征（桶的**纯 query 部分**） | 含 ASCII / 字符数 ≥ 16 | ⚠️ 现只有 `query_terms: usize` ⇒ 需扩为 `query_chars: usize` + `has_ascii: bool` + 同步进 `S9-T3` 白名单 | **零成本**、与既有桶定义的 `mixed` / `natural` **同源**、**无标注** | 只覆盖两桶；对 exact/paraphrase 无区分力 |

🔴 **显式规则**（第 1 轮评审 **P4-1**）：**`FusionCtx` / `LaneStats` 新增任何字段，
必须同步进 `S9-T3` 的白名单断言**。理由：I9-1 的判据形状是「字段集合 **==** 白名单」
⇒ 「临时加字段」会让 `S9-T3` **当场红** ⇒ PR9-1 实现期的「临时加字段」不可能静默变成违约
（不加这条规则，§4.3 的三个候选信号在 **PR9-1 定形前**都构造不出来，而加了又说不清算不算违约）。

⇒ **设计期不自选**：**spike S9-S1** 把 s1 / s3 / s4 各跑一遍**灵敏度曲线**，按 §4.5 的决策门选一个（选不出来就判「不投」）。

### 4.4 不变式清单

| 编号 | 不变式 | 判据 |
| --- | --- | --- |
| **I9-1** | 自适应信号**只由**「召回结果 / query / 索引统计」构造；**永不**读 qrels、桶标签、或任何相关性数据 | **S9-3**（源码级 + 类型级） |
| **I9-2** | **禁止**通过「从 `lanes` 里删掉一路」来剔除该路 —— 权重按 `lane_idx` 取（`rrf.rs:61-62`）⇒ 删 lane 会让权重**串到另一路** | `S9-T2`（空 lane 的槽位仍在、权重仍对齐） |
| **I9-3** | 关闭开关时**逐位一致** | **S9-2** |
| **I9-4** | 自适应**不改**候选池 / 截断顺序 / `candidate_k` ⇒ 只改融合权重 | `S9-T4`（`candidate_k` 两侧相同；`Metrics.candidates` 不变） |
| **I9-5** | 自适应**不引入**随机（NFR-06） | 代码里无 RNG（评审红线） |
| **I9-6** | 精排**一字不动**（`Reranker` / `candidate_window` / `R` 全部原样） | `S9-T5` |

### 4.5 标定设计（spike **S9-S1**）与决策门

**协议**（硬纪律）：

```text
① 先冻结图：helix build --index <snap>（一次），此后所有臂 --index <snap> --runs 1
② 固定 k = 10、candidate_k 两侧相同（都是 max(3k, 10) = 30 —— 精排关 ⇒ window = k）
③ 先跑 baseline：--rrf-weights 1.0,1.5   （= 今天的默认；这就是"固定权重"臂）
④ 再逐 θ 跑自适应臂（同一冻结图）⇒ 出「θ 灵敏度曲线」
⑤ 配对：脚本按 per_query 的 qid 配对，逐桶算 ΔMRR@10(1)，并出 4 桶 + 全局 4 条
```

**决策门**（**合取**，缺一即判「不投」）：

| 门 | 判据 | 阈值 |
| --- | --- | --- |
| G1 | paraphrase 桶 `ΔMRR`（自适应 − 固定权重） | **≥ +0.01**（S9-1a） |
| G2 | exact / natural / mixed 三桶**各自**的 `ΔMRR` | **≥ −0.0024**（S9-1c） |
| G3 | 全量 320 条 `ΔMRR` | **≥ 0**（S9-1d） |
| G4 | **G1 ∧ G2 ∧ G3 在 θ 的一段区间内同时成立**（不是单点） | 区间宽度 ≥ 一个可解释的档（如 0.1 的信号量纲） |
| G5 | 判据**只看名次**（对 R46 免疫）+ 精排关 + 同一冻结图 | 结构条件，逐条自证 |

**θ 扫描网格（预注册，第 1 轮评审 P4-2）**

> G4 的「区间宽度 ≥ 一个可解释的档」必须在**看到数据之前**钉死（同 `NFR-10` ③ 判后记录的
> **预注册义务**：判据的严格度不许在出数后被事后解释）。

| 项 | 预注册值 |
| --- | --- |
| 信号归一化 | `s` **归一化到 `[0, 1]`**（各候选信号量纲不同 ⇒ 必须先归一）；**归一化式随信号选型在 S9-S1 里一并预注册**（⇒ 选型与归一化**同时**冻结，不许先选信号、后挑归一化） |
| θ 网格 | **`0.00, 0.05, 0.10, …, 0.95, 1.00`** —— 起点 `0.00`、步长 **`0.05`**、终点 `1.00`，共 **21 点** |
| 「一个可解释的档」 | = **网格上 ≥ 3 个连续点** ⇒ 区间宽度 **≥ 0.10** |
| 落地 | 网格与「档」的定义**写进 `scripts/eval_s9.sh` 的常量**；脚本输出直接标注每个 θ 与其归属的达标区间 ⇒ 判据可复核、不需人工解释 |

⚠️ **G4 是防过拟合的关键**（对应 N5）：**单点达标不算达标**（在 80 条上总能挑到一个点）。
若不存在这样的区间 ⇒ 判「不投」。
⚠️ **报数纪律**（D-S9-05）：报 `Δ` 的**幅度与同向**，**不写「显著」**；同时报每桶 `n` 与 baseline 的绝对数，便于评审独立复算。

### 4.6 与 R45 / R46 的边界

- **R45**（窗口被 `candidate_k` 静默夹取）：本步**不碰窗口**（I9-6）；判据两侧 `candidate_k` 相同 ⇒ R45 不进本步的判据面，**只在 §9 登记为「Step 10 的输入」**。
- **R46**（`Hit.score` 语义随开关而变）：判据只看**名次** ⇒ 免疫；但**必须钉死精排开关**（精排打开会改名次 ⇒ 那是**另一个读数面**，只作对照）。

### 4.7 T7-16（Agent query 评测集）与 T7-15（假负例对照）

**T7-16**（**只作对照**，D-S9-06 / D-S9-07）：

- 通道：**外部 LLM API**，形态 **provider-agnostic** —— 三个环境变量（`HELIX_LLM_BASE_URL` / `HELIX_LLM_API_KEY` / `HELIX_LLM_MODEL`）+ OpenAI 兼容 `POST /chat/completions`；**密钥只走环境变量、不入库**。
- 生成协议：多轮 + 自查（prompt 与温度/种子等参数**必须记录**）⇒ **R57**（可复现性：模型版本漂移）。
- **产出形态**：query + **自带弱标注**（否则既算不出桶、也评不了分 —— **N6**）；**冻结入库**（不依赖"下次再跑一遍就能得到同样的集"）。
- 🔴 **边界**：**NFR-15 的主判据不建立在它上**（主判据 = 既有 320 条；见 D-S9-04）—— 否则标注噪声会混进判据。

**T7-15**（**只作对照**）：用「**仅 qrels 语料**」重建一份对照资产，量化随机负例带来的假负例对指标的**影响幅度**；protocol 与 T7-16 同级（冻结 + 只作对照），读数落 §8.16。

---

### 4.8 第 1~2 轮评审响应（2026-09-24；本文 v0.1 → **v0.2**）

> 两轮评审（`pulls/70/reviews` `5300723031` / `5300824624`，基线 `116b1d8`）共
> **0×P1/P2 + 1×P3 + 6×P4**，结论 = 第 1 轮「通过」、第 2 轮「**有条件通过**」。
> 🔴 **第 2 轮评审如实声明**：第 1 轮以事实核验为主（源码主张 / 读数 / 锚点**逐条复算、全部属实**），
> 对**设计机制本身**推得不够 ⇒ **P3-1 在第 1 轮被放过**。⇒ 这本身就是一条教训：
> **「事实全对」不代表「机制闭合」** —— 核验清单里要单列「机制是否闭合」这一类。

#### 逐条处置

| # | 评审意见 | 处置 | 落点 |
| --- | --- | --- | --- |
| **P3-1** 🔴 | `weights_override` 只定义「决策」、**没定义「应用」** ⇒ 机制不闭合 | ✅ **本 PR 补写**：新增 provided 方法 **`fuse_adaptive`**（默认 = `self.fuse` ⇒ 既有实现零改动）；`RrfFusion` 覆写之；`weights_override` 保留为**决策钩子**；并写死「**开关关 ⇒ 编排层不调用 `fuse_adaptive`、不构造 ctx**」 | §4.1 / §6.4 / §7（S9-T6）/ 附录 A / 架构 §5.6 |
| **P4-1** | `FusionCtx` 最小集与 §4.3 候选信号**不闭合**（s1 / s3 / s4 都放不进现定字段） | ✅ §4.3 补「**对应的 ctx 字段**」列 + 显式规则「**新增字段须同步 `S9-T3` 白名单**」；`LaneStats` 补**空态定义**（`Option`，不用 `0.0` 哨兵） | §4.1（`LaneStats`）/ §4.3 |
| **P4-2** | G4 的「一个可解释的档」是定性表述 ⇒ 应**预注册**网格 | ✅ 预注册：θ 网格 `0.00 → 1.00`、步长 `0.05`（21 点）；「档」= **≥ 3 个连续点**（宽度 ≥ 0.10）；并写进 `eval_s9.sh` 常量 | §4.5 |
| **P4-3** | 约 15 处**裸行号锚点**合并即漂移（本文自己挪的行） | ✅ 全部改为 **「§ 锚点 + 文档版本 + 行号」双钉**，行号取 **HEAD（= 合并后）** 的实测值；并在文首增列「**锚点体例**」行 | 文首 / §1.1 / §1.2 / §2.4 N2 / §3 / §6.3 / §6.4 / 附录 D |
| **P4-4** | tier 1 是**行为空转**；`S9-T7` 按现文区分不了它是否生效 | ✅ §4.2 点破「RRF 只用名次 ⇒ 空 lane 本就零贡献」+ 列出它的**三条真实价值**；`S9-T7` 改为断言**可观测面**（`fusion_weights == Some([0.0, w])`） | §4.2 / §7（S9-T7） |
| **P4-5** | S9-1b / S9-1c 的容差**口径错标**（把政策容差标成噪声界；两个数不一致） | ✅ 统一为 **`0.0024`**，备注改为「**政策容差（交易余量）**」并写明：同冻结图 ⇒ Δ 是**精确值**、不含漂移成分；取实测量级只为①不凭空造数 ②为跨图复跑留余量 | §1.3（S9-1b / S9-1c） |
| **P4-6** | 附录 B 的 θ **无从传入**（布尔开关 + θ 只出现在文件名） | ✅ 改为 `--adaptive-fusion-theta <v>` 值形态 + `for θ in <预注册网格>` 循环；注明取自 §4.5。⚠️ **该写法的循环变量 `θ` 非法**（第 3 轮评审 **P4-8**）⇒ 已改为 ASCII `t`，**见 §4.9** | 附录 B |
| 流程 | **Step 9 跟踪 issue** 随本 PR 建 | ✅ 已建 **#71**（标签 `enhancement` / `V2-Step8+`；沿用 Steps 2~8 体例） | 文首「上游」/ §8 |

#### 与评审不同 / 需更正的一处（数据诚信）

- ⚠️ 评审 P4-5 说「S9-1b 的 `0.002` **无出处**」—— **属实**：那是我从 issue #4 的验收建议里
  顺手取的数，与 S9-1c 的 `0.0024` **不是同一个来源**。已统一为 `0.0024` 并在备注里写明出处。
- ✅ 除 P4-5 外，**评审的其余事实核验（V1~V16）我逐条复核均属实**，其中 P3-1 我**独立复算过**：
  编排层确实只有 `&dyn FusionStrategy` + `fuse` 读私有字段（`rrf.rs:62`）⇒ **没有任何通道**送 `w`。

#### 本轮无断言可变异

改动全部落在**设计文档**（无代码、无测试）⇒ **不适用变异自证**。替代验证：
① 全部替换**多锚点预检**（`--check`，每条 `assert count == 1`）；
② `mdcheck.py` 对本文 **0 处**；
③ 跨文档一致性断言（见提交信息）；
④ 反向断言：旧措辞（如「噪声容差」「`0.002`（噪声容差）」）**必须已消失**。

#### 未闭环（如实登记）

- **Q9-1 / Q9-2** 仍是**实现期**问题（信号选型 / BM25 零分 lane）⇒ 设计期不拍。
- **`fuse_adaptive` 的签名是设计期形态**：`LaneResults` 与 `FusionCtx` 的**具体字段**在 PR9-1
  定形时可能微调（尤其 s1 / s3 / s4 的选型会决定 ctx 要不要扩）⇒ 本节的**机制**（两方法 + 开关规则）
  已闭合，**字段名**未冻结。

---

### 4.9 第 3 轮评审响应（2026-09-24；本文 v0.2 → **v0.3 ⇒ 可开工**）

> 第 3 轮评审（`pulls/70/reviews` `5303731329`，基线 `a6d87e4`）= **通过** + **3×P4（均不阻塞）**；
> 前两轮的 **1×P3 + 6×P4 逐条验证为闭合**。⇒ 本版把这 3 条一并处置完毕。

#### 逐条处置

| # | 评审意见 | 处置 |
| --- | --- | --- |
| **P4-7** | 架构侧 **3 处双钉行号又漂了（+26）** —— 附录 D 的 §14.5 / §14.6、§3-8 的读数纪律行 | ✅ 全部重取 **`a6d87e4` 实测值**：§14.5 `:1644 → :1670`、§14.6 `:1662-1676 → :1688-1702`（`R54` 在 `:1702`）、读数纪律 `:1687 → :1713`；并把**根因**写成文首「锚点体例」的两条硬纪律（见下） |
| **P4-8** | 附录 B 的 `for θ in …` **不是合法 bash**（`` `θ': not a valid identifier ``） | ✅ 循环变量改 ASCII **`t`**（θ 只留注释 / 文件名语义）；并**独立复算属实**（bash 3.2 与 5.x 都报同一条错）。⚠️ 顺带登记：守门脚本 `check_shell_expansion.py` **扫不到文档附录里的 bash 块** ⇒ 已记进 **#69** |
| **P4-9** | `Metrics.fusion_weights` / `fusion_signal` 的**填充路径未闭合**（P3-1 修复的连锁遗漏） | ✅ §4.1 补**规则 3**：开关开时编排层**另调一次** `weights_override(&ctx)` 专用于填 `Metrics`（`&self` 纯函数 ⇒ 两次调用必然同值）；并写明**为什么不能**改 `fuse_adaptive` 的返回签名 / 不能让策略共享可变状态；§6.2 同步补「填充路径」 |

#### 🔴 P4-7 的根因（值得跨轮记住）

**行号是在 `f3d7b2c` 时点量的，而随后的 `a6d87e4` 自己给架构 §5.6 加了 25 行 + 1 个版本表行 ⇒ 挪了下方所有锚点。**
⇒ 两条硬纪律已写进文首「锚点体例」：

1. **版本标签与行号必须同一时点**（本轮曾标 `v1.24` 却给了 `v1.23` 时点的行号 ⇒ **标签本身是假的**）；
2. **行号必须在「本 PR 全部编辑完成后的最终 head」上量** —— 只要本 PR 自身会在锚点**之上**插入内容，中途量到的行号必然漂。

#### ⚠️ 一条自我登记：修 P4-6 时造出了 P4-8

附录 B 的 `for θ in …` 是**我在第 2 轮响应里为修 P4-6（θ 参数化）而写的**，结果它是非法 bash。
⇒ **「修复」本身也是新代码 / 新命令**，同样要过「能不能真跑」这一关；这条与
「改了做法要回头扫描述该做法的句子」（第 2 轮）是**同一族的另一半**：**改了命令就要回头问「它真能跑吗」**。

#### ✅ 开工前清单（PR9-1 可据此直接开工）

| 项 | 状态 |
| --- | --- |
| 形态（两个 provided 方法 + 编排层三条规则） | ✅ §4.1（**机制已闭合**）；⚠️ PR9-1 实现时增至**三个**（补 `fusion_signal` 上报通道，见 §4.10 ②——本清单为 v0.3 时点历史行） |
| `FusionCtx` / `LaneStats` 字段与**空态** | ✅ §4.1（含「新增字段须同步 `S9-T3` 白名单」） |
| 自适应规则 tier 1 / tier 2 | ✅ §4.2（tier 1 的**行为空转**已点破） |
| tier 2 的候选信号与 ctx 字段 | ✅ §4.3（**选型留给 spike S9-S1**，不自选） |
| 判据与决策门（含 θ 网格预注册） | ✅ §4.5（`G1~G5`，网格 `0.00→1.00` 步长 `0.05`） |
| 不变式 / 测试计划 | ✅ §4.4 / §7（`S9-T1 ~ S9-T12`） |
| `Metrics` 两个字段与**填充路径** | ✅ §6.2 + §4.1 规则 3（本轮补齐） |
| 兼容性 / 影响面 | ✅ §6.1（默认关 ⇒ **零行为变化**） |
| PR 切分 | ✅ §8（`PR9-1` 含生产代码，**默认 `false`**） |
| 跟踪 issue | ✅ **#71** |

⚠️ **仍留给实现期 / spike 的（设计期刻意不拍）**：`Q9-1`（tier 2 信号选型 → S9-S1）、
`Q9-2`（BM25 是否产生 `score == 0` 的非空 lane）、`Q9-3~Q9-5`（评审拍板项）。

---

### 4.10 PR9-1 实现期定形记录（2026-09-24；本文 v0.3 → **v0.4**）

> §4.8 的「未闭环」里写过：`fuse_adaptive` 的签名与 ctx 的**具体字段**在 PR9-1 定形时可能微调。
> 本节记录实际定形——**机制（两方法 + 开关三规则）零改动落地**；以下为字段面与连带决策。
> 对应实现 = PR9-1（S9-02 + S9-03 + 判据 S9-T1~T10）。

#### ① `FusionCtx` / `LaneStats` 的最终字段（S9-T3 白名单即此）

| 类型 | 定形字段 | 与设计原文的差异与理由 |
| --- | --- | --- |
| `LaneStats<'a>` | `len` / `top_score: Option<f32>` / `last_score: Option<f32>` / **`entries: &'a [(ChunkId, Score)]`** | 设计 §4.3 s1 说的是「`chunk_ids` 切片」——但 `LaneResults` 的元素是 `(ChunkId, Score)` **元组**，从元组切片投影不出独立的 `&[ChunkId]`（除非拷贝）⇒ 取**语义等价的零拷贝整段借用**（s1 对 `entries` 逐条取 `id` 消费） |
| `FusionCtx<'a>` | `lanes` / `query_terms` / **`query_chars`** / **`has_ascii`** / **`query_df: f32`** / **`top_k: usize`** | 后四个是 §4.3 的 s1/s3/s4 落位：`query_df` = **去重**词项中 `df > 0` 的占比（跨段对每段各查、任一段有即算）；`top_k` = 判据的 `k`（s1 的 top-k 口径须与判据钉死）且 `weights_override` 签名里没有 `k` ⇒ 只能经 ctx 传入 |

⚠️ **s2 与三个无消费者字段的归属（v0.5 / 评审 P4-3 登记）**：候选信号 s2（BM25 路内部分布，
§4.3 的 `top_score / last_score`）**不在本 PR 的三个已实现候选内**（CLI 只暴露
`overlap|df|shape`）；`top_score` / `last_score` / `query_terms` 当前**无生产消费者**——
保留是为「spike 后 s2 上桌 / 新 query 侧信号」时零改动（同 `embed_sessions` 预留先例），
删除须同步 `S9_T3` 白名单（判据已把它们钉成契约）。

#### ② 第三个 provided 方法 `fusion_signal`（P4-9 的连锁缺口，实现期补闭合）

§4.1 规则 3 说「编排层另调一次 `weights_override(&ctx)` 专用于填 `Metrics`（`fusion_weights` / `fusion_signal`）」——
但 `weights_override` 的返回类型 `Option<Vec<f32>>` **装不下信号值**（设计自身的不闭合处）。
⇒ 补 `fn fusion_signal(&self, ctx) -> Option<f32>`（provided，默认 `None`）：
- 与 `weights_override` 同为 `&self` **纯函数** ⇒ 规则 3 的「再调一次必然同值」论证**原样成立**；
- 不改 `weights_override` / `fuse_adaptive` 的已评审签名（P3-1 的谈判成果不动）；
- 信号无定义（如 s1 需要 exactly 两路 / `k == 0`）⇒ `None`，tier 2 不触发。

#### ③ `Metrics` 拆 `Copy`（设计未点破的连锁）

`fusion_weights: Option<Vec<f32>>` 与 `#[derive(Copy)]` 互斥 ⇒ `Metrics` 自 Step 9 起
**只有 `Clone`**。实现前实测核对：全仓 `Metrics` 消费面均为**字段访问 / 引用**（无隐式拷贝依赖）
⇒ 拆除零影响。**此为 §6.1「按字面是破坏性」之外的又一处**，PR 正文已单列。

#### ④ Q9-2 定论：tier 1 **不扩**「`top ≤ 0`」

BM25 累加点（`retriever/bm25.rs` 的 TAAT 循环）：`partial = idf · tf(k1+1)/(tf+norm)`，
其中 `idf = ln(1 + x)`、`x = (n − df + 0.5)/(df + 0.5)` —— `df ≤ n ⇒ x > 0 ⇒ idf > 0`；
posting 条目 `tf ≥ 1` ⇒ **每个进入 lane 的条目分值恒 `> 0`**。
⇒ 「非空但全零分的 BM25 lane」**结构性不存在**，tier 1 保持「`len == 0`」单一条件。

#### ⑤ tier 2 与信号的实现取形

- **降权取形 = 降到 `0`**（§4.2「单调映射降到 0」的最简形态；非缩放/档位）⇒ S9-1b 的上界
  「结构性可达」成立（触发后该次 hybrid 的名次 ≡ vector 单路）。
- **s4 = `has_ascii` 的 0/1 二值**（与 `t2_prep.rs:193` 的 mixed 判据同源；`query_chars` 入 ctx
  但不进 s4——「短 query ⇒ exact 桶 ⇒ BM25 占优」与「低信号 ⇒ 弃 BM25」方向相反，掺进去
  反而伤 exact 桶）。⚠️ 二值信号 ⇒ θ 网格上只有两个行为档，spike 如实读。
- **s1 的短 lane 仍按 k 归一**（分母换成实际长度会把「分歧」藏起来）。
- **三信号都可经 CLI 选择**（`--adaptive-fusion-signal overlap|df|shape`）——spike S9-S1 要
  「各跑一遍灵敏度曲线」（§4.3），没有选择通道就跑不了对照。

#### ⑥ CLI 面（附录 B 的落地口径）

`--adaptive-fusion on|off`（**值形态**，默认 off；v0.5 起经 `BoolishValueParser` 解析——
`on`/`off`/`true`/`false` 都接受，评审 P2-1 后补齐，**判据** = `adaptive_fusion_cli值形态经clap解析`）
+ `--adaptive-fusion-theta <V>`（开关开时**必填、无默认值**——隐式阈值会造成不可复现读数）
+ `--adaptive-fusion-signal <NAME>`（默认 `overlap` 为**占位**、非定稿结论）。
开关关时单独给出 θ / **非默认值**信号 ⇒ **报错**（不静默忽略，NFR-07；⚠️ P4-5 范围登记：
`--adaptive-fusion-signal overlap` 与不传在 clap 默认值下**不可区分** ⇒ 不会被拒）。
档位进 stdout 摘要与 JSON `config.adaptive_fusion`（sweep 的 21 个 θ 文件各自自证跑的哪一档）。

#### ⑦ 覆盖边界（如实登记）

- `bench` 的 `make_searcher` 装配段（`adaptive → with_adaptive_fusion` / `RrfFusion::new_adaptive`）
  **无单测**——需要真实 `Setup`（语料 + embedder）；由 PR9-2 的 spike **端到端覆盖**
  （跑数时 `fusion_weights` 断言先红即暴露断线）。
- `S9-T11`（判据臂）需冻结图 + 语料，按 §7 标注走 `scripts/eval_s9.sh`（PR9-2 交付），
  本 PR 不含。
- 变异验证 6 组（tier1 关 / tier2 关 / tier2 降错路 / 编排开关失效 / Metrics 不填权重 /
  df 覆盖恒零）**全部命中**，逐轮 md5 核对还原。⚠️ **v0.5 更正（评审 P4-1 独立复算）**：
  首轮登记按**过滤子集**跑 ⇒ M1 实为 **3** 条（漏 `S9_T7_编排层tier1可观测面`）、M2 实为
  **5** 条（漏 `S9_门面adaptive_fusion真的流到编排层`）——修正版与运行范围见 **§4.11**
  的连带表；此后变异一律 `--lib` 全量。

---

### 4.11 PR9-1 第 1 轮评审响应（2026-09-25；本文 v0.4 → **v0.5**）

> 评审（`pulls/72/reviews` `5307035441`，基线 `d38c6b0`，10 条行内）= **有条件通过——
> 无阻塞项（0×P1）+ 1×P2 + 3×P3 + 6×P4**。评审为**独立验证型复审**：新增用例数、CI 读数、
> 逐位重构保真、`t2_prep` 同源声明均被独立复核属实（其中「`s4` 与 `t2_prep.rs:193/195` 逐字
> 对上」获专项认可）。本节逐条处置；机制面零改动（P2-1 是 CLI 参数声明的 1 行 + 判据补齐）。

#### 逐条处置

| # | 评审意见 | 处置 | 落点 |
| --- | --- | --- | --- |
| **P2-1** 🔴 | `--adaptive-fusion` 值形态四处不一致（`value_name="on\|off"` vs 实际只认 `true\|false`）；`on` 实测被 clap 拒 ⇒ **PR9-2 照 CHANGELOG/设计写 `on` 会 21 点全失败** | ✅ **采纳口径②**（评审已本机验证过）：加 `value_parser = BoolishValueParser`（`on`/`off`/`true`/`false` 都接受，与 `--brute-fallback off` 同族）+ **补「值形态经 clap 端到端」的判据**（评审点破根因：原 8 条参数用例全部绕开 clap ⇒ 值形态零判据面）+ **四处文案同步**（stdout 摘要/两条 bail/θ 必填提示改 `on`） | `bench.rs`（1 行代码 + 1 条新用例 `adaptive_fusion_cli值形态经clap解析`）+ 本表 + §4.10 ⑥ 更正 + CHANGELOG |
| **P3-1** | 设计头部状态行/日期行未随 v0.4 更新（「可开工」「纯设计」成将来时残留） | ✅ 版本行升 v0.5、状态行改「设计已过三轮评审 + **PR9-1 已实现**（PR #72）」、日期行分版本标注。⚠️ **第 2 轮评审 P4-1 更正**：本行原文「§4.9 逐项加注」**与实物不符**——实际只对 §4.9 **形态行**（第 1 行）加注；§6.4 / 附录 A 实改；**§7 的 S9-T6 行当时未加注**（已在本轮补上，「两个」→「三个」）。原措辞引文：「§4.9 开工前清单（历史节）逐项加『已完成，见 §4.10/§4.11』注记」 | 文首三行 + §4.9（形态行）+ §6.4 + 附录 A；§7 S9-T6 行本轮补注 |
| **P3-2** | `fusion_signal` 只落 §4.10 ②；**附录 A（API 一览）与 §6.4（架构回写清单）仍写「两个」** ⇒ PR9-4 回写会漏 | ✅ 附录 A 补 `FusionStrategy::fusion_signal` 行；§6.4「两个」改「三个」并点名第三个 + 「回写时以附录 A 为准」；§4.9 清单的历史行加注（增至三个，见 §4.10 ②） | 附录 A / §6.4 / §4.9 |
| **P3-3** | I9-1 反扫**不覆盖 `FusionCtx` 的唯一生产构造点**（`query/searcher.rs:394`）⇒ 标签泄漏可静默通过 | ✅ **扩扫**：`S9_T3` ② 的扫描集加入 `include_str!("../query/searcher.rs")`（第五个文件，标注「构造点」）；并在断言消息写明「该文件将来提这三个英文标识会红——**有意的**（它是信号输入端）」。**变异实测**：在构造点注入禁词 ⇒ 断言**当场红**且消息准确指向该文件（注入串含完整禁词才有效——首轮两个注入串因带空格/加号不成串而无效，已废弃） | `adaptive.rs` `S9_T3` ② |
| **P4-1** | 变异命中登记各少 1 条（M1 实测 3 / M2 实测 5）且未标运行范围 | ✅ **独立复算属实**（本机复现：M1 = 3 条、M2 = 5 条，`272 passed; 5 failed; 7 ignored` 与评审逐字一致）。**根因确认**：我当时按**过滤子集**跑变异 ⇒ 运行范围窄于注入的爆炸半径（M1 漏的在 `query/searcher.rs`、M2 漏的在门面段）。§4.10 ⑦ 的登记表**补齐两条命中**并加运行范围标注；此后变异一律 `--lib` 全量 | §4.10 ⑦（下表） |
| **P4-2** | `tier1与tier2叠加不重复打折` 在 tier 1 关闭时仍绿（θ=0.9 时 tier 2 独立置 0 ⇒ 幂等不可分辨） | ✅ 采纳「如实两记」：用例文档改写为「w[0] 幂等 + **w[1] 不被误伤**」，并明写「本用例区分不了 tier 1 是否生效（M1 实测仍绿）；当前实现没有叠加打折路径」 | `rrf.rs` 用例文档 |
| **P4-3** | 白名单 3 字段（`top_score`/`last_score`/`query_terms`）无消费者，s2 未实现而 §4.10 未登记 | ✅ 两处登记：`LaneStats`/`FusionCtx` 的类型文档写明「当前无生产消费者；为 s2 及将来 query 侧信号预留字段位（同 `embed_sessions` 先例）；删须同步 S9_T3 白名单」；§4.10 ① 补 s2 不在三个候选内 | `adaptive.rs` 类型文档 + §4.10 ① |
| **P4-4** | `S9-T4` 注释「（排序会变）」无断言落点 | ✅ **补行为断言**（比改措辞更强）：`assert_ne!(ids_off, ids_on)` 把「触发态」从 Metrics 面钉到**行为面**。🔴 落地时抓到真问题：**首轮夹具两路名次恰好同序**（FakeEmbedder 哈希与 BM25 tf 序对齐）⇒ 行为断言**结构性必红** ⇒ 重设计**分歧夹具**（doc0=「检索段落」BM25 命中/余弦 −0.53；doc1=「段落段落」不命中/余弦 +0.88；**手算锚点**：off 态 doc0=0.04059 ＞ doc1=0.02459 ⇒ doc0 第一；触发态 0.02419 ＜ 0.02459 ⇒ **名次翻转**）——这条让 S9-T4 的对照从「规则没触发的假对照」变成真对照 | `query/searcher.rs` S9-T4 |
| **P4-5** | 「开关关时单独给信号 ⇒ 报错」在信号取默认值 `overlap` 时不成立（clap 默认值不可区分显式/缺省） | ✅ **适用范围写明三处**：`adaptive_rule_of` rustdoc（「会被拒的是**非默认值**信号；显式 `overlap` 与不传不可区分——clap 默认值的固有局限」）+ §4.10 ⑥ + CHANGELOG | `bench.rs` rustdoc + §4.10 ⑥ |
| **P4-6** | `df > 0` = 词典收录（含未物理化墓碑）≠ 可召回 | ✅ **口径写明、不改实现**（方向保守：信号偏高 ⇒ 更少弃 BM25，不误伤该保留的路）：`df_coverage` 文档补「`df > 0` 的含义是词典收录，不是可召回 ⇒ 覆盖率期望上轻微高估；方向保守」 | `query/searcher.rs` `df_coverage` 文档 |

#### P4-1 连带：变异登记表的**修正版**（§4.10 ⑦ 原表的更正；运行范围 = `cargo test -p helix-core --lib --no-fail-fast` 全量）

| 注入 | 实测命中（**修正后**） |
| --- | --- |
| M1 tier 1 关闭 | `S9_T2_空lane槽位仍在且权重不串路` + `S9_T7_tier1可观测面与行为空转` + **`S9_T7_编排层tier1可观测面`**（共 **3** 条） |
| M2 编排层开关失效 | `S9_T1` + `S9_T4` + `S9_T7` + `S9_T10` + **`S9_门面adaptive_fusion真的流到编排层`**（共 **5** 条） |
| 本轮新增两条（响应 P2-1 / P3-3） | 去掉 `BoolishValueParser` ⇒ `adaptive_fusion_cli值形态经clap解析` 红；构造点注入禁词 ⇒ `S9_T3` ② 红且消息指向构造点文件 |

⚠️ **两条废弃的无效注入（如实登记）**：构造点变异首轮用 `buck""""et`（带引号）与 `qr+els`
（带加号）——**都不含连续的完整禁词** ⇒ 扫描「零命中」是注入串不合法的假阴性，不是扩扫
无牙齿；换连续 `qrels` 后**当场红**。

#### 与评审不同 / 补充说明

1. **P4-4 选择了比评审建议更强的处置**：评审给的两个选项是「改措辞」或「补断言」；
   补断言落地时抓出「首轮夹具两路名次恰好同序」的结构性问题 ⇒ 重设计分歧夹具（含手算
   锚点）。**这不是评审意见有误**——评审原文就写了「若该夹具确实会变」，是夹具本身的问题
   被断言逼了出来（又一次「断言要能红才有用」的实例）。
2. **P2-1 选口径②而非①**：评审两口径都实测过；取②的理由 = `on|off` 更贴合「开/关」语义、
   与 `--brute-fallback off` 同族、且评审已在本机验证过该行改动。判据补齐采纳评审的根因
   分析（原 8 条用例全绕开 clap）。
3. **P3-1 的「同类残留同批扫」**：除文首三行外，全扫了本文含「两个 provided 方法」的
   4 处——§7 的 S9-T6 行与 §4.9 清单两处是**历史语境**（P3-1 评审轮次的两通道），
   分别加注不改写；§6.4 与附录 A 两处是**将来执行依据**，按 P3-2 实改。

### 4.12 PR9-1 第 2 轮评审响应（2026-09-25；本文 v0.5 → **v0.6**）

> 评审（`pulls/72/reviews` `5314490717`，基线 `d3ebf52`，响应验证型复审 + 3 条行内）=
> **✅ 通过——无阻塞项、达到可合并形态**；新意见 = **1×P3 + 1×P4**（均不阻塞）。
> 第 1 轮 10 条处置**逐条独立验证为落实**（含手算锚点的离线重实现复算——逐位吻合）。

#### 逐条处置

| # | 评审意见 | 处置 |
| --- | --- | --- |
| **P3-1** | P2-1 的**展示面残余**：`--help` 仍同屏打印 `<on\|off>` 与 `[possible values: true, false]`（`BoolishValueParser::possible_values()` 仍返回 true/false）。⚠️ 评审自查：其第 1 轮口径②只验了「`on` 被接受」，未验该行是否消失——**一半责任在评审侧**，且已把修法在本机验过 | ✅ **采纳修法**：`#[arg]` 加 `hide_possible_values = true`（一行）；**同批补 help 渲染判据**（评审建议）：`adaptive_fusion_cli值形态经clap解析` 新增第 ⑥ 臂——断言 `render_help` 实际渲染文本含 `--adaptive-fusion <on\|off>` 且**不含** `possible values: true, false`。**变异实测**：去掉 `hide_possible_values` ⇒ 第 ⑥ 臂当场红（消息「展示面未闭合」）；**实机核验**：`cargo build -p helix` 后 `target/debug/helix bench --help` 确认 `<on\|off>` 保留、矛盾行消失（计数 = 0） |
| **P4-1** | §4.11 P3-1 行两处「已做」与实物不符：§4.9 **不是「逐项」**加注（10 行仅 1 行有）；§7 的 S9-T6 行（`:647`）**未**加注仍写「两个」 | ✅ **采纳评审倾向的口径①（改措辞兑现状）+ 顺手补注**：§4.11 P3-1 行就地更正——写明实际范围（§4.9 形态行加注 / §6.4 与附录 A 实改 / §7 S9-T6 行**当时未加注**、本轮已补），**原措辞引文保留**；§7 S9-T6 行加注「实现期增至**三个**（补 `fusion_signal`；其默认 `None` 已由 `S9_T6_默认实现…` 的断言覆盖）」 |
| （无编号） | 手算锚点独立复算通过的验证记录（`−0.5283` / `+0.8802` 逐位吻合；夹具不属 flaky 族） | 无需处置——记录在案，感谢把「上一次的建议被采纳后是否真的成立」验到底 |

#### 备注

- 评审对第 1 轮 P4-4 的**升级认定**（「夹具层面的结构缺陷」，高于其上轮所判的 P4）记录在案——
  这正是「断言要能红才有用」的实例，也修正了我们对手算锚点纪律价值的认识。
- 本轮**未复跑守门链**（评审明示不要求：P3-1 = 一行 clap 属性、P4-1 = 两处文档措辞）；
  复验范围 = 新判据用例（cli bin）+ 实机 `--help` + `mdcheck`。

### 4.13 spike S9-S1 读数与结论（2026-09-25；本文 v0.6 → **v0.7**）

> 载体 = `scripts/eval_s9.sh`（**S9-04**，可复跑）；判定 = §4.5 的五门合取（**G1~G5，已在设计期预注册**）。
> **结论 = 判「不投」**：三个候选信号**无一满足 G4**（区间达标）；其中 `df` / `shape` **连单点达标都没有**。
> ⇒ 按 **D-S9-10**：交付「工具 + 如实记录未达标」，`NFR-15` **保持「打开」**并写明**复审触发条件**（见 4.13.6）。
> ⚠️ 本节**不改** §4.5 的任何阈值 —— 判据的严格度**不许在出数后被事后解释**（预注册义务）。

#### 4.13.1 协议（`scripts/eval_s9.sh`，可复跑）

| 项 | 值 |
| --- | --- |
| 冻结图 | `data/t2-frozen.snapshot`（sha256 前 16 = `de7b3d1a8250a2d5`，54,844,663 字节）—— **所有臂同一份**（G5② ） |
| 查询集 | `data/t2-queries.jsonl`（320 条；exact / natural / mixed / paraphrase **各 80**） |
| 臂 | 三路锚点 1 + 固定权重基线 1 + **自适应 63**（3 信号 × 21 θ 点）= **65 次 `bench` 调用**（第 1 轮评审 **P4-1**：原稿在 CHANGELOG / PR 正文写「64 臂」而括号自己加起来是 65 ⇒ 统一到**实测调用数**） |
| 共同参数 | `--modes hybrid --k 10 --runs 1 --rrf-k 60 --rrf-weights 1.0,1.5`、精排关、`--no-latency` |
| 判定量 | `per_query[].mrr`（**名次函数** ⇒ 对 R46 免疫，G5③）**逐 qid 配对**求 Δ |
| 机器 / 范围 | macOS aarch64 / 本机（**效果类读数**；⚠️ 延迟类不可跨机引用 ⇒ 本节不含延迟） |
| 确定性 | **三轮全量复跑 ⇒ 63 行曲线逐位相同**（第 3 轮 = 本响应轮，专为产出持久 TSV 而跑）。脚本另含内置自检：**三个信号的 `θ=0.00` 臂各自**与基线**逐 query 零差**（v0.8 起；原稿用全局标志 ⇒ 只查第一个信号，见 §4.14 的「与评审不同之处」②） |

#### 4.13.2 三路锚点与 **Q-R1 复现**（同一冻结图，`MRR@10(1)`）

| mode | 全局 | exact | natural | mixed | paraphrase |
| --- | --- | --- | --- | --- | --- |
| bm25 | 0.6495 | 0.8477 | 0.7521 | 0.7146 | **0.2838** |
| vector | 0.6828 | 0.7696 | 0.7484 | 0.6739 | **0.5395** |
| hybrid（基线 = 固定权重） | **0.6941** | 0.8219 | 0.7691 | 0.7052 | **0.4803** |

⇒ **Q-R1 在本冻结图上复现**：paraphrase 桶 `hybrid 0.4803` **低于** `vector 0.5395`（逐 query 配对均值差 **+0.0592**）
—— 即 hybrid 在该桶被弱路拖到单路之下；而**全局** hybrid `0.6941` 仍是三路最高（S9-1d 的「全局结论保持」前提）。

#### 4.13.3 θ 灵敏度曲线（按**等价类**压缩；21 点全表见持久产物 `data/eval/s9-2026-09-25/readings.tsv`）

> 触发规则是 `s < θ` ⇒ θ 落在「两个相邻信号取值之间」时**行为完全相同** ⇒ 相邻网格点常成对相同。
> 下表把**行为相同**的 θ 合并成一行（`θ=0.55–0.60 (2 点)` 读作「该区间内 2 个网格点行为一致」）。
>
> 🔑 **持久产物（v0.8 / 第 1 轮评审 P3-3 后）**：`data/eval/s9-2026-09-25/readings.tsv`（**已入库**）——
> 315 行数据 + 头部元信息（冻结图指纹 / 桶结构 / **两个 Q-R1 口径** / 门阈值）。原稿只落 `mktemp -d`
> ⇒ 临时目录一清，「21 点全表」就**无处可指**（评审本机全盘搜过、零命中）。复现 = `OUT_DIR=<dir> ./scripts/eval_s9.sh`。

**信号 = `overlap`（s1：两路 top-10 重合率；取值是 `1/10` 的整数倍）**

| θ 区间 | 点数 | paraphrase | exact | natural | mixed | 全局 | G1 | G2 | G3 | 判定 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0.00 | 1 | +0.0000 | +0.0000 | +0.0000 | +0.0000 | +0.0000 | ❌ | ✅ | ✅ | — |
| 0.05–0.10 | 2 | −0.0028 | +0.0000 | −0.0031 | −0.0063 | −0.0031 | ❌ | ❌ | ❌ | — |
| 0.15–0.20 | 2 | −0.0068 | +0.0000 | −0.0010 | −0.0063 | −0.0035 | ❌ | ❌ | ❌ | — |
| 0.25–0.30 | 2 | −0.0026 | +0.0000 | −0.0010 | −0.0004 | −0.0010 | ❌ | ✅ | ❌ | — |
| 0.35–0.40 | 2 | −0.0054 | +0.0000 | +0.0073 | +0.0197 | +0.0054 | ❌ | ✅ | ✅ | — |
| 0.45–0.50 | 2 | −0.0036 | −0.0021 | +0.0073 | +0.0145 | +0.0040 | ❌ | ✅ | ✅ | — |
| **0.55–0.60** | 2 | **+0.0189** | **+0.0025** | **+0.0208** | **+0.0128** | **+0.0137** | ✅ | ✅ | ✅ | **✅达标** |
| 0.65–0.70 | 2 | +0.0357 | −0.0183 | +0.0056 | +0.0069 | +0.0074 | ✅ | ❌ | ✅ | — |
| 0.75–0.80 | 2 | +0.0432 | −0.0304 | −0.0107 | −0.0224 | −0.0051 | ✅ | ❌ | ❌ | — |
| 0.85–0.90 | 2 | +0.0511 | −0.0460 | −0.0238 | −0.0245 | −0.0108 | ✅ | ❌ | ❌ | — |
| 0.95–1.00 | 2 | +0.0592 | −0.0523 | −0.0207 | −0.0314 | −0.0113 | ✅ | ❌ | ❌ | — |

**信号 = `df`（s3：query 词项的索引侧词典覆盖率）**

| θ 区间 | 点数 | paraphrase | exact | natural | mixed | 全局 | G1 | G2 | G3 | 判定 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0.00–0.65 | 14 | +0.0000 | +0.0000 | +0.0000 | +0.0000 | +0.0000 | ❌ | ✅ | ✅ | — |
| 0.70–0.80 | 3 | +0.0003 | +0.0000 | +0.0000 | +0.0000 | +0.0001 | ❌ | ✅ | ✅ | — |
| 0.85 | 1 | +0.0003 | −0.0063 | +0.0000 | +0.0107 | +0.0012 | ❌ | ❌ | ✅ | — |
| 0.90–1.00 | 3 | +0.0003 | −0.0063 | +0.0021 | +0.0107 | +0.0017 | ❌ | ❌ | ✅ | — |

**信号 = `shape`（s4：`has_ascii` 的 0/1）**

| θ 区间 | 点数 | paraphrase | exact | natural | mixed | 全局 | G1 | G2 | G3 | 判定 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0.00 | 1 | +0.0000 | +0.0000 | +0.0000 | +0.0000 | +0.0000 | ❌ | ✅ | ✅ | — |
| 0.05–1.00 | 20 | **+0.0592** | **−0.0523** | **−0.0207** | +0.0000 | **−0.0034** | ✅ | ❌ | ❌ | — |

#### 4.13.4 判定（§4.5 的五门合取）

| 信号 | G1（paraphrase ≥ +0.01） | G2（三桶各 ≥ −0.0024） | G3（全局 ≥ 0） | **G4（≥3 连续点）** | **结论** |
| --- | --- | --- | --- | --- | --- |
| `overlap` | 部分点过（θ ≥ 0.55 的 8 点中有 4 点） | 部分点过 | 部分点过 | ❌ **最长连续 2 点**（θ ∈ [0.55, 0.60]，宽度 0.05） | ❌ **不投** |
| `df` | ❌ **无任何点**（最大 +0.0003） | — | — | ❌ 无任何点 | ❌ **不投** |
| `shape` | ✅ 20 点全过（+0.0592） | ❌ 20 点全不过（exact −0.0523 / natural −0.0207） | ❌ 20 点全不过（−0.0034） | ❌ 无任何点 | ❌ **不投** |

**⇒ 三信号皆判「不投」**（G4 是共因：`overlap` 差一点、另两个连单点都到不了）。

#### 4.13.5 三条可核对的解释（把「不投」说到机制层）

1. 🔑 **`shape` 的机制是有效的，挡路的是「降权取形」而非信号本身**（**逐 query 严格验证**，非推断）：
   `θ = 0.50` 臂的 320 条 query **全部落且只落在两个规范态** ——
   **240 条（exact/natural/paraphrase 各 80）逐 query `mrr` ≡ vector 单路**（桶均值差 **`+0.000e+00`**），
   **80 条（mixed）逐 query ≡ baseline**（桶均值差 **`+0.000000`**），
   **「两者都不等」的条数 = `0`** ⇒ 触发集**恰为 240 条非 ASCII query（75%）**、零例外。
   ⇒ 规则的**query 条件性**成立（不是全局改权重）。⚠️ 但**「一刀降 0」把 exact/natural 一起砍了**
   （桶差 `−0.0523` / `−0.0207`）⇒ G2/G3 双破。设计 §4.2 曾把**档位 `{1.0, 0.5, 0.0}`** 列为可选取形，
   而 PR9-1 定形为**只实现「降到 0」**（§4.10 ⑤）⇒ **档位形态未实现 ⇒ 未扫**
   （见 4.13.7 的边界登记与 4.13.6 的复审触发条件①）。
2. 🔑 **`overlap` 的「2 点」是量子化 + 预注册网格的算术结果，不是判据坏掉**：
   `overlap = |两路 top-10 交| / k` ⇒ 取值是 `1/10` 的整数倍（`{0, 0.1, …, 1.0}`）；
   θ 网格步长 `0.05` **恰好是信号量子的 1/2** ⇒ **每个信号取值对应一个等价类、恰占 2 个网格点**。
   ⇒ G4（≥3 连续点）**要求两个相邻等价类同时通过**；本数据点只有 `0.5` 那一类通过
   ⇒ 最长连续 **2 < 3** ⇒ G4 不成立。（若 θ 网格更密或 `k` 更大，「一个类」会更宽 ⇒ 该结构性注记随口径变。）
3. 🔑 **`df` 在本语料上「惰性」**：12K 中文语料 + jieba 词项下，query 去重词项的词典覆盖率
   **几乎恒为 1.0** ⇒ 只有 `θ ≥ 0.70` 才捞到极少数 query，且 paraphrase 的 Δ 最大仅 `+0.0003`
   （远低于 G1 的 `+0.01`）⇒ 该信号**在本评测集上没有区分力**（不是「方向错」，是「量太小」）。

#### 4.13.6 复审触发条件（`NFR-15` 保持「打开」时一并写明）

1. **换降权取形**（`{1.0, 0.5, 0.0}` 档位、或 `w_bm25 ← w_bm25 · (1 − s/θ)` 之类的单参数映射）
   —— 由 4.13.5 ① 的证据支持（信号有效、取形太糙）；实现须新开 PR + 走同样五门。
2. **换信号定义**（例如把 `overlap` 与 `df` 组合成单参数复合量、或改用「BM25 top-k 内部分布」s2）
   —— 注意 **D-S9-08** 只允许「零参数 / 单阈值」形态，复合信号须先改设计再扫。
3. **换 `k`**（`overlap` 的量子 = `1/k`，`k` 变大 ⇒ 每个等价类更宽 ⇒ G4 的通过宽度条件更易满足；
   但**判据的 `k` 一变，§1.3 的 S9-1a~d 全部要重测**，含 baseline）。
4. **换语料 / 桶占比**（`df` 的惰性、`overlap` 的类分布都与语料强相关）。
5. ⚠️ **不得**用「按桶标签调权」去凑（那正是 **R55** 的标签泄漏，§D-S9-02 已禁）。

#### 4.13.7 覆盖边界（如实登记）

- **降权取形只扫了「降到 0」一种**（PR9-1 的实现取形，§4.10 ⑤）⇒ 4.13.5 ① 的结论**不能**读成「自适应融合整体无效」。
- **触发集合只在 `shape` 上可「零例外」指认**：`bench` 的 JSON **不记录 `FusionCtx` /
  `Metrics.fusion_weights`** ⇒ 触发与否只能**由结果反推**。`shape` 恰好可反推（规则是二值 + 参照臂
  正交）：240 条 ≡ vector、80 条 ≡ baseline、**无第三态**（4.13.5 ① 的表）。
  ⚠️ 但 `overlap` / `df` 是**多值信号**，同一 `θ` 下不同 query 的触发性无法由聚合 Δ 区分
  （「按结果分类」会把「触发但 BM25 无贡献」与「未触发」混为一谈）⇒ 这两个信号的**逐 query 触发集合
  没有产物可指认**。⇒ 若复审要按触发率分桶，需先给 `bench` 增记录（属**新面**，须单列报评审）。
- **臂级 JSON 未持久化**（65 份，体积大、不入库）：入库的持久产物**只有 TSV**（`data/eval/s9-2026-09-25/readings.tsv`，
  头含冻结图指纹与两个 Q-R1 口径）；要逐臂原始 JSON 就重跑脚本（`${RUN_DIR}` 路径在脚本输出末尾打印，
  由系统清理临时目录）。⇒ 「可指到产物」这条对**读数**成立（TSV），对**臂级原始工件**不成立。
- **只测了 MRR@10(1) 的 Δ**（判据口径）；NDCG/Recall 未逐点判定（脚本已落 TSV，按需可扩展）。
- **单机单次配置**：效果类读数不依赖机器，但**语料固定**（12K / 320 query）⇒ 结论不外推（`eval-report.md` §9 的既有纪律）。

### 4.14 PR9-2 第 1 轮评审响应（2026-09-25；本文 v0.7 → **v0.8**）

> 评审（`pulls/73/reviews` `5316771251`，基线 `30b61b9`，独立验证型复审 + 7 条行内）=
> **✅ 通过 —— 无阻塞项、达到可合并形态（0×P1 + 0×P2 + 3×P3 + 4×P4）**；**「不投」的结论被独立复算成立**
> （评审自己重跑 4 条关键臂 + 按 ASCII 归属**逐 query** 复核 `shape` 的 240/80 零例外，全部与 §4.13 逐位吻合），
> 且**不要求复跑守门链**（本轮零 `.rs` 改动）。本节逐条处置。

#### 逐条处置

| # | 评审意见 | 处置 | 落点 |
| --- | --- | --- | --- |
| **P3-1** | `eval_s9.sh:241` Q-R1 复核行**标签与公式口径不一致**：标签写「paraphrase 桶」，公式算的是**全量**（实测 **−0.0113**）⇒ 复跑者会在桶的标签下读到负数、以为「Q-R1 没复现」（判定本身不受影响） | ✅ 采纳（并按评审口径①做到**两个口径都打印**）：公式改为 `para_qs` 的**桶口径**（**+0.0592**），另起一行给**全量参考值**并注明「**不可与本行互换**」；`> 0 ⇒ …` 改为中性表述「期望方向 = **桶值为正**」。**两个值都写进 TSV 头**（产物自带，不再只活在 stdout） | `eval_s9.sh` 配对段 + TSV 头 |
| **P3-2** | `:201` G5「逐臂自证」**漏核 `signal` / `theta`** —— 而 JSON 里记这两个字段**正是为了**「21 个 θ 文件各自自证跑的哪一档」（§4.10 ⑥）⇒ 传参错会**静默评估同一条曲线三遍** | ✅ `assert_g5` 增 `sig` / `t` 形参 + 两条断言；调用点传入**本臂身份**。⚠️ **θ 容差取 `1e-6`**（评审实测：JSON 里 θ 是 f32 噪声 `0.550000011920929`，用 `1e-9` 会把每个臂都误判成失败）—— 已写进函数文档。**变异实测** M1 / M2（见下表） | `eval_s9.sh` `assert_g5` + 调用点 |
| **P3-3** | 读数**不可追溯到持久产物**：`RUN_DIR = mktemp -d` 已清，全机搜不到 `readings.tsv` ⇒ §4.13.3 说的「21 点全表见脚本产物」**无处可指** | ✅ 采纳评审口径①：脚本加 `OUT_DIR="${OUT_DIR:-}"`（缺省仍 `mktemp`；给了就**跑完并判定成功之后**复制 TSV 过去 ⇒ 落盘的不会是半成品）+ **本轮 TSV 入库** `data/eval/s9-2026-09-25/readings.tsv`（含 README 说明口径）；§4.13.1 / §4.13.3 / §4.13.7 三处同步（含**如实登记「臂级 JSON 未持久化」**） | `eval_s9.sh` + `data/eval/s9-2026-09-25/` + §4.13 |
| **P4-1** | `CHANGELOG.md:18`「**64 臂**（三路锚点 1 + 基线 1 + 3 信号 × 21 θ）」——括号自己加起来是 **65**（PR 正文同写 64） | ✅ 按**实测 `bench` 调用数**统一为 **65 次调用**（评审给了两条路，见「与评审不同之处」③），并在 §4.13.1 的「臂」行**补出总数**（三个载体同口径，防再歧义） | CHANGELOG + PR 正文 + §4.13.1 |
| **P4-2** | S9-1b 的两个均值**硬编码 `/ 80`**（换 `QUERIES=` 后静默错 80/n 倍）；「四桶 n=80」只**打印**不断言；空桶 `Δ = 0.0` 会让 **G2 对该桶恒真** | ✅ 三处都改：① 分母改**实际桶大小** `len(para_qs)`；② 桶结构升级为**判据**（三条独立检查 + 独立退出码 **6/7/8**：桶数之和须 == query 总数 / 无空桶 / 四桶等分）；③ 空桶 ⇒ **`sys.exit`**（不再当 0 分放行）。**变异实测** M4（三臂） | `eval_s9.sh` 配对段 |
| **P4-3** | ① 头部**状态行**未随 v0.7（未提 PR9-2 / spike 结论）；② §7 的 **S9-T11 行**未注记「已由脚本承载、结论见 §4.13」；③ §7 的 **S9-T6 行**「仍写两个且**无注记**」 | ✅ ①② **属实、已改**（状态行并补 §4.11/§4.12/§4.13/§4.14 四条线；S9-T11 行注记「载体 = 脚本 + 已出结论」并保留原口径引文）。⚠️ ③ **前提与实物不符** —— 该注记在 `30b61b9` 上**已存在**（`06e8432` 引入）；仍按「不可被扫读误判」重排该格，见「与评审不同之处」① | 文首状态行 / §7 两行 |
| **P4-4** | PR 正文「守门链」**标题重复出现两次** | ✅ 删掉占位标题、只留一个；正文整篇重发（顺带把本轮新增结论并入） | PR 正文 |
| （无编号） | 正文 §三·1：**「内置自检」覆盖面比听起来窄**（`zero_seen` 是全局标志 ⇒ 三个信号里**只有第一个**被自检；抓不到「某信号传参错」） | ✅ **不从文档上补限定，直接修实现**：删掉全局标志、**三个信号的 `θ=0.00` 臂都自检**（代价可忽略）；脚本头注与 §4.13.1 同步；并写明它与 G5 的覆盖面**互补**（自检抓「配对逻辑错」、G5 抓「传参错」）。**变异实测** M3（双臂：新版红 / 旧版绿） | `eval_s9.sh` 自检段 + 头注 + §4.13.1 |
| （无编号） | 正文 §三·3 的**正名**：`--no-latency` 在本 PR 是**正确用法**（跳性能段、保留效果段） | ✅ 照收；并在脚本协议注释里**写明这一点**，防后人按教训库里「`--no-latency` 连带跳过 B2」的旧条目误改（那条针对**延迟**判据） | `eval_s9.sh` 头注 |

#### 变异 / 独立验证表（脚本类改动；评审要求「至少在本机跑一次」）

| 变异 / 复算 | 手法 | 结果 |
| --- | --- | --- |
| **M1**（P3-2 的 `signal` 断言 · **真接线注入**） | bash 循环里的 `--adaptive-fusion-signal "$sig"` 改成固定 `overlap`（`SIGNALS="df"` 跑） | ✅ **G5 红**：`signal=overlap（本臂应为 df）`，`exit 3` |
| **M2**（P3-2 的 `theta` 断言 · **真接线注入**） | `--adaptive-fusion-theta "$t"` 改成固定 `0.50` | ✅ **G5 红**：θ 不符（`exit 3`） |
| **M3**（自检覆盖面 · **双臂**） | 在配对式里对 `shape` 单独注入错参照（`arm − vector` 而非 `arm − baseline`）；**抽取内嵌 python 段**用真产物 JSON 跑 | ✅ **新版（每信号自检）红**（`exit 5`）；**旧版（全局标志）绿** ⇒ 证明这正是被点到的覆盖缺口，非空谈 |
| **M4**（P4-2 的桶结构判据 · **三臂**，同一抽取块 + 手工改 baseline JSON） | ① 一条 query 的桶标签改 `bogus`；② 删 1 条 paraphrase；③ 删全部 paraphrase | ✅ 分别 `exit 6 / 8 / 7`（三条检查**各自**可红，非「一条大断言」） |
| **M5**（P3-1 的口径 · 正向） | 跑后读 stdout | ✅ 桶口径 **+0.0592**、全量 **−0.0113** 两行都在；与 §4.13.2 的桶值一致 |
| **复算（采信前先自算）** | 本轮第 3 次全量复跑（为产出持久 TSV 而跑） | ✅ 63 行曲线与前两轮**逐位相同**（§4.13.1 的确定性行随之更新为「三轮」） |

#### 与评审不同之处（三条，都不改结论）

1. **P4-3 ③ 的前提不成立（该注记已存在）**。评审写「§7 的 S9-T6 行仍写『**两个** provided 方法』**且无注记**」。
   实测：该行**有注记**（`⚠️ 实现期（§4.10 ②）增至**三个**…`），且在评审自己核对的那个提交 `30b61b9` 上就是如此
   —— `git log -S '实现期（§4.10 ②）增至**三个**'` 显示由 **`06e8432`**（PR #72 的 squash 合并，2026-09-25T09:44:39Z）引入；
   #72 第 2 轮响应评论（`5830070596`）也写了这一条。⇒ **该处无需修的「缺漏」不存在**。
   **但仍做了改动**：把该格**重排为「结论前置」**（先写「本条现覆盖三个」，再把「两个」明确标成 **P3-1 轮次的当时口径**）
   —— 评审连续两轮把它读成「没注记」，说明**原措辞的扫读性不够**，这属于真问题（与「被推翻的旧值要留引文」不冲突：
   引文保留，只是不再占据「结论位」）。
2. **P3-2 的容差数值直接采用评审实测值**（`1e-6`），并把它写进函数文档 + 注释 —— 这条我一并复算了：本轮产物里
   θ 字段确实是 f32 舍入后的十进制（如 `0.550000011920929`）；`1e-9` 会全臂误报，评审的判断**成立**。
3. **P4-1 选「65」而非「去掉『三路锚点 1』」**：理由 = 与脚本**实际执行**一致（评审也按 65 次的子集重跑了 4 次），
   而「把锚点叫作参照不算臂」会让「臂」这个词在两个 PR 间口径漂移。并**在 §4.13.1 补出总数** ⇒ 三个载体同口径。

#### 未闭环 / 盲区

- **臂级原始 JSON 未入库**（只入 TSV）：本轮选的是评审口径①（最省事那一支）⇒ 「读数可指到产物」成立，
  但「**逐臂 JSON 可指到**」不成立（§4.13.7 已如实登记）。若要连 JSON 也留证，需新开一个面（属 PR9-4 或独立项）。
- **`eval-report.md` §8.16 与四处定义面回写**仍不在本 PR（归 **PR9-4**）；本 PR 只把「读数与结论」的载体补成可持久指认。
- ⚠️ 本节的 M1 / M2 是**真接线注入**（改 bash 后真跑），M3 / M4 是**抽取内嵌 python 段 + 真产物 JSON** 的方式
  （不重跑 65 臂 —— 抽取时每次从被测文件重新取，避免「副本变异假过」那一类坑）。

### 4.15 PR9-2 第 2 轮评审响应（2026-09-25；本文 v0.8 → **v0.9**）

> 评审（`pulls/73/reviews` `5319498739`，基线 `dc6bfa5`，**响应验证型复审** + 3 条行内）=
> **✅ 复审通过 —— 无阻塞项、达到可合并形态；本轮 0×新意见**。第 1 轮的 7 条 + 正文两条非编号意见
> **逐条被独立验证为落地**，且评审明确「**不采信响应自述**」（用新配对段跑它自己上一轮重跑的
> 4 份臂 JSON 来验）。3 条行内里 **2 条是验证记录**（非意见）、**1 条是对 §4.14「与评审不同之处①」
> 的认错回帖** ⇒ **无新意见需要处置**，本节只做记录与复现复核。

#### 4.15.1 评审自查：它第 1 轮 P4-3 ③ 的前提确实不成立（已由评审方确认）

评审单列「评审自查」承认：该注记在 **`30b61b9` 上已存在**（由 `06e8432` 引入），而它核「无注记」
是在 **`d3ebf52`**（#72 第 2 轮响应**之前**的 head）—— **当时属实、现已过时**；
根因 = **跨轮基线前进后，把上一轮的「遗留清单」原样平移、未在新基线逐条重核**
（并已写进其评审技能：响应验证轮开工第一步 = 在**新基线**上复核上一轮全部遗留项）。
⇒ **§4.14「与评审不同之处①」成立，本文该条不再悬置**。

#### 4.15.2 评审的两条独立验证：本响应轮**用我方自己的产物复现**

| 评审的验证记录 | 我的手复现（方法 = **抽取内嵌 python 段** + **我自己**第 3 轮的 65 份臂 JSON） | 结果 |
| --- | --- | --- |
| 入库 TSV ↔ 它上一轮重跑 JSON 的**全精度对账** | 从 JSON 按 `qid` 配对复算 `shape θ=0.50` / `overlap θ=0.55` 各 5 行，与 TSV 按**存储精度**（10 位小数）比 | ✅ **10/10 逐位一致** |
| 新配对段的**四向测试** | 正向 2（`shape θ=0.50` / `overlap θ=0.55` ⇒ **exit 0**）+ 负向 2（`signal` 错标 `df` / `theta` 错标 `0.9` ⇒ **exit 3**，消息精确到「本臂应为 X」与「容差 1e-6」） | ✅ **四向全部按预期** |

⇒ 与响应方 3 轮全量复跑合起来，同一条结论（判「不投」）现有**三条独立证据互相咬合**（评审的原话）。
⚠️ **方法边界如实登记**：本条复现是**判据级 + 产物级**（抽内嵌段跑真产物），**不是**端到端的又一次 65 臂重跑。

#### 4.15.3 臂级 JSON 是否入库：本 PR 的处置口径（评审征询作者拍板）

评审在「未闭环」里把它挂给作者（其意见 = **不必**）。**本轮决策：不扩面**，理由两条：

1. **可复算性已足够** —— TSV + 冻结图指纹（sha256 前 16）覆盖复算所需的全部信息（本条已被评审的
   TSV 对账**实证**，不是我方的推断）；
2. **成本不对称** —— 臂级 JSON 侧 ≈ 65 份、体积大，且**重跑脚本即可重建**（`OUT_DIR=` 已固化）。

⚠️ 但要分清：若将来要「按触发率分桶」（§4.13.7 的盲区），需要的是 **`bench` 增记录**（属**新面**），
这与「把 JSON 入库」是**两件不同的事** —— 前者要单独立项、单列报评审。

#### 4.15.4 本响应轮的改动面（极小）

- 本文 **§4.15**（本节）+ 文首三行升 **v0.9**；CHANGELOG 一条。
- **零 `.rs` 改动、零脚本改动** ⇒ §4.13 的读数、§4.14 的处置结论**均不变**；评审明示的
  「不要求复跑守门链」继续适用。本轮复验 = `mdcheck` + `make shell` + `bash -n` + §4.15.2 表的 5 项复现。
- 🔑 **一次自检脚本的自造 bug 如实登记**（与 #62 同族）：复现脚本第一遍把 `run_block()` 的返回值
  （`(rc, out)` 元组）**当成 `rc` 去比 `0`** ⇒ 两条正向被报成「🔴」（而它自己打印的 `exit=0` 是对的）。
  ⇒ 判据：「**逐条打印全绿、结论却报红 ⇒ 先怀疑结论的聚合逻辑**」，不要先去改被测对象。

### 4.16 PR9-3 实现期定形记录（2026-09-26；本文 v0.9 → **v0.10**）

> 本 PR = **S9-06**（T7-16 / T7-15）。前置：**R58 已由用户 2026-09-26 拍板批准**（语料外发走
> `D-S9-07` 的外部 LLM API 通道）。本节记录实现期定形 + **两处偏差登记** + T7-15 的读数与解读边界。

#### 4.16.1 R58 拍板记录，与「真实资产未在本 PR 生成」的处置

| 项 | 记录 |
| --- | --- |
| 语料外发 | **用户 2026-09-26 拍板：批准**（按 `D-S9-07`：provider-agnostic 三环境变量 + OpenAI 兼容 `POST /chat/completions`） |
| 显式开关 | 生成器**每次运行**仍要求 `HELIX_LLM_ALLOW_EGRESS=1`，且**先打印**「将外发多少条 / 端点 host / 字符量级」——「拍板」是一次性的，但「外发」这件事应在每次运行时**可见**，不靠记忆 |
| 本 PR 是否真跑 | **否**：本机无 `HELIX_LLM_*` 三环境变量（真跑需可用端点）。⇒ 本 PR 交付**通道 + 生成器 + 离线桩（S9-T12）+ 资产 schema**；**真实资产待端点就绪后由同一脚本产出**（落 `data/eval/t7-16/`） |
| 连带边界 | 因此 **T7-16 的对照读数在本 PR 为空**（无资产 ⇒ 无读数）；PR9-4 写 §8.16 时须**分列**「T7-15 有读数 / T7-16 待产出」 |

#### 4.16.2 Q9-4 定形：弱标注 = 「多轮 + **独立**自查」

- **轮 1（生成）**：给 K 篇候选段落（`--per-batch`，默认 4）⇒ 要求为**每一篇**写一条「用户会向检索系统提出的」中文 query。
- **轮 2（自查 / 弱标注）**：回灌「轮 1 的 query + 该批**全部**候选段落」⇒ **独立**判定每段的 `grade ∈ 0..3`。
- **一致性判定（自查的牙齿）**：轮 2 给**生成源段**的 grade **≥ 1** 才保留；否则**丢弃**该条。通过率写进元信息（可报告的质量指标）。
- 🔑 **为什么这样定形**（而不是「把生成源段直接写成 grade 3」）：后者把「源段必相关」当成**公理**
  ⇒ 模型只要生成一条**跑题**的 query，它就会被当成正例收进资产，而弱标注**没有任何拒绝机制**。
  独立一轮判定后，「生成质量」这件事**可以被判负** —— 本 PR 的 self-test 用「源段被判 grade 0」
  这条**负例**验证拒绝路径真的生效（不是纸面规则）。

#### 4.16.3 生成器载体：`scripts/gen_t7_16.py`（Python 标准库）+ **两处偏差登记**

- **载体选择的三条理由**：① 评测资产工具在本仓的载体是**脚本 / example**（`scripts/eval_s9.sh`、
  `crates/core/examples/t2_prep.rs`）；② 放进产品 crate 会**扩大产品面** —— 还要新增**直接** HTTP 依赖
  ⇒ 牵动 `deny.toml` / MSRV / CI 依赖面，而 `hf-hub → fastembed` 里那份 `reqwest` 只是**传递**依赖、
  不该被产品代码直接消费；③ 与 PR9-2 已确立的「**臂级离线分析属评测资产而非产品代码**」一致。
  **通道实现 = `urllib`（标准库）⇒ 零新增依赖。**
- 🔴 **偏差 1（载体形态）**：设计 §7 把 **S9-T12** 列在「测试计划（`S9-T1 ~ S9-T12`）」里，读起来像一条
  **Rust 用例**；实际载体 = 脚本的 `--self-test`（**28 项断言**：解析 / 落盘 / 配对 + 7 个负例）。
  ⇒ 已在 §7 该行**补注载体**（不改写测试计划的编号体系，只补「载体」这一列的事实）。
- 🔴 **偏差 2（CI 落点）**：新增 CI job **`eval-assets`**（job 数 **10 → 11**）。它与 `ci.yml` 头部既有的
  「**评测脚本故意不进 CI**」**不冲突**：那条政策针对**读数类**（需 12K 段落 embed ≈230 s；毫秒级 P99
  数字在共享 runner 上不可引用），而本 job 跑的是**零网络、零模型、毫秒级**的纯逻辑自检。
  ⇒ 已在 `ci.yml` 头部**与该 job 的注释里**写明这个区分（否则读政策的人会以为它违规）。

#### 4.16.4 资产 schema，与 `type: "agent"` 这个决定

- `agent-queries.jsonl` 与 `data/t2-queries.jsonl` **同 schema**：`{"qid","query","type","relevance":[{"source","grade"}]}`，
  `grade ∈ 0..3`（`bench::load_judgments` 的硬校验；越界即报错）。
- 🔴 **`type` 固定 `"agent"`，不冒充四桶**：`helix bench` 的 JSON 分桶表是**固定四桶**
  （`mode_to_json` 里写死 `["mixed","natural","exact","paraphrase"]`）⇒ 本资产的**四桶行恒 `n = 0`**；
  对照读数取**全局行** + `per_query` 的逐 `qid` 配对。⚠️ **后果直说**：不要拿它的分桶行做任何比较
  —— 那是 `0`（「没有这类 query」），不是「降为 0」。
- 🔑 **候选池必须取自既有语料**（这是**约束**，不是巧合）：`bench::validate_sources` 要求每条 `source`
  都能在**被检索的那份索引**里找到 ⇒ T7-16 的 query 只能「针对既有语料里的段落」生成。
  【顺带解释了 R58 的实质：**外发的就是这些段落的正文**。】

#### 4.16.5 T7-15 的资产、口径与**解读边界**

- 对照语料 = `data/t2-corpus.jsonl` 中 `source ∈ 320 条 query 的全部 qrels 段落`的行
  ⇒ **12000 → 5042 篇**（含 grade 0 的已判定负例 —— 与 `t2_prep` 的「池内零假负例」同口径）；
  脚本内**自证**「qrels 集合内每个 source 都在语料里」（缺失 **0**，缺失即报错）。
- 建库口径与主图**逐项相同**（`--vectors --single-chunk`）；两臂**只换 `--index`**。
- 🔴 **解读边界（必须与读数同写）**：这一换**同时**改变 ①假负例的多少 与 ②**语料规模 / 统计量**
  （BM25 的 `idf`、HNSW 图都变）⇒ Δ 是**合成效应**，**不能**读成「纯假负例效应」。
- 🔑 **一个比全局均值更有信息量的读法**：逐 query 配对里 **280 / 320（87.5%）完全不变**、中位 Δ = `0`
  ⇒ 说明**大部分 query 的名次根本没受到语料裁剪的影响**（被删掉的随机负例段落大多不在 top-10 内）。

#### 4.16.6 T7-15 读数（2026-09-26，本机；产出 = `data/eval/t7-15/readings.tsv`）

**MRR@10(1)：两臂 × 三模式**（同一批 320 条 query，只有 `--index` 不同）

| mode | 主基线（12000 篇） | 对照（5042 篇） | Δ（对照 − 主） |
| --- | --- | --- | --- |
| bm25 | 0.6495 | 0.6657 | **+0.0162** |
| vector | 0.6828 | 0.6862 | **+0.0033** |
| hybrid | 0.6941 | 0.6972 | **+0.0031** |

**hybrid 的逐桶对照**

| 桶 | 主基线 | 对照 | Δ |
| --- | --- | --- | --- |
| exact | 0.8219 | 0.8073 | **−0.0146** |
| natural | 0.7691 | 0.7884 | **+0.0194** |
| mixed | 0.7052 | 0.6990 | **−0.0062** |
| paraphrase | 0.4803 | 0.4942 | **+0.0139** |

**hybrid 逐 query 配对分布（n = 320）**：均值 **+0.0031** ｜ 中位 **+0.0000** ｜
min **−0.5000** ｜ max **+0.6667** ｜ 变差 **17** / 变好 **23** / **不变 280**。

⇒ **读法**：方向与「随机负例含假负例 ⇒ 拉低主基线」的预期一致（`bm25` 最明显 `+0.0162`），
但**幅度小**（hybrid 全局 `+0.0031`）且**桶间方向不一致**（`exact`/`mixed` 略降、`natural`/`paraphrase` 略升）
⇒ 与 4.16.5 的「合成效应」读法自洽：**这不是一个可以用单一机制解释的位移**。
本对照**只作对照**，对 `NFR-15` 的主判据**无影响**（主判据建立在既有 320 条 + 主基线之上）。

#### 4.16.7 覆盖边界（如实登记）

- **真实 LLM 生成未跑**（无端点 ⇒ 4.16.1）；`--stub` / `--self-test` 路径已覆盖（28 项断言，含 7 个负例）。
- **T7-16 无对照读数**（无资产）⇒ §8.16 只能记「待产出」（4.16.1 的连带边界）。
- T7-15 的 Δ 是**合成效应**（4.16.5）；**语料固定**（本仓 12K / 320 query）⇒ 结论不外推（`eval-report.md` §9）。
- `bench` 的**四桶**对本资产恒 `n = 0`（4.16.4）⇒ 本 PR **未新增可用的分桶对照**。
- 本轮**零 `.rs` 改动** ⇒ 守门链的**用例计数应与 PR9-2 终态逐项相同**（这正是「零代码影响」的可核对判据）。

## 5. 决策记录（D-S9-01 ~ D-S9-10）

见文首「决策速览」。需**评审拍板**的三条：

1. **D-S9-02**（信号禁含标注）+ 由此对 **FR-35 原文措辞**的更正（§6.3）—— 这是本步**唯一**触碰需求措辞的地方。
2. **D-S9-08 / G4**（**不做网格搜索 + 单点达标不算达标**）—— 它把「判据怎么才算过」的严格度显著提高（后果：**更可能判「不投」**）。
3. **D-S9-04**（精排默认关为判据面）—— 若评审认为判据应在**精排开**的口径下定值，则 §4.5 的协议与 `k` / `candidate_k` 全部要重算（⚠️ **这会改变 `candidate_k`**：精排开 ⇒ `window = R = 20` ⇒ `candidate_k = max(30, 20)` 仍 30，但**交付条数**与名次会变 ⇒ 必须重测 baseline）。

---

## 6. 影响面与兼容性

### 6.1 兼容性

| 变更 | 是否破坏性 | 依据 |
| --- | --- | --- |
| `FusionStrategy` 加 **provided 方法** | **否** | 默认实现 ⇒ 既有实现一行不改（同 `Reranker::candidate_window` 先例） |
| 新增 `FusionCtx` / `LaneStats` 类型 | **否** | 纯新增 |
| `Config` 加 `adaptive_fusion`（默认 `false`） | **否** | `Config` **不是外部可达类型**（未从 `helix_core::search` 再导出 —— `S8-08` 已核）；且默认关 ⇒ 零行为变化 |
| `Metrics` 加字段（可观测性） | ⚠️ **按字面是**（`Metrics` 是 `pub`、字段全 `pub`、**无** `#[non_exhaustive]`，`metrics.rs:43-44`） | **先例**：Step 7 已加 `rerank_window` / `rerank_elapsed`（`metrics.rs:101` / `:119`）；且 `Metrics: Default` ⇒ 影响面小。**评审可要求改为不扩 `Metrics`**（改走 `Explain`） |
| CLI / `bench` | **否**（本步不改） | D-S9-09 |

### 6.2 `Metrics` 新增字段（提案，待评审）

- `fusion_weights: Option<Vec<f32>>`（**仅**在自适应生效时 `Some`）—— 「本次用了哪组权重」；
- `fusion_signal: Option<f32>`（本次算出的信号值）—— 「为什么这么选」。
  ⚠️ 二者都**只读**且默认 `None` ⇒ 关闭开关时与今天同形（除结构体多两个字段）。
- 🔴 **填充路径**（第 3 轮评审 **P4-9**，本表原缺）：开关开时由**编排层另调一次**
  `weights_override(&ctx)` 专用于填这两个字段（**完整论证见 §4.1 规则 3**）；
  ⚠️ 没有这条规范时，`S9-T7` ①（断言 `fusion_weights == Some([0.0, w])`）与 `S9-T10`（「开时写对」）
  **都要求编排层写出它们，但没人说值从哪来** ⇒ PR9-1 会现场发明口径。

### 6.3 对 **FR-35** 措辞的更正（**本文提出，需评审拍板**）

- 原文（`requirements-spec.md` §附-1 FR 表 **FR-35** 行，**v1.24 时点 `:309`**；`plan-v2.md` §5 需求编号草案 **FR-35** 行，**v0.26 时点 `:617`**）：「融合权重**按桶**（paraphrase / exact / 长尾）自适应」。
- 建议改为：「融合权重**按 query 侧可观测特征**自适应（**不得依赖相关性标注**）；『桶』仅用于**评测分组**」。
- 理由：**N1**（桶含标注 ⇒ 生产不可用 + 标签泄漏）。
- ⚠️ **保留原措辞引文**（本项目纪律：被推翻的结论不要静默删除）。

### 6.4 需回写的**架构漂移**（**N2**）

`architecture-design.md` **§5.6**（旧伪码位于 **v1.22 时点 `:492-501`**）的 trait 伪码与实现不一致（`weights` 形参 / `ScoredHit` 类型名 / 缺 `k` 形参）⇒ 本轮**按实现更正**，并把新增的 **三个** provided 方法（`weights_override` + `fuse_adaptive` + `fusion_signal`——第三个是 §4.10 ② 的实现期补齐，**信号值的上报通道**）一并写进该节（**原伪码全文引文见 §2.4 N2**）。⚠️ 回写时按**附录 A** 为准（API 一览已含三行）。

---

## 7. 测试计划（S9-T1 ~ S9-T12）

| 编号 | 名称 | 进 CI | 说明 |
| --- | --- | --- | --- |
| **S9-T1** | 关闭开关 ⇒ **逐位一致**（`hits` 的 `(chunk_id, score)` 序列） | ✅ | 与既有 `S8-08` 的零行为变化判据同形；**必须**含「今天的行为」作为期望值来源（用 `RrfFusion::default()` 现算，不硬编码） |
| **S9-T2** | **空 lane 的槽位仍在**、权重仍对齐（I9-2） | ✅ | 构造「BM25 空 / vector 非空」与反向两臂；断言权重**没有串到另一路**（用可区分两路的合成输入） |
| **S9-T3** | **I9-1 的类型级/源码级自检** | ✅ | 断言 `FusionCtx` 的字段集合 = 白名单（编译期常量 + 单测）；并在仓库内**反扫**「`fusion` 模块依赖 `qrels` / `relevance` / `bucket`」= 0 命中（⚠️ 对照样本先自证） |
| **S9-T4** | 自适应**不改** `candidate_k` / `Metrics.candidates`（I9-4） | ✅ | 开关两态对同一 query 比 `candidate_k` 与 `candidates` |
| **S9-T5** | 精排一字不动（I9-6） | ✅ | `Reranker::candidate_window` 的默认值与 `R` 联动回归（复用既有用例即可，**新增**一条「fusion 开关与窗口正交」） |
| **S9-T6** | `weights_override` 返回 `None` ⇒ `fuse_adaptive` 与 `fuse` 直调**同结果** | ✅ | ⚠️ **本条现覆盖三个 provided 方法**（实现期 §4.10 ② 补 `fusion_signal` 上报通道）。下文出现的「**两个**」是 **P3-1 评审轮次的当时口径**（当时本条钉住「决策通道」+「应用通道」两条）；第三个的「默认 `None`」已由 `S9_T6_默认实现两通道与fuse直调逐位一致` 的 `assert_eq!(s.fusion_signal(&ctx), None)` 覆盖。（v0.8 重排本格：结论前置以免被扫读成「仍是两个」，见 §4.14 的「与评审不同之处」①） |
| **S9-T7** | tier 1：`len == 0` ⇒ **可观测面**报权重 0、且结果 ≡ 单路 | ✅ | 合成 lane。⚠️ **断言必须落在可观测面**（**P4-4**）：① `Metrics.fusion_weights == Some([0.0, w])`（**这条才区分得了 tier 1 是否生效**）；② 结果与「只有 vector 一路」**逐位相同** —— ⚠️ ② **独立于 tier 1**（空 lane 本来就零贡献 ⇒ 不开 tier 1 时 ② 也成立）⇒ 单靠 ② 无效 |
| **S9-T8** | 确定性（NFR-06）：同输入两次调用同结果 | ✅ | 含「同分按 `chunk_id` 升序」 |
| **S9-T9** | `Config::adaptive_fusion` 默认 `false` + 不进**配置指纹** | ✅ | 照 `embed_sessions` 先例（会话数不进指纹的判据可复制） |
| **S9-T10** | `Metrics` 新字段的语义（关时不写、开时写对） | ✅ | |
| **S9-T11** | **端到端判据臂**（S9-1a ~ S9-1d） | ✅ **载体 = `scripts/eval_s9.sh`**（**PR9-2** 交付；**不以 Rust 用例重复实现** —— 理由 = 臂级离线分析属评测资产而非产品代码，见 PR #73 正文） | **已跑出结论 = 判「不投」**（**§4.13**；复审触发条件见 4.13.6）。原稿口径（PR9-1 时点）：「⚠️ **`#[ignore]`**（需语料 + 冻结图）／走 `scripts/eval_s9.sh`（本步新增）」——⚠️ **`#[ignore]` 用例最终未新增**，由脚本承载（第 1 轮评审 **P4-3** ②） |
| **S9-T12** | T7-16 生成器的**离线桩**（不发网络请求） | ✅ **载体 = `python3 scripts/gen_t7_16.py --self-test`**（**PR9-3** 交付；见 §4.16 ③ 的**偏差 1**） | 用固定桩响应断言解析/落盘/配对逻辑 + 7 个负例（**CI 不发外部请求**）。⚠️ 本行原列在「测试计划」里、读起来像 Rust 用例；实际载体是脚本的 `--self-test`（**28 项断言**），并已由 **CI job `eval-assets`** 承载（§4.16 ③ 偏差 2） |

---

## 8. 实施任务拆分（S9-01 ~ S9-07）与 PR 切分

| 任务 | 内容 |
| --- | --- |
| **S9-01** | **本设计**（本文，`.rs` 零改动）+ 四处定义面回写 |
| **S9-02** | `FusionCtx` / `LaneStats` + `FusionStrategy::weights_override`（provided）+ 编排层接线 + `Config::adaptive_fusion`（默认 false）+ `Metrics` 两字段 |
| **S9-03** | 自适应规则实现（tier 1 + tier 2 的**信号与单阈值**，**由 S9-S1 选型**）+ 单测 S9-T6/T7 |
| **S9-04** | `scripts/eval_s9.sh`（**同一冻结图** + 两次运行 + 脚本侧配对 + 4 桶 × 4 条判据 + θ 灵敏度曲线） |
| **S9-05** | **spike S9-S1** 跑数与结论（**合取决策门 G1~G5**）⇒ 判「投 / 不投」 |
| **S9-06** | T7-16（外部 LLM API 通道 + 生成脚本 + 冻结资产 + 自查） / T7-15（假负例对照） |
| **S9-07** | 收尾：`eval-report.md` **§8.16** + 四处定义面回写 + `NFR-15` 定稿（**达标则去「拟」；不达标则收窄 + 另立 + 保持打开 + 复审触发条件** —— 三条并记） |

**PR 切分建议（4 段）**：

| PR | 内容 | 依赖 |
| --- | --- | --- |
| **PR9-0** | 本文（设计 **v0.2**，PR **#70**）+ 四处回写 + 建**本步跟踪 issue #71** | — |
| **PR9-1** | S9-02 + S9-03 + 判据 S9-T1~T9（**含生产代码**；默认关 ⇒ 零行为变化） | PR9-0 |
| **PR9-2** | S9-04 + S9-05（评测载体 + spike 结论） | PR9-1 |
| **PR9-3** | S9-06（T7-16 / T7-15；**只作对照**，可与 PR9-2 并行） | PR9-0 |
| **PR9-4** | S9-07 收尾 | PR9-1~3 |

---

## 9. 风险与未决问题

### 9.1 新增风险（**R55 ~ R58**，→ 架构 **§14.7**）

| 编号 | 风险 | 触发条件 | 后果 | 应对 |
| --- | --- | --- | --- | --- |
| **R55** | **桶标签含 qrels ⇒ 标签泄漏**：`exact` / `paraphrase` 的划分用到了 `grade ≥ 1` 段落（`t2_prep.rs:206-230`） | 实现若「按桶标签」调权 | 评测集上**必然**「优于固定权重」，但结论**不可信、不可投产**（且这正是 NFR-15 的字面口径） | **D-S9-02**（信号禁含标注）+ 判据 **S9-3**（类型级 + 反扫）+ §6.3 更正 FR-35 措辞 |
| **R56** | **80 条/桶的样本量 ⇒ 阈值可被拟合** | 用网格搜索挑最优点 | 「达标」是挑出来的；换样本即失效（同族：**R53** 的尾部不可复现） | **D-S9-08** + 决策门 **G4**（**区间**达标才算）+ 只报幅度不称显著（**D-S9-05**） |
| **R57** | **LLM 生成资产不可复现**：模型版本 / 端点 / 采样参数漂移 ⇒ 「同一份集」下次跑不出来 | 只存 query 不存来源参数 | 对照读数不可复核 | ① **冻结入库**（资产为准，不靠重跑）；② **记录 model + 参数 + prompt**；③ 生成脚本**只读环境变量**、**CI 不发外部请求**（S9-T12 用桩） |
| **R58** | **语料 / query 外发（数据出境与合规）** | 把 T2Ranking 语料或 query 发给外部 LLM 端点 | 合规风险（且 T2Ranking 的使用条款需核） | ⚠️ **开工前须用户确认**（**D-S9-07** 的附条件）；若不允许 ⇒ 降级为「规则化生成」并把**同构性声明降级**写进 §8.16 |

### 9.2 未决问题（Q9-x）

| 编号 | 问题 | 谁答 / 何时 |
| --- | --- | --- |
| **Q9-1** | tier 2 的**信号选型**（s1 / s3 / s4）与单阈值形态 | **spike S9-S1**（PR9-2） |
| **Q9-2** | BM25 是否会产生 **`score == 0` 的非空 lane**（决定 tier 1 是否可扩为「`len == 0` 或 `top ≤ 0`」） | **实现期**（读 `retriever/bm25.rs` 的累加点 + `to_lane` 不过滤这一半已核）；⚠️ **本文不拍这个前提** |
| **Q9-3** | 判据面是否应改到**精排开**的口径（**D-S9-04** 的备选） | **评审拍板**（若改，§4.5 协议与 baseline 全部重测） |
| **Q9-4** | T7-16 的**弱标注**怎么产生（LLM 自评 / 多轮一致 / 人工抽检比例） | **PR9-3 设计期**；⚠️ 不得把弱标注用于 NFR-15 的主判据（**N6**） |
| **Q9-5** | 是否**扩展 `bench`** 以支持「同轮 A/B 两个 hybrid 配置」（替代脚本侧配对） | **评审拍板**；默认**不扩**（D-S9-09） |

---

## 附录 A：新增 / 变更 API 一览 + 不变式（写进 rustdoc 的三条以内）

| 项 | 形态 | 兼容性 |
| --- | --- | --- |
| `FusionCtx<'a>` / `LaneStats` | **新增** `pub struct`（`fusion` 模块） | 非破坏 |
| `FusionStrategy::weights_override` | **新增 provided 方法**（默认 `None`）—— **决策**钩子 | **非破坏**（既有实现零改动） |
| `FusionStrategy::fuse_adaptive` | **新增 provided 方法**（默认 = `self.fuse`）—— 上一条的**应用通道**（**P3-1**：没有它，`Some(w)` 无处可用） | **非破坏**（默认转发 ⇒ 既有实现逐位一致） |
| `FusionStrategy::fusion_signal` | **新增 provided 方法**（默认 `None`）—— 信号值的**上报通道**（§4.10 ②：`weights_override` 的返回类型装不下信号值，规则 3 的实现期补齐） | **非破坏**（默认 ⇒ 既有实现零改动） |
| `Config::adaptive_fusion` | **新增** `pub` 字段，默认 `false` | 非破坏（`Config` 非外部可达；默认关 ⇒ 零行为变化） |
| `SearchIndexBuilder::adaptive_fusion(bool)` | **新增** builder 方法 | 非破坏 |
| `Metrics::fusion_weights` / `Metrics::fusion_signal` | **新增** 字段（`Option`，默认 `None`） | ⚠️ **按字面是破坏性**（见 §6.1；先例 = Step 7） |

**写进 rustdoc 的三条不变式**（I9-1 / I9-2 / I9-3）：信号只含可观测 ⇒ 不读相关性数据；剔路只能**置 0**、**不得删槽**（否则权重串路）；关闭开关 ⇒ **逐位一致**。

## 附录 B：执行命令（可复制的真实命令）

```bash
# ① 判据臂（同一冻结图；baseline = 固定权重）
cargo run --release -p helix -- bench --index <snap> --queries data/t2-queries.jsonl \
  --modes bm25,vector,hybrid --k 10 --runs 1 --rrf-k 60 --rrf-weights 1.0,1.5 \
  --json /tmp/s9_baseline.json
# ⚠️ θ 必须**能传进去**（第 2 轮评审 **P4-6**）：布尔开关拿不到 θ ⇒ sweep 无从传入。
#   ⇒ PR9-1 定 CLI 形态时用 `--adaptive-fusion-theta <v>`（值形态），θ 取自 §4.5 预注册的网格。
# 🔴 **循环变量必须是 ASCII**（第 3 轮评审 **P4-8**）：`for θ in …` 在 bash 3.2 / 5.x 下都报
#   `` `θ': not a valid identifier ``（多字节 UTF-8 不能做 bash 变量名）⇒ 「可复制的真实命令」粘贴即错。
#   ⇒ 循环变量用 `t`，θ 只留在**注释 / 文件名语义**里。
for t in 0.00 0.05 0.10 0.15 0.20 0.25 0.30 0.35 0.40 0.45 \
         0.50 0.55 0.60 0.65 0.70 0.75 0.80 0.85 0.90 0.95 1.00; do
  cargo run --release -p helix -- bench --index <snap> --queries data/t2-queries.jsonl \
    --modes hybrid --k 10 --runs 1 --rrf-k 60 \
    --adaptive-fusion true --adaptive-fusion-theta "$t" \
    --json "/tmp/s9_adaptive_theta-${t}.json"   # ⚠️ 两个开关都是 PR9-1 新增
done

# ② 脚本侧配对（按 qid 配对 per_query，逐桶算 ΔMRR@10(1) + 4 桶 + 全局 + θ 曲线）
bash scripts/eval_s9.sh --baseline /tmp/s9_baseline.json --arms /tmp/s9_adaptive_theta-*.json
```

⚠️ **守门够不着这一处**（第 3 轮评审 **P4-8** 的顺带发现）：`scripts/check_shell_expansion.py` 只扫 `scripts/` 与仓库根的 `*.sh`，**扫不到文档附录里的 bash 块** ⇒ 本处这类语法错误只能靠人工/评审发现。⇒ 已登记进 **#69**（文档形态守门）的清单。

🔑 **正式载体（v0.7 起）**：上面的手抄命令已被 **`scripts/eval_s9.sh`**（S9-04，PR9-2 交付）取代 ——
它把 θ 网格、G1~G4 阈值、G5 自证、qid 配对与 TSV 产物**固化成常量与代码**（本附录的手抄形态保留作
「最小可复现命令」的说明）；§4.13 的全部读数即由该脚本产出。

⚠️ **`--adaptive-fusion` 的 CLI 开关本身要先过 `#50` 的同族问题**（Step 7 的窗口参数也曾纠结「库内默认 vs CLI 默认」）⇒ PR9-1 设计期定：**库侧默认 `false`；CLI 只提供显式打开**（不给 CLI 默认值 ⇒ 与 `embed_sessions` 的处置口径一致）。

## 附录 C：依赖源码核实记录

| 位置 | 事实 |
| --- | --- |
| `crates/core/src/fusion/mod.rs:9-13` | 模块与再导出只有 `rrf` / `weighted` |
| `crates/core/src/fusion/mod.rs:21-28` | trait 签名**无查询上下文**（**N3**） |
| `crates/core/src/fusion/rrf.rs:19-22` | `weights` 是**字段**、非 `fuse` 形参（**N2**） |
| `crates/core/src/fusion/rrf.rs:61-62` | 权重按 `lane_idx` 取 ⇒ **删 lane 会串权重**（**I9-2**） |
| `crates/core/src/query/searcher.rs:362-367` | **空 lane 也会被 push** |
| `crates/core/src/query/searcher.rs:863-865` | `to_lane` **不过滤**分数（**Q9-2** 的一半已核） |
| `crates/core/src/rerank/mod.rs:56-85` | **provided 方法**先例（**D-S9-01** 的依据） |
| `crates/core/src/query/metrics.rs:43-44` | `Metrics` 无 `#[non_exhaustive]`、字段全 `pub`（§6.1） |
| `crates/cli/src/bench.rs:111-116` / `:313-325` | `--rrf-k` / `--rrf-weights` 已存在（固定权重臂**今天可跑**） |
| `crates/cli/src/bench.rs:2166-2180` | `per_query` JSON（脚本侧可配对） |
| `crates/core/examples/t2_prep.rs:206-230` | **桶划分用到 qrels**（**N1** / **R55**） |
| `data/t2-queries.jsonl` | 320 条、**`type` = 桶标签已物化**（**N6**） |

## 附录 D：本文引用的项目内证据

| 主张 | 位置 |
| --- | --- |
| paraphrase 桶被弱路稀释（0.2306 / 0.4294 / 0.3799；25/11/44，p=1.0） | `eval-report.md` **§3.3**（标题行 `:48`，该文件**当前版本**） |
| 图抖动极差 ≤ 0.0024（→ S9-1c / S9-1b / G2 的**量级**来源；口径是**政策容差**，见 §1.3） | `eval-report.md` **§3.4**（标题行 `:63`，该文件**当前版本**） |
| 配对符号检验「不显著」的教训（最小 p = 0.1553） | `plan-v2` §4 Step 9 + `NFR-15` 原文 |
| H7（评测资产能否替换主基线） | `plan-v2.md` §4.0 **H7** 行（**v0.26 时点 `:117`**） |
| Step 9 的验收门槛 | `plan-v2.md` §6 **Step 9** 行（**v0.26 时点 `:648`**） |
| R45 / R46 的既有登记 | `architecture-design.md` **§14.5**（**v1.24 时点 `:1670`**） |
| Step 8 的风险占位到 R54（⇒ 本步从 **R55** 起） | `architecture-design.md` **§14.6**（**v1.24 时点 `:1688-1702`**，`R54` 在 `:1702`） |
