# HelixIndex V2 开发计划（plan-v2）

> 面向 Agent 场景的通用检索引擎内核 —— **V2（下一个版本）的开发计划**。
> 本文件回答：V2 要做什么、为什么、按什么步骤做、每一步做到什么程度算完成。

| 项 | 内容 |
| --- | --- |
| 版本 | **v0.19（2026-09-16）**：**V2.1 计划修订** —— 把 2026-09-16 的 V2.1 计划审视结论（**15 条**，索引见 **§附-4**）落到计划面：① **§4.0 新增 H4~H7**（delta 分段后的 manifest / 原子发布形态；「读不阻塞写」的语义；NFR-11 写延迟目标值；评测资产能否替换主基线）；② **Step 8 补两条任务** —— **T7-25**（**前置**：R34 修法，精确扫描改逐层 `get_layer_iterator` 以消除**递归读锁**）与 **T7-26**（查询侧会话池 + 挂起的 Q3，解架构 **R43** / NFR-10 ③），并补齐依赖、验收与 PR 切分；③ **Step 9 顺序修正**（**T7-16 → T7-14**）+ 补**可证伪验收**与「评测资产只作对照、不替换主基线」纪律；④ **Step 10 补 T7-27**（**prefilter / 排序键索引**，承接架构 **R11 / R13** 的「V2.1 复议」，第一用例 = **高基数数值字段**）+ R45 / R46 的交叉注记；⑤ **新增「V2.1 横切任务」区**（#54 / #50 / #42 / #34 / R41 / R44 / R40 / #2）；⑥ **§5 新增 FR-34 / FR-35 / NFR-14 / NFR-15 草案**（**待评审**）并修正 FR-18 状态；⑦ **§6 的 V2.1 门槛改写为可判定条目**、V2.0 末条补齐勾选；⑧ **Step 8 跟踪 issue = #58**（此前 V2.1 无跟踪 issue，见 §附-3 的「跟踪缺口」）。⚠️ **不修订任何既有指标数值**；本 PR **纯文档**（`.rs` 零改动）。**v0.18（2026-09-16）**：**V2 Step 7 收尾（S7-05，设计 `v2-step7-design.md` 升 v0.3）** —— **NFR-12 定稿**（原「≤500ms（拟）」**废弃**：实测最小档 `R=10` 的 P99 = **5220ms**、差 **10.4×** ⇒ 改为**双口径**：**CPU/FP32 上界** 默认 `R=20` 下 P50 ≤ 8.8s / P99 ≤ 10.9s + **GPU/量化待测**；并写明**仅显式开启精排时适用**与**与 `R` 联动**；`R` 定稿 **20**）、**§14.5 R44 数值回填**（精排内存增量 **2.64 GiB / 7.71×**）、**R48 收窄后关闭**、**R47 全部实测**（P1/P2/P3）、**锚点改符号指代**（规则见架构 §14.5 导读）、**Step 7 的 T8/T9/T10 已本地实跑**。**v0.17（2026-09-14）**：**V2 Step 7 设计评审收口**（设计 `v2-step7-design.md` 升 **v0.2**）—— 评审 **4 项拍板全部同意**、**F1~F6 全部采纳**；本轮**只更正事实性错误**：§8 架构侧条目的「`Config` 新增字段」与 **D-S7-02** 矛盾 + R44~R48 描述与架构 §14.5 不符（**整段重写**）、NFR-12 的 500ms 补出处与「与默认 `R` 联动」、标定语料「10 万级」→ **T2Ranking 12K / 320 query**；**不改任何决策 / 预算 / 风险条目**。**v0.16（2026-09-14）**：**V2 Step 7（精排）详细设计已出、待评审**（`v2-step7-design.md` **v0.1**）——四处定义面回写（需求 **v1.16** / 架构 **v1.15** / 本文件 / `docs/README.md`）+ **NFR-12 补三部分口径**（rerank **增量** P99 **≤ 500ms（拟）**，⚠️ **对应 `R=10` 档上界、与默认 `R` 联动**；独立于 NFR-02）+ **NFR-05 补限定词「不含精排器」**（模型 `model.onnx.data` ≈2,187MB）+ **架构 §14.5 新增 R44~R48** + 补 **S7-T1~T12** 测试计划；⚠️ **前置 ① 的结论**：P5 锚点自带 **~0.6% 图漂移** ⇒ **Step 7 的 A/B 必须在「同一张冻结图」上做**；main 现为 **`516b2c7`**（PR **#48** / **#49** 均已并入）。**v0.15（2026-09-13：**Step 6 收尾（S6-10）——`v2-step6-design.md` 升 **v0.4****：四处定义面回写（需求 **v1.15** / 架构 **v1.14** / 本文件 / `docs/README.md`）+ **NFR-10 定稿并收窄适用范围**（② 只覆盖「不含查询侧编码的读路径」，编码侧并发登记为架构 **R43** 并**保持「打开」**）+ **NFR-05 补口径限定词**（「检索路径，batch 1」）+ **架构 §14.4 扩为 R36~R43**（新增 R41/R42/R43）+ 集成测试补 **S6-T10**；main **曾为** **`8f4d08c`**（PR #44 已并入）。此前 v0.14：Step 6 设计评审收口——评审 F1~F7 全部采纳、**D-S6-05 / D-S6-08 拍板**、Q4~Q7 收口；需求随之 v1.14、架构 v1.13、main 曾为 `bb23553`）** |
| 日期 | 2026-09-16 |
| 状态 | **V2.0（Step 1 ~ Step 7）已全线合并（main `62acb37`，PR #57 = S7-05 收尾）；V2.1 计划已按审视结论修订（2026-09-16，索引见 §附-4），下一步 = `v2-step8-design.md`（Step 8 设计 PR）**。**Step 1 ~ Step 7 实现完毕 + S7-05 收尾（2026-09-16）**：Step 7 的设计 PR **#51** → 实现 **#52** / **#53** / **#55** / **#56** → **收尾 S7-05**（`v2-step7-design.md` 升 **v0.3**）**全部合并**，本文件随之升 **v0.18**。此前 **决策已定案（D-J1~J7 + D-J8~J11）；Step 1 ~ Step 6 均已完成并合并进 `main`，横切 T7-21 / T7-24 亦已并入；Step 7 详细设计已出、待评审**（main **曾为** **`516b2c7`**；Step 3 = `ed25d5c`；Step 4 = S4-01 `1efc128` + 核心 `6f34ef2` + CLI/workload `ac9fcbe` + 文档回写 `9e2211e`；Step 5 = 设计 `ee42b34` + 实现 `48c0ac7` + 收尾 `590315d`，issue #22 已结案；**Step 6 = 设计 PR #38 → `46ab3a4` → spike S6-01~03（PR #40）→ 增量构建 S6-06/07/09（PR #43）→ 并发采集点 S6-08（PR #44 → `8f4d08c`）→ 收尾 S6-10（本 PR）**，issue **#2**）；**Step 6 收尾结论（2026-09-13）**：**S6-04 / S6-05 判「不投」**（T7-09：E2 增益 +11.7%/+16.0% 而峰值 RSS +96.4%/+289.2%，两侧门槛都不过）；**NFR-10 已按实测定稿并收窄适用范围**（需求升 **v1.15**：② 只覆盖「不含查询侧编码的读路径」，编码侧并发登记为架构 **R43** 并**保持「打开」**）；架构升 **v1.14**（**R38 / R39 关闭**、**R40 上锁（残余仍在）** + 新增 R41/R42/R43）；**横切 T7-24 工程卫生（PR #48 → `e756bcc`）与 T7-21 `parallel_build` 默认翻转（PR #49 → `516b2c7`）均已并入**；**Step 7 详细设计已出（`v2-step7-design.md` v0.1，2026-09-14，待评审）—— 开工前置三条已结清、**四处拍板（D-S7-04 / D-S7-05 / D-S7-01 默认 R / D-S7-08）已获评审全部同意**（设计收口 **v0.2**，F1~F6 全部采纳）**。任务编号按 2026-09-07 复审重排** |
| 上游 | `requirements-spec.md`（需求定义源）、`architecture-design.md`（架构/ADR）、`eval-report.md`（P5 实测） |
| 前置 | V1（P0~P6）全部完成，见 `plan.md`（V1 计划，已冻结，不再更新） |
| 复审 | 2026-09-07 全量复审（7 处调整 A1~A7 + 4 个拍板问题 D1~D4）**结论已全部并入本文**，不另立复审文档；调整项索引与建议执行顺序见 **§附-3** |

> ⚠️ **编号变更（2026-09-07）**：V2.0 剩余步骤与 V2.1 全部步骤**从 Step 3 起重新编号**。
> 旧号→新号对照见 §附-2。历史设计文档（`v2-step1-design.md` / `v2-step2-design.md`）中的
> Step 编号**指重排前**，阅读时请对照映射表。

---

## 1. 本文档定位

| 文档 | 定位 |
| --- | --- |
| `docs/devel/plan.md` | **V1 开发计划**（P0~P6），已完成并冻结，不再追加任务 |
| **`docs/devel/plan-v2.md`（本文档）** | **V2 开发计划**：V2 的任务分解、开发步骤、验收门槛、进度追踪**都在本文件维护** |

本文档只做**规划**（做什么、按什么顺序、怎么验收），不展开 trait/算法/ADR 等**详细设计**——每个步骤开工时，如涉及架构决策，再单独出对应设计说明（沿用 `pX-design.md` 命名）或回写 `architecture-design.md`。§4.0 已把「开工前必须先澄清的设计决策」前置列出。

---

## 2. 版本范围与决策记录（已定案）

### 2.1 范围

| 批次 | 主题 | 归属 |
| --- | --- | --- |
| **V2.0** | 质量与性能夯实 + 精排（Reranker） | 本版发布 |
| **V2.1** | 读写并发 + 相关性深化 + 场景机制 + **过滤延续（prefilter / 排序键索引，T7-27 —— spike，先判投不投）** | 下一迭代（**2026-09-16 计划修订**：prefilter 由架构 **R11 / R13** 的「V2.1 复议」承接，见 §附-4 P0-3） |

**范围外（显式声明，防蔓延）**：
- ❌ **权限 / 多租户隔离**（需求 §7「权限/多租户隔离（v2）」）——内核只提供**命名空间过滤机制**（FR-32），鉴权、权限策略、租户边界由**场景层/接入层**负责。
- ❌ 不自研 HNSW（见 D-J7）；不引入嵌入式 KV / LMDB；不做 schema-first；不改 BM25/RRF 定稿参数。

### 2.2 决策记录（已定案）

| 编号 | 决策 | 结论 |
| --- | --- | --- |
| **D-J1** | V2.0 首版范围 | ✅ **工程夯实 + T7-01（Reranker）**；相关性深化与场景机制后置 V2.1 |
| **D-J2** | Reranker 模型 | ✅ **`bge-reranker-v2-m3`**（fastembed `RerankerModel::BGERerankerV2M3`） |
| **D-J3** | 崩溃恢复方案 | ✅ **原子 rename**（与图持久化的多文件原子性统一在 ADR-A 决策，见 §4.0 H1） |
| **D-J4** | delta 分段（高复杂） | ✅ **延后到 V2.1**；V2.0 先用「图持久化 + 增量构建」缓解写瓶颈 |
| **D-J5** | 时间衰减归属 | ✅ 内核只给 **打分钩子**（FR-33），衰减策略放场景层 |
| **D-J6** | 量化模型 | ✅ **不引入**；走「并行 + 增量构建」（fastembed 无 `bge-small-zh-v1.5` 量化变体） |
| **D-J7** | T7-05 改义 | ✅ 由 `plan.md` 原「自研 HNSW / 评估 arroy」改义为「hnsw_rs 能力接入（图持久化 + 并行建图）」——依据 §附 调研：hnsw_rs 0.3.4 已原生支持，自研不必要 |

### 2.3 复审决策（2026-09-07 全量复审，D1~D4 已拍板）

> 复审共提出 7 处调整（A1~A7），**全部采纳**并已落到本文对应章节，索引见 **§附-3**。

| 编号 | 决策 | 结论 | 对应复审问题 |
| --- | --- | --- | --- |
| **D-J8** | **NFR-03 构建指标口径** | ✅ **拆「首次全量 / 增量追加」双口径**：原单口径「1 万 chunk 含 embedding < 120s」在「不引入量化（D-J6）、不换模型」的前提下**无已识别路径可达**（实测 225.5s，差 1.6×）。新口径见 §4 Step 6 与需求文档 NFR-03 | **D1** |
| **D-J9** | **低选择度过滤延迟（R18）** | ✅ **从 V2.1 提前到 V2.0**（S 级，方案已明确）；同时**新增 NFR-13** 把「10 万级 + 低选择度」纳入口径，否则它永远在 NFR-02 之外 | **D2** |
| **D-J10** | **V2.0 剩余步骤顺序** | ✅ 重排为 **Step 3 可靠性 → Step 4 资源回收 → Step 5 查询性能与可观测 → Step 6 构建性能 → Step 7 精排**。即：原 Step 5（资源回收）**提前**，原 Step 4（构建性能）**顺延** | **D3** |
| **D-J11** | **`parallel_build` 默认值翻转** | ✅ **走独立 PR**，不并入任何 Step：改默认值的同时必须同步改 **S2-T22**（现断言「默认必须串行」） | **D4** |

---

## 3. 需求来源（遗留问题清单）

> 每项标注严重度与威胁的质量属性。取舍顺序（需求文档 6.3）：**正确性/确定性 > MRR > 延迟 P99 > 召回 > 资源**。
> **编号说明**：`Q-` 前缀为本计划自用，与架构 §14 风险表（R 系列）、`plan.md` 阶段号（P 系列）、P6 任务（I- 系列）**无关**。

| ID | 问题 | 证据 | 严重度 |
| --- | --- | --- | --- |
| Q-C1 | **删除后向量残留（幽灵候选）**：`Index::remove` 不回滚向量索引；`is_live_chunk` 定义了未调用 → 已删 chunk 占 Top-K 名额、召回静默下降、图膨胀 | `index/mod.rs:173`、`vector/mod.rs`、`retriever/vector.rs` | 高（正确性） |
| Q-C2 | 墓碑 chunk 永久保留，不物理回收 | `index/forward.rs` | 中（资源） |
| Q-C3 | 快照落盘无原子性，崩溃损坏 | `storage/snapshot.rs` | **高（正确性）** —— ⚠️ V2 Step 2 重新定级（原「中」）：图进入 `save()` 后「快照」由单文件变多文件，多文件间无原子性 ⇒ **图新/快照旧的版本错配**属正确性风险，见 `v2-step2-design.md` §1（Q-C3 部分） |
| Q-P1 | NFR-03 构建 225.5s 未达标（ort ~51 条/s） | `eval-report.md` 8.2 | 中 |
| Q-P2 | NFR-04 冷启动图重建 11.57s（图不持久化，D7） | `eval-report.md` 8.3 | 中 → **✅ 已解决**（V2 Step 2 / ADR-A，实测完整冷启动 ≈100ms） |
| Q-P3 | 图重建抖动 R-P5-13（NDCG 极差 0.0024） | `eval-report.md` 3.4 | 低 → **✅ 已解决**（图落盘即冻结拓扑，同快照两次加载逐位一致） |
| Q-I1 | 过滤位图 `allowed_chunks` 全表扫描 O(N)，10 万级威胁 P99 | `query/filter.rs:39` | 高（延迟） |
| Q-I2 | 过滤是 post-filter，低选择度隔离召回暴跌 | `query/searcher.rs`、`query/filter.rs` | 高（召回） |
| Q-M1 | embed 串行（`LocalEmbedder` Mutex 全程持锁） | `embed/local.rs:67,75` | 中 |
| Q-M2 | 无并发检索 QPS 压测 | `cli/bench.rs` | 中 |
| Q-R1 | paraphrase 桶 hybrid 被弱路 BM25 稀释 | `eval-report.md` 3.3 | 高（MRR） |
| Q-R2 | Reranker 仍是 NoOp（FR-18） | `rerank/noop.rs` | 高（MRR） |
| Q-R3 | 假负例敏感度对照未做 | `eval-report.md` 10 | 低 |
| Q-R4 | 评测 query 来自人类日志，缺 Agent query | `eval-report.md` 9 | 中 |
| Q-U1 | 写端独占，`into_searcher()` 后无法写（FR-17 完整版未做） | `p6-design.md` 6.4 | 中 |
| Q-U2 | 旧 API 未标 deprecated（D-I6 延后） | `p6-design.md` 10.2 | 低 |
| Q-S1 | 无命名空间/会话隔离（依赖 Q-I2） | — | 中 |
| Q-S2 | 无时间衰减机制 | — | 低 |

> 另有跨快照永续的次生问题：`SearchIndex::remove` 后 `save()` 原样带走已删向量、`load_with` 全量重灌（`search/index.rs` 注释「会被丢弃」实际不成立）——Q-C1/Q-C2 不只在内存、还会**随快照持久化**，**Step 4**（资源回收）验收必须覆盖。

---

## 4. V2 开发步骤

> 步骤按依赖关系与质量属性优先级编排。每步列：任务、依赖、验收、风险、量级。
> **任务 ID**：T7-01~06 沿用 `plan.md` P7；**T7-07~20 为本计划新增**（T7-10 预留，原「量化评估」因 D-J6 不引入而空出，不启用）；
> **T7-21~24 为 2026-09-07 复审新增**（T7-21 `parallel_build` 默认值翻转、T7-22 低选择度暴力兜底、T7-23 `Metrics` 可观测化、T7-24 工程卫生）。
> 量级：S（天级）/ M（周级）/ L（双周+）。

### 4.0 开工前必须澄清的设计决策（不阻塞 Step 1 启动）

| # | 决策点 | 说明 | 归属步骤 |
| --- | --- | --- | --- |
| **H1** | 图持久化格式 + **多文件原子性** | hnsw_rs 图落盘是**双文件**（`hnsw.graph` + `hnsw.data`，`HnswIo`/`DumpInit`）。图持久化进入 `save()` 后，「快照」不再是单文件，「临时文件 + rename」无法保证跨文件一致（图新、快照旧版本错配）。**必须二选一**：① 图并入快照 blob（`FORMAT_VERSION` 升版）；② sidecar + manifest/generation 目录级原子替换。Step 2 与 Step 3 **不是无依赖**，需先合出这份 ADR（记为 **ADR-A**）。**→ 已由 `v2-step2-design.md` v0.3 回答（2026-09-06 拍板）**：**方案 C**——sidecar 三件套 + 独立 manifest 作唯一原子发布点（tmp+fsync+rename+fsync 目录），`FORMAT_VERSION` 保持 2；图 = 快照的派生缓存，可随时丢弃（删 manifest/删图/篡改任一字节 → 降级重建，功能不丢）。7 项决策（D-S2-01~07）已获外部评审全部同意；Step 3 的原子快照直接沿用该 manifest 机制 | Step 2 + Step 3 |
| **H2** | 向量删除的真实语义 | hnsw_rs 0.3.4 **无 remove/delete API**（架构 7.5 早记录「物理删除需 usearch，引入 C++」）。T7-07 实际语义只能是**向量墓碑 + `search_filter` 存活位图**；Q-C1 的「图膨胀」只能**缓解**，真正回收靠 T7-12 重建图。**Step 4**（原 Step 5）的「体积不无界增长」验收**必须显式覆盖向量图 + `raw_vectors` + 快照文件**。**→ 已由 `v2-step1-design.md` §4.3 / §6 D-S1-01 回答**：存活状态只住 `Index.forward`（单一真源），以位图谓词传给向量路，`VectorIndex` 不加 `tombstone`。 | Step 1 + Step 5 |
| **H3** | 精排候选窗口 | 当前编排 `search_parts` 融合后先 `take(k)` 再进 reranker（`query/searcher.rs` 的融合后截断处），精排只能重排 k 条。要拿像样的 MRR 提升必须放开窗口到 `candidate_k`（3k）或可配 R，这直接决定延迟预算。需**新增精排延迟 NFR**（NFR-12），并在 T7-01 内设计窗口参数。**→ ✅ 已由 `v2-step7-design.md` 取形并完成实现与标定（2026-09-14 设计 v0.1 → 2026-09-16 收尾 v0.3）**：**H3 = 可配 `R`、默认 20**（D-S7-01，**数值已由 S7-04 定稿**：决策门出口 ②、拐点在 `R = 20`）；窗口必须与 `candidate_k`（`searcher.rs` 的 `candidate_k` 计算处）**联动**——`candidate_k` 同时是**融合输出上限**（`parts.fusion.fuse(&lanes, candidate_k)`），不串进去则 **`R > k×3` 时被静默夹住**（R=100 在 k=10 时退化成 30，设计「发现 A」）；NFR-12 已补齐**三部分口径**（延迟 / 质量 / 降级可观测）。⚠️ **锚点写法（2026-09-16 更正）**：本节与 §附-1 原用**本仓行号**（`take(k)` 曾记 `:196` → `:286`；`candidate_k` = `:118`；`rerank` 调用 = `:316`；融合上限 = `:245`）—— 这些行号**已随 Step 7 实装全部漂移**（实测：`candidate_k` = **`:149`**、融合后截断 = **`:337`**、`rerank(...)` = **`:375`**、`fuse(...)` = **`:276`**）⇒ 按架构 §14.5 的锚点规则**改符号指代**，历史行号仅作记录。 | Step 7 |
| **H4** | **delta 分段后的 manifest / 原子发布形态** | ADR-A 方案 C 的发布点是**单 manifest**（sidecar 三件套 + tmp+fsync+rename+fsync 目录），其不变式是「**图 = 快照的派生缓存**」。Step 8 引入 delta 段落后：manifest 要挂**多段图**、compaction 要**合并段**、读侧要看到一个**已提交前缀** ⇒ 直接改动 ADR-A 的既定形态，属**架构级**问题。Step 8 的设计必须先回答（**出 ADR-B，或显式声明「沿用 ADR-A 并扩展」**），否则实现期会推翻 Step 2/3/4 的定稿 | Step 8 |
| **H5** | **「读不阻塞写」的语义** | 三种可能语义（互不蕴含）：① 读路径**不取写锁**；② **写不抬高读延迟**（需 NFR 口径）；③ 读能看到**未 commit** 的写。**本计划的取形 = ① + ②**（口径落新 **NFR-14**），**③ 明确不做**（与 NFR-11 的「`commit()` 后可见」冲突） | Step 8 |
| **H6** | **NFR-11 的写延迟目标值** | S6-10 收尾自己留的白：「写延迟目标值仍未定」——现有旁证 `eval-report.md` §8.11 的 **1.14 s/批**是**从累计 embed 耗时推算**、**不是直接计时**。Step 8 的 delta / 异步合并正是定它的场合（同时把口径从「推算」改为「直接计时」） | Step 8 |
| **H7** | **评测资产能否替换主基线** | T7-15（仅 qrels 语料重建）与 T7-16（Agent query 集）都会**新造评测资产**。默认**只作对照、不替换**（主基线 = T2Ranking 12K / 320 query）；若判定「替换」，则 P5 锚点、Step 7 的 `R = 20` 标定、NFR-12 的 8.8s / 10.9s **全部作废并重测** ⇒ 该决定须**显式拍板**（沿用 Step 7 的「结论不外推」纪律） | Step 9 |

### V2.0 —— 质量与性能夯实 + 精排（Step 1 ~ Step 7）

> **执行顺序（2026-09-07 重排后）**：Step 3 可靠性 → Step 4 资源回收 → Step 5 查询性能与可观测
> → Step 6 构建性能与多线程 → Step 7 精排。横切任务（T7-21 / T7-24）走独立 PR，不占步骤号。
> **含横切任务的完整执行顺序见 §附-3**。

#### Step 1 · 正确性修复（地基，最先做）

> **详细设计**：`docs/devel/v2-step1-design.md`（**v1.1**，2026-09-05，已吸收外部评审并**实施完成**）。
> v0.2 关键修订：`hnsw_rs::search_filter` 的停止条件**原判断有误**——带 filter 时**从不 fast return**，且堆未满时距离剪枝全程关闭 ⇒ `allowed < ef` 时必然整图遍历（结构性，tuning 治不了）。
> 由此新增：**无过滤热路径走普通 `search()` + 按存活比例过采样**（保住 fast-return 与 `EF_SEARCH=200`），**有过滤才走 `search_filter`**。另新增字段索引全类型覆盖、惰性谓词（消除 O(N) 展开）、`query_has_hits` 探针。
> 已回答 H2（向量软删除的真实语义）并核实 `hnsw_rs::search_filter` 的真实行为；
> 实施任务拆分为 S1-01~S1-11，建议顺序：先修幽灵候选（含跨快照），再做过滤下推。

**实施进度（2026-09-05 收尾）**：S1-01 ~ S1-11 **全部完成**。

| # | 任务 | 交付 |
| --- | --- | --- |
| S1-01~09 | 正确性（位图 / 存活位图 / 字段索引 / 谓词 / 快照重建 / 两路下推 / HNSW 双路径 / 编排层） | PR #6（`61e3dac`），8 个 CI job 全绿；GLM-5.3 评审 Approve |
| S1-10 | fixture + bench 选择度曲线 + NFR-02 重测 | PR #8（`0d87b39`）：`scripts/gen_synth_corpus.py`（确定性合成生成器）+ `scripts/eval_filter.sh`（一键扫描）+ bench 新增 `--filter` / `--filter-cost` / 自适应 oracle |
| S1-11 | 文档回写 | PR #8（`0d87b39`）：架构文档 **v1.5**（ADR-010、§5.4/§5.4.1、§5.5/§5.5.1、§7.5 重写、§7.6.1、§8.3/§8.4、R11~R18）+ 需求文档 v1.5 + `docs/README.md` 索引 + 设计文档 v1.0 |
| — | issue #9（bench `--oracle-depth ≥ 501` clamp panic） | PR #10（`e1e4b60`）：下界先被上限夹住再 clamp + `saturating_mul` + clap `1..=500` 范围校验 + 边界单测；设计文档升 v1.1（§0.3） |

**S1-10 实测结论 —— A. 1 万级**（release，K=10，60 query × 10 reps）：

- **T14① NFR-02 达标**：无过滤 P99 = bm25 0.42ms / vector 3.54ms / hybrid 3.46ms，均远低于「基线 × 1.10」
  （4.29 / 9.21 / 9.57ms）——**path A 保住 fast-return 的设计目标达成**（R15 已验证）
- **T13 两条结论**：① 未降级字段（`sel_01=hit`）求值从 **0.2221ms → 0.0005ms，加速比 471.8×**
  —— Q-I1 的收益真实存在；② **`ts_ms`（高基数 Range）档位加速比 0.9×**
  （新旧都退化为全扫，新路径还多一层「查索引发现降级 → 回落」的开销）——
  **Q-I1 对该场景收益为 0**，与评审 P2-2 的预判一致，且撞线比直觉更快（数值字段 terms + numbers
  合计计入限额，约 **512 篇**就降级）

**S1-10 实测结论 —— B. 10 万级**（release，K=10，200 query × 5 reps）：

| 档位 | 选择度 | bm25 P99 | vector P99 | hybrid P99 | vector ÷ 无过滤 |
| --- | --- | --- | --- | --- | --- |
| none | 1.0 | 2.43 | 5.81 | 3.46 | 1.0× |
| sel-10% | 0.1 | 0.50 | 7.91 | 7.75 | 1.4× |
| sel-1% | 0.01 | 0.32 | 41.33 | 41.30 | **7.1×** |
| sel-0.1% | 0.001 | 0.25 | 158.45 | 144.87 | **27.3×** |
| ts-range-degraded | 0.001 | 8.26 | 124.26 | 121.87 | **21.4×** |

- **R18（新增风险）：低选择度过滤查询延迟爆炸**。选择度 ≤1% 时直接违反 NFR-02（限额 10/20ms，
  实测超 **4~16×**）。这是 v0.2 对 R13 的预估（"随规模线性上升"）所不及的量级——
  实际是**规模 × 选择度的复合效应**。V2.0 **接受**：正确性优先于延迟，且优化前的 post-filter
  在该选择度下几乎返回不了任何结果（Q-I2 的原始病症）。V2.1 必须解决，
  数据已支持的廉价方案是 **allowed 很小时绕开 ANN 直接暴力扫描**（10 万级 0.1% 档位 allowed=100，
  代价约 0.05ms vs 158ms）
- **NFR-02 无过滤档位在 10 万级仍达标**（2.43 / 5.81 / 3.46ms），path A 未退化
- **P2-2 在 10 万级得到直接证据**：`ts-range-degraded` 的 bm25 P99 = **8.26ms**，
  比 bm25 无过滤检索本身（2.43ms）还贵 **3.4 倍**——降级字段的 O(N) 全扫已超过整个检索的代价
- **v0.3 的错误归因已推翻**：⚠️ 此前写「hybrid 重合率未达 0.99，缺口全部来自向量路」**是错的**。
  实测 bm25 与 vector **分路重合率都是 1.0000**，只有融合后掉到 0.685~0.948 ⇒ 分歧**不可能**
  来自任何一路的下推，**只能**诞生于融合层（filter-then-fuse vs fuse-then-filter 的排名语义差异）。
  因此 **hybrid 重合率不能当召回缺口读**；真正的召回判据是分路重合率 1.0000 + 返回条数 10.00（均满分）。
  两种融合顺序哪种相关性更好**未决**，需选择度专用相关性评测集 → 列 V2.1

| 任务 | 解决 |
| --- | --- |
| T7-07 **向量墓碑 + 存活过滤**（`VectorIndex` 软删除 + `search_filter` 存活位图，非物理 remove） | Q-C1 |
| T7-08 过滤下推 + 字段索引（替换 `allowed_chunks` 全表扫描） | Q-I1、Q-I2 |

- **依赖**：无。
- **验收**：删除后三路（含向量/hybrid）不再召回该 doc（回归测试）。10 万级语料过滤耗时：**10 万级为扩展验证**（需另行准备 fixture，`data/` 现仅 12K/24K）；NFR-02 的标定口径仍为 1 万 chunk（hybrid P99<20ms），10 万级按新口径另测另录。
- **风险**：**高**（`VectorIndex` trait 加软删除，Hnsw/Brute 两实现都改；且软删除语义需与 H2 澄清一致）。
- **量级**：M。

#### Step 2 · 冷启动与确定性（D7）

| 任务 | 解决 |
| --- | --- |
| T7-05 图持久化 + 并行建图（`HnswIo` 双文件 dump + `parallel_insert`） | Q-P2、Q-P3 |

- **依赖**：**先合出 ADR-A（H1，与 Step 3 合并）**，定稿图持久化格式与多文件原子性。**✅ ADR-A 已拍板**（`v2-step2-design.md` v0.3，2026-09-06）：方案 C（sidecar 三件套 + 独立 manifest 原子发布点，`FORMAT_VERSION` 保持 2）。
- **验收**：12K 冷启动（加载 + 图加载）< 2s；**同一快照两次加载**检索结果逐位一致（消 R-P5-13；注意「两次重建」在 `parallel_insert` 下不成立，验收必须指「同快照两次加载」）。
- **风险**：低（API 已源码核实；开工前复核另发现两项：Description 落盘 magic 实为 4、重载图 datamap_opt=true 拒绝覆盖旧文件——对策均已成文，见设计文档「开工前复核记录」）。
- **量级**：M。

> **状态（2026-09-07 更新）：✅ 已完成并合并进 `main`。**
> main 提交链：`68074c9`（#12 core 图持久化）→ `e9ac2b0`（#15，源自 #13：CLI/bench 接线 + 并行建图）
> → `15c1900`（#16，源自 #14：文档回写 + 12K 实测入库）→ `1e51156`（#17：S2-T6 oracle 断言改多 query 均值）
> → `01e01eb`（#35：S2-T6 断言**再修正**为**两条拓扑无关的不变式**，**issue #34**）
>   = `graph_points == raw_vectors` 的**内容覆盖** + **全部 N 篇自匹配**。
>   ⚠️ 撤掉数值门的理由**不是**「绝对阈值不可用」：CI 那次的 `均值 0.545` 落在抖动分布**之外**、
>   **根因至今未定位**（#34 保持 open），换门是为了让失败**可归因**；详见设计文档 §8 S2-T6 行。
> `main` 的 CI 全绿，issue #3 已关闭，8 项验收全绿。
> ⚠️ **踩过的坑**：原 #13/#14 的 base 是各自的堆叠分支，GitHub 显示 `MERGED` 但内容**从未进 main**，
> 最终靠 cherry-pick 两个 squash 提交、重建 base=main 的 PR 才真正落地 ⇒ **堆叠 PR 的 base 一律设 main**。
>
> S2-01~S2-12 全部落地，守门全绿
> （fmt / clippy `-D warnings` / **213 测试** / MSRV 1.90 / `RUSTDOCFLAGS="-D warnings" cargo doc` / `--no-default-features` /
> `--features charabia` / cargo-deny）。
>
> **12K 实测（`data/t2-corpus.jsonl`，release，macOS aarch64）**：
>
> | 项 | 实测 | 判据 |
> | --- | --- | --- |
> | 快照加载（含位图/字段索引重建） | 57~77ms | —— |
> | 图 sidecar 加载 | 23~37ms | —— |
> | **完整冷启动** | **≈100ms**（三次 82~100ms） | **< 2s ✅**（余量 20×） |
> | 降级路径（图 sidecar 缺失） | 56~78ms + 图重建 ≈10~11.5s ≈ **11s** | 对照：即 D7 时代的水平 |
> | 图 dump 耗时 | 43.5ms（31.6MB） | 远低于预估，非瓶颈 |
> | 磁盘增量 | 52.3MB → ≈84MB（**1.6×**，graph 7.9~8.1 + data 23.7） | 已接受（R22） |
>
> ⚠️ **达标依赖图 sidecar 命中**——降级路径仍是 ~10s。这正是 NFR-07「降级不得静默」
> 的由来：`GraphStatus::Rebuilt(reason)` 必须显式可见，否则 NFR-04 会静默失效而不自知。

#### Step 3 · 可靠性（原子快照）✅ 已完成并合并（PR #27 → `ed25d5c`，2026-09-08）

> **完成记录**：单实现 PR #27 落地 S3-02~09（设计 `v2-step3-design.md` v0.3，D-S3-01~07 全按建议采纳）。
> **验收对照**：① 崩溃不变式 ✅——故障注入 cp1/cp2/cp3（S3-T3~T5 单元层 + S3-T6 端到端）
> 证明 save 序列任一注入点崩溃后 `load` 恒得旧或新快照，`SnapshotCorrupted` 不可达；
> ② 快照重写后旧图失效 ✅——既有测试零修改回归（S3-T2）；③ tmp 孤儿回收 ✅——快照 tmp
> save 截断复用 / load best-effort 删除，manifest tmp 由 save 路径回收（S3-T3/T8）；
> ④ R19 写路径收敛 ✅——`dump_graph_caught`（S3-T7），Lenient 下 save Ok + `PersistFailed`；
> ⑤ fsync 代价实测入 `eval-report.md` §8.7（+0.027~0.035s，S3-T10）；⑥ 守门清单全绿。
>
> **2026-09-08 评审回应（`a474d86`，CI 全绿）**：Deepseek 3 条 inline 全部处理——
> 补 S3-T8 单测（写/清两侧命名一次改齐的测试兜底）、T9 断言前取 `InjectionGuard`
> （消除 flaky 窗口）、fsync_dir 平台注释按实证分列（macOS/APFS 目录 fd `sync_all`
> 实测 Ok）。**集成补强（`dc31b3f`）**：新增 S3-TI1~TI5——tmp 孤儿回收 load/save
> 两侧终态、半截快照公开错误面（`SnapshotCorrupted`/`Io`，绝不 panic）、图三件套
> 命名对齐（目录清单级）、Strict 失败完整生命周期（失败不丢索引 + 清障恢复）。

| 任务 | 解决 |
| --- | --- |
| T7-13 原子快照（tmp + fsync + rename + fsync 父目录，D-J3） | Q-C3 |

**范围（2026-09-07 复审重写，依据 ADR-A）**：ADR-A 已把「多文件原子性」收敛为
**manifest 唯一发布点 + 父快照 CRC 版本锚点**，故架构 §7.6.2 明确：
> 「Step 3 只需让 `foo.idx` 自己 tmp+rename，图 sidecar 自动跟随」

⇒ 本步骤是**单文件原子写**，不是多文件事务；原验收里「覆盖图 + 快照双文件」的描述已过时。

**⚠️ 现状事实**（✅ 已于 Step 3 消除，以下为开工前事实存档）：`storage/snapshot.rs:88` 仍是 `File::create(path)` 直写 + `flush()`，
**既无 tmp+rename、也无 fsync** ⇒ 崩溃中途产生半截文件，下次 `load` 得
`Error::SnapshotCorrupted`——**不是降级重建，是索引丢失**。（对比：图 manifest 已原子，
`storage/graph.rs:236 write_manifest_atomic()`。）

| 子任务 | 内容 |
| --- | --- |
| S3-a | 从 `write_manifest_atomic` 抽出通用 `atomic_write(path, bytes)`：tmp → flush → `sync_all` → rename → fsync 父目录；快照与 manifest **共用一份实现** |
| S3-b | `save_with_crc` 改走 `atomic_write`，补上此前完全缺失的 fsync |
| S3-c | tmp 孤儿回收：save/load 时清理 `*.idx.tmp` 与 `*.idx.hnsw.manifest.tmp`（并补 `.gitignore`） |
| S3-d | 故障注入测试：`catch_unwind` + 可注入失败点，在「写完 tmp、未 rename」处 panic，断言**原快照完好** |
| S3-e | 顺带收 **R19**（写路径 `panic_any` 的 TOCTOU 残余）——与 S3-a 属同一处代码边界，合并做最省 |

- **依赖**：无（ADR-A 已定案）。
- **详细设计**：✅ 已出并拍板（`docs/devel/v2-step3-design.md` **v0.3**，PR #26）——
  D-S3-01~07 全部按建议采纳（含 R19 收敛、D-S3-07 Strict 清理）；S3-02~09 按其
  任务拆分实施（量级 S，单 PR）。
- **验收**：中断/崩溃后 `load` 要么拿到**旧快照**、要么拿到**新快照**，**绝不出现 `SnapshotCorrupted`**；图 sidecar 因 CRC 锚点自动降级（该行为已被 Step 2 覆盖，不重复验收）。
- **风险**：低；⚠️ 造「写文件必失败」的测试场景**别用 chmod**（CI 以 root 运行时权限位无效，Step 2 已踩过），让目标位置被**同名目录**占据更稳。
- **量级**：S。**排在 V2.0 剩余工作最前**——Q-C3 定级为「高（正确性）」。

#### Step 4 · 资源回收（原 Step 5，D-J10 提前）

| 任务 | 解决 |
| --- | --- |
| T7-12 墓碑物理回收（compaction，重建向量图 + 回收 `raw_vectors` + 重写快照） | Q-C2 |

- **依赖**：Step 1（软删除语义就位，✅ 已合）；**Step 2 的图 sidecar 机制**（✅ 已合）；
  **Step 3 的 `atomic_write`**（✅ 已合 `ed25d5c`）⇒ **全部前置已解除阻塞**（2026-09-08）。
- **详细设计**：🟩 **已出并拍板**（`docs/devel/v2-step4-design.md` **v0.3**，2026-09-08）。
  主线：按存活集**重新物化**（正排稠密化 + 倒排 remap + 死词摘除 + 图重建）+ **ID 重编号**
  （D-S4-01，不重编号则快照正文的空洞无法消除 ⇒ 验收不可达）；
  落盘复用 Step 3 `atomic_write`（不引入新崩溃一致性机制），`FORMAT_VERSION` 保持 2。
  10 个决策 D-S4-01~10，**其中 D-S4-01 = 重编号、D-S4-10 = `compact()` 开头先 `self.commit()?`
  已拍板**（2026-09-08）；任务拆分 S4-01~10（PR 切分：S4-01 先合 → 核心单 PR → CLI/workload → 文档回写）。
- **新发现（设计期）**：`flush()` 把 pending 灌进 `raw_vectors` 时**无 liveness 检查**
  ⇒ `add → remove → commit` 会让已删 chunk 的向量入库并跨快照永续（资源问题，FR-26 不破）。
  已列 **S4-01**（独立小 PR，可先合）。
- **⚠️ Step 2 引入的新复杂度**：compaction 重建图后**必须重发 manifest**（`nb_point` /
  `graph_crc` 全变），否则新图永远匹配不上 ⇒ 每次冷启动都走降级重建（≈10s），
  **白重建一次且 NFR-04 静默失效**。
- **验收**：长期零散写入后，**向量图 sidecar + `raw_vectors` + 快照正文**三者体积均不随删改
  无界增长（覆盖跨快照永续问题）；compaction 后 `GraphStatus` 回到 `Loaded`（不是 `Rebuilt`）。
- **新增**：可复现的「零散写入」workload 脚本（`scripts/` 目前没有；fixture 已齐：
  `data/synth-100000-corpus.jsonl`）。
- **风险**：中；**重建图会重新引入拓扑抖动**（NFR-06 不破，但评测可比性需在文档说明）。
- **量级**：M。

#### Step 5 · 查询性能与可观测（新增，D-J9）

| 任务 | 解决 |
| --- | --- |
| **T7-22** 低选择度暴力兜底（R18，从 V2.1 提前） | Q-I2 次生 / **NFR-13**（新增） |
| **T7-23** `query::Metrics` 可观测化 | issue #7 遗留 / NFR-10、NFR-11 前置 |

- **T7-22**：10 万级实测，选择度 ≤1% 时 vector P99 达 41~158ms（超 NFR-02 限额 4~16×）。
  方案已明确且廉价：**`allowed` 小于阈值时绕开 ANN、直接暴力扫描**
  （10 万级 0.1% 档 `allowed≈100`，代价约 **0.05ms vs 158ms**）。阈值在实现时标定。
  ⚠️ 该场景此前**不在任何 NFR 口径内**，故同步新增 **NFR-13**。
  ⚠️ **设计期修正（v0.10）**：「0.05ms」只覆盖"对 ~100 个候选算距离"一段，
  **不含定位候选**的 `O(N)` 遍历成本（1~10ms，待标定）——完整分解见设计文档 §4.2.4。
- **T7-23**：`query::Metrics` 目前只经 `tracing::info!` 输出——**外部拿不到实例 ⇒ 无法单测、
  无法被 bench 采集**；而 `vector_shortfall` 正是「V2.1 是否引入 prefilter」的判据，
  Step 6 的 NFR-10/11 实测也要靠它。需暴露进 `SearchResponse` 或 bench 采集链路。
  顺带修 `query/metrics.rs:14` 里「见 issue #7」的失效引用（#7 已关闭）。
- **详细设计**：🟩 **已定稿并落地**（`docs/devel/v2-step5-design.md` **v0.5**（S5-04 标定定稿版），2026-09-10；
  经 PR #33 两轮评审 + PR #36 实现期评审）。
  主线：向量路扩为**三条路径**（A 热路径 / B `filtered-ANN` 不变 / **C 精确扫描**），
  **策略归后端**（`VectorIndex::search_exact_filtered` 必选 + `prefers_exact` 默认钩子）、
  **分派与记账归编排层**（`Metrics.vector_route`）；候选来源取**方案 A 遍历向量存储**
  （`hnsw_rs` 的**全量点遍历**——`&PointIndexation` 的 `IntoIterator`，每点恰一次
  \+ `Point::get_v()` 零拷贝，源码核实 + 实测见设计文档 §3.1/附录 C；
  ⚠️ **不是 `get_layer_iterator(0)`**，那只走 layer 0，会漏掉 level ≥ 1 的点约 3%，
  该误判已在 v0.2 纠正）；
  `Metrics` 进 `SearchResponse` + 补 per-lane 耗时。9 个决策 **D-S5-01~09 已全部拍板**
  （二次评审对 D-S5-01/03/05 无异议 ⇒ **阻塞解除**；D-S5-02/04 仅数值待 S5-04 标定，
  形态与口径已定、**不阻塞实现**），任务拆分 S5-01~08
  （PR 切分：兜底 / 可观测 / 文档回写）。**阈值与 NFR-13 预算已由 S5-04 标定回填**（见下方「实现与标定」）。
- **⚠️ 设计期新发现**：`searcher.rs:99-104`（`index_is_empty`）与 `:125-127`（过滤排空）
  **两条**早退路径漏设 `metrics.took` ⇒ 日志 `took_ms=0` 而响应 `took` 为真值
  （同类第三处 `:212-213` 反而设了）⇒ 列 D-S5-08 随 T7-23 修。
  ⚠️ 复核**不能**用 `grep "metrics.log"`——`:99-104` 根本不调它，结构上找不到。
- **文档回写（2026-09-10，随本 PR #33 一并落地）**：`requirements-spec.md` 升 **v1.10** ——
  **NFR-13** 补「**端到端 P99（含过滤求值）** + 拟 ≤20ms」口径与**成本口径修正**
  （「约 0.05ms」只是算距离那一段、不含 `O(N)` 定位候选 1~10ms）、**NFR-07** 补本 Step 的
  落地路径（D-S5-05~08）；`architecture-design.md` 升 **v1.9** —— **§8.3** 新增「Step 5 补外部可见」
  块 + 改写「当前限制」、**§14 章级主表 R18 补 ④**（口径修正 + 三路径 + ⚠️ 只覆盖低选择度子集，
  **R11/R13/R17/R18 不得标「已解决」**）、**§14.3 新增 R31~R35**、`docs/README.md` 风险范围改 **R1~R35**。
- **实现与标定（已完成）**：
  - **代码**：PR **#36**（`feat/v2-step5-exact-fallback`）已并入 main **`48c0ac7`** —— 三提交：
    `c29c31f` 后端兜底 / `23a3cc7` 编排层分派 + `Metrics` 可观测 + D-S5-08 修复 + CLI/bench 入口
    / `bded58c` 评审响应（口径改条件式 + S5-T7 补真后端行为护栏）。
  - **收尾**：PR **#37**（`perf(vector):`）已并入 main **`590315d`**（2026-09-11，**用户手动合并**）——
    两提交 `fdf365c`（S5-04 阈值 8192 定稿 + NFR-13 双口径）与 `af748c9`（S5-08 文档回写 + 评审响应）；
    CI run `34554828828` 9/9；**issue #22 已结案**（`CLOSED`/`COMPLETED`）。⚠️ 本文件此前的
    「PR #36 → `48c0ac7`」已由本条补齐（#37 才是 main 的当前落点）。
  - ⚠️ **遗留的文档尾巴已随 Step 6 设计 PR 一并清理**：`docs/README.md` / `architecture-design.md` §14.3 /
    `v2-step5-design.md` 状态行 / 本文 §7 进度表与 §6 门槛勾选；另 §4 Step 3 小节标题里的
    「PR #27 **待合并**」与 `docs/README.md` 同处的陈旧行（Step 3 早已并入 `ed25d5c`）同批清。
  - **S5-04 标定**：10 万级 A/B（`--brute-fallback off` vs 强制精确，8 档位 × 2 轮）⇒
    **阈值定稿 `8192`**（两端点实测 + 归一插值交叉点 ≈ 9700；原初值 1024 **过保守**，会漏掉 `allowed=5043`
    这个实测 2.06× 的档位）。全表见 **`eval-report.md` §8.9**。
  - **终版回写（S5-08，随本批）**：`requirements-spec.md` 升 **v1.12** —— **NFR-13 由「拟 ≤20ms」
    改为双口径**（常规 ≤20ms / 降级字段 ≤35ms）；`architecture-design.md` 升 **v1.11** ——
    §14.3 **R31/R33「未标定」→「已标定」**、⚠️ **R34 措辞改写为「含潜在死锁」**、
    §14 章级主表 **R18 补 ⑤**（标定后端到端 P99）；`docs/README.md` 索引行同步；设计文档升 **v0.5**。
- **依赖**：无；**T7-23 是 Step 6 的前置**（✅ 已满足）。
- **验收**：✅ **全部达成**——10 万级 `sel-1%` / `sel-0.1%` / 降级字段档位的端到端 P99 进入 NFR-13 预算
  （常规最差 **7.06ms** ≤ 20ms、降级字段最差 **22.84ms** ≤ 35ms；对照**兜底前** 112.69 / 223.24ms）；
  `Metrics` 已有单测 + bench 采集点。⚠️ 该收口**只覆盖低选择度子集**（`allowed > 8192` 仍回路径 B，
  R11/R13/R17/R18 不得标「已解决」）。
- **量级**：S。

#### Step 6 · 构建性能与多线程（原 Step 4，D-J10 顺延）

| 任务 | 解决 |
| --- | --- |
| T7-09 embed 并行（**先 spike 再定是否投 M 级**） | Q-P1、Q-M1 |
| T7-11 增量构建（content_hash → embed 缓存，跳过已 embed 文档） | Q-P1 |
| T7-17 并发检索压测进 bench（QPS + 正确性，`Searcher` 跨线程） | Q-M2 |

- **⚠️ NFR-03 已按 D-J8 拆双口径**，本步骤按新口径验收（不再是「1 万 chunk 全量 <120s」）：

  | 口径 | 目标 | 依据 |
  | --- | --- | --- |
  | **首次全量** | 1 万 chunk 含 embedding 构建 **< 240s** | 实测 225.5s（12K）；在「不量化（D-J6）、不换模型」前提下无已识别路径可降到 120s |
  | **增量追加** | 追加 10% 文档 < 首次全量 × 10% × 1.2 | Agent 场景的真实使用形态是反复追加，T7-11 才是这条口径的落点 |

- **T7-09 的技术前提已被证伪一半**（2026-09-07 复审，源码级核实）：
  - `fastembed-6.0.2` 的 `InitOptions.intra_threads` 默认 `None` = **用满所有核**
    （`src/init.rs:30-33`），我们未覆盖 ⇒ 单 session 的 ONNX 推理**已在满核跑**，
    多 session 大概率是**抢核**；
  - 批次不是瓶颈：32/64/128/256 → 62.6 / 59.1 / 54.4 / 51.6 条/s（`eval-report.md:147-148`）；
  - 建图并行只占 4%（225.5s 里 embed 209.9s）。
  ⇒ **T7-09 先做 S 级 spike，出数据再决定是否投 M 级**：

  | 实验 | 内容 |
  | --- | --- |
  | E1 | 单 session 基线（现状） |
  | E2 | 2~4 session × `intra_threads` 分片 |
  | E3 | **CPU EP vs CoreML EP**（`ort-2.0.0-rc.13` 有 `coreml` feature，fastembed 暴露 `with_execution_providers`）——macOS aarch64 上**从未评估过**的杠杆 |

  ⚠️ E3 若有效，**向量数值会变** ⇒ 必须同时重测 **NFR-06 确定性**与相关性基线，
  且 `ConfigFingerprint` 需纳入 execution provider（否则 CoreML 建库 / CPU 加载会静默错配，
  正是 B1/B2 那类坑）。

- **依赖**：T7-23（Step 5）是 T7-17 的前置（✅ **已满足**：`Metrics` 已进 `SearchResponse`，随 Step 5 合并）；T7-11 依赖 Step 1（✅ 已合）。
- **风险**：中（embed 并行内存峰值上升，需重测 NFR-05；E3 改变数值）。
- **量级**：M（含 S 级 spike）。

> **状态（2026-09-13 更新）：✅ Step 6 已完成并合并、且已收尾**（`docs/devel/v2-step6-design.md` **v0.4**，
> issue **#2**）：设计 PR **#38**（评审 7 条意见 F1~F7 全部采纳、**D-S6-05 / D-S6-08 拍板**、Q4~Q7 收口）
> → spike S6-01~03（**PR #40**，**判「不投」**）→ 增量构建 S6-06/07/09（**PR #43**）
> → 并发采集点 S6-08（**PR #44** → `8f4d08c`）→ **收尾 S6-10（本 PR）**。
> ⚠️ 评审的两处**合并前必修**（均在附录 B）**已落地**：**F1** delta 必须与 base **不相交**（否则 1200 条全部命中 `add` 短路查重 ⇒ NFR-03 ② 的测量空转；已验证「新增 1200 / 去重跳过 0」）；**F2** `cargo run -p helix-cli` 的包不存在 ⇒ 已改为 **`-p helix`**。
>
> 设计期核实与修订（以源码为准，**三处与本节原描述不一致，已更正**）：
>
> | 本节原描述 | 核实结果 |
> | --- | --- |
> | 「`ort-2.0.0-rc.13` 有 `coreml` feature」 | ✅ 对（`ort/Cargo.toml` `coreml = ["ort-sys/coreml"]`） |
> | 「fastembed 暴露 `with_execution_providers`」 | ✅ 对（`text_embedding/init.rs:51/83/122`），且 `lib.rs:89` 重导出 `ExecutionProviderDispatch` |
> | ⚠️「走 CoreML 只需加 feature」 | ❌ **不成立**：fastembed 6.0.2 **没有 coreml 透传**（只有 `directml`）⇒ 必须把 `ort` 提为**直接依赖**并开 `coreml` |
> | ⚠️ EP 类型名 | ❌ **已过时**：rc.13 的真实路径是 **`ort::ep::CoreML`**（`#[cfg(feature="coreml")]`），**不是** `CoreMLExecutionProvider` |
> | ⚠️「单 session 按 `DEFAULT_BATCH_SIZE=256` 串行分块」 | ✅ 对，但**实际进入 ONNX 的 batch 是 64**——调用链在 `flush()` 的 `batch_size`(64) 处已切片（`search/index.rs:390`），内部 256 的切分**从不触发**。排障时别把 256 当实际 batch |
>
> 设计期新事实（对 T7-11 有利）：**doc 级增量骨架已在** —— `content_hashes` 入快照
> （`index/mod.rs:88/501-503/535`）⇒ `load` 之后 `doc_id_by_hash` 仍可用，重复文档**天然跳过 embed**；
> 真正缺的只有 ① CLI 无「追加」入口（`BuildArgs` 只有 `--input`）② hash 粒度是**整篇文档**（改一字 ⇒ 全量重 embed）。
> 另发现 **`default_embedder()` 静默降级**（`search/config.rs:312-318` 的 `.ok()`）——要向量却拿到纯 BM25 且无告警（架构 **R38**）。
>
> 风险登记（**S6-10 收尾后**）：**架构 §14.4 R36~R43** —— **R36 / R37 随 spike 判「不投」消解**、**R38 / R39 关闭**、**R40 上锁（残余仍在）**、**S6-10 新增 R41**（建库路径峰值 RSS）/ **R42**（依赖安全公告漂移）/ **R43**（**查询侧编码封顶并发**，保持打开）。
> ⚠️ **本 Step 不承诺**把 NFR-03 ① 从 225.5s 压到 120s（D-J8 已判定不可达）；NFR-03 ① 只做「别弄坏」。
> ✅ **已拍板（评审 2026-09-11）**：**D-S6-08 / Q4 —— NFR-10 口径 = 方案 A**（`--threads 4` QPS ≥ `--threads 1` × **2.5**；
> 逐位一致为**前置条件**；测量口径 = 预热丢弃首轮 + 顺序交错 + 报告运行范围；**数值标「拟」、待 S6-08 实测定稿**）
> ⇒ 需求文档已升 **`requirements-spec.md` v1.14**。**Q5 —— 「追加 10%」= 文档数**（chunk 数只作参考、不进判据）；
> **Q6** = 方案 A（`default_embedder` 硬失败，不需额外兼容开关）；**Q7** = 沿用既有 `--no-graph-persist`、不新增。
> 评审同时背书：**D-S6-01「只承诺 spike」**、**「先 T7-11（跳过 embed）后 T7-09（加速 embed）」的顺序**，以及 **R38 的定性**（既有缺陷、非设计引入）。

#### Step 7 · 精排（相关性第一刀，V2.0 收尾，原 Step 6）

| 任务 | 解决 |
| --- | --- |
| T7-01 Reranker 接入（fastembed `TextRerank`，`bge-reranker-v2-m3`，D-J2）+ **精排候选窗口放开到 `candidate_k`/可配 R** | Q-R2 |

**开工前置三条（2026-09-07 复审新增）**：

1. **先复现基线**：验收写的是「MRR@10(1) 相对 P5 基线 0.6922 可测提升」，但**没人复现过基线**。
   第一步就用 `scripts/eval_quality.sh` + T2Ranking qrels 跑出当前值**与波动范围**，
   否则「提升」无法判定（Step 1 已吃过「重合率 0.685~0.948 波动」的亏）。
2. **模型下载可行性**：API 已核实可用（`TextRerank::try_new(RerankInitOptions)` +
   `RerankerModel::BGERerankerV2M3` 均存在于 fastembed 6.0.2），但该模型含外置
   `model.onnx.data`（**GB 级**）⇒ 需先确认下载体积与镜像可达性。
3. **H3 必须先定**：精排窗口 + NFR-12 至今未决；放开到 `candidate_k`（3k）会让 rerank
   成为延迟主导项。

**开工前置三条 —— 结账单（2026-09-14）**：

| # | 前置 | 结论 | 证据 |
| --- | --- | --- | --- |
| ① | 先复现基线 | ✅ **已复现，且复现出「基线自身带图漂移」这一定性事实** | P5 锚点 `hybrid MRR@10(1) = 0.6922`，本次复现 **0.68815**（Δ ≈ 0.6%）；**对照：bm25 三项逐位复现 Δ=0** ⇒ 唯一变量是**未播种的 HNSW 图**（`StdRng::from_os_rng()`）⇒ **Step 7 的 A/B 必须在「同一张冻结图」上做**（`--index` 读快照 + `--runs 1`；⚠️ `--runs > 1` 会**故意重建图**）。见 issue `#23` 评论 `#issuecomment-5658037231` |
| ② | 模型下载可行性 | ✅ **已实下载并验 sha256** | reranker = **`rozgo/bge-reranker-v2-m3`**（⚠️ **非** BAAI 官方；官方库**没有 ONNX 导出**），`model.onnx.data` **≈2,187 MB**、sha256 **`84b66c78…8945`**。⚠️ 2.17 GB **FP32** ⇒ **内存与 NFR-05 / R41 同族，须独立预算**（架构 **R44**） |
| ③ | H3 必须先定 | ✅ **设计已取形**（数值待标定） | **H3 = 可配 `R`、默认 20**（D-S7-01，**2026-09-16 定稿**：S7-04 标定走决策门出口 ② —— 拐点在 `R=20`、`20→50` 的边际降到首段的 **6.6%**）；`R` 与 `candidate_k` **联动**；**NFR-12 补齐三部分口径**。⚠️ 设计仍**留 4 项待评审拍板**（D-S7-04 精排默认开关 / D-S7-05 `Hit.score` 语义 / D-S7-01 默认 R / D-S7-08 `max_length`） |

**🔑 设计期源码复核（本节原描述有 5 处需更正，已改；一律以 `v2-step7-design.md` 附录 C 为准）**：

| 本节原描述 | 核实结果 |
| --- | --- |
| 「融合后先 `take(k)` 再进 reranker（`query/searcher.rs` 的融合后截断处）」 | ⚠️ **行号沿革 → 已改符号指代（2026-09-16）**：原记 `:196`，2026-09-14 更正为 `:286`（`candidate_k` = `:118`、`parts.reranker.rerank(...)` = `:316`、`SearchResponse` 字面量 = `:323-329` / 空响应 = `:498-514`）；**实装后再度全部漂移**（实测 `candidate_k` = `:149`、`take(take_n)` = `:337`、`rerank(...)` = `:375`）⇒ 按架构 §14.5 锚点规则改用**符号指代**，历史行号仅作记录 |
| 「放开窗口到 `candidate_k`（3k）或可配 R」 | ✅ 方向对，但 **`candidate_k` 同时是融合输出的上限**（`parts.fusion.fuse(&lanes, candidate_k)`）⇒ 不把窗口**串进** `candidate_k`，则 **R=100 在 k=10 时会静默退化成 30**（设计「发现 A」） |
| 「精排延迟独立口径，不并入 NFR-02」 | ✅ 对；但**必须是「增量」口径**（`rerank_elapsed` 单独计时），且**候选回捞组装是线性放大的**——`matched_terms` 对**全段** chunk 文本跑 `analyze_doc`（`crates/core/src/query/explain.rs:13-17`）⇒ **R 每翻一倍，组装成本同步翻倍**（设计「发现 B」） |
| 「bge-reranker-v2-m3」 | ✅ 对，但**来源是 `rozgo/…` 而非 BAAI 官方**（fastembed `src/models/reranking.rs:28-33`） |
| 「`TextRerank` API 已核实可用」 | ✅ 对（`fastembed` 的 `TextRerank::try_new` —— `src/reranking/impl.rs`；v6.0.2 下 `:44`）；⚠️ 但 `rerank()` **返回全量排序结果**（`src/reranking/impl.rs` 的 `TextRerank::rerank`，v6.0.2 下 `:215-224`。⚠️ **2026-09-16 更正**：原文写「stable sort ⇒ 同分保持输入序」——**本项目的实装刻意不沿用**该行为，改用 **`chunk_id` 升序**（D-S7-07，`crates/core/src/rerank/scoring.rs` 的 `apply_scores`）；「返回全量排序结果」这一条不变）⇒ 需自行 `take(top_n)` 并按 `RerankResult.index` **回填** |

**设计文档**：✅ **已实现完毕并收尾**（`docs/devel/v2-step7-design.md` **v0.3**，2026-09-16；实现 PR **#52 / #53 / #55 / #56** 全部合并，收尾 **S7-05** = 本次 PR）。主线：`Reranker` trait **零签名破坏**（`crates/core/src/rerank/mod.rs`）；新增 **`LocalReranker`**（fastembed `TextRerank` + `RerankerModel::BGERerankerV2M3`，仿 `embed/local.rs` 的 `Mutex` + `default_cache_dir()` 形态，模型缓存目录 `~/.cache/helix-index/models`）；`Reranker` trait 新增 **provided 方法 `candidate_window(&self, k) -> usize`（默认返回 `k`）**（D-S7-02 —— **`Config` / `SearchParts` 一律不改**，故自定义实现一行不改、NoOp 路径逐位零回归）；`SearchResponse::metrics` 追加 **`rerank_window` / `rerank_elapsed`**（纯加法）；`Explain` 追加 **`rerank_score`**（⚠️ 破坏性）；编排层改为**先按窗口回捞（`candidate_k = max(3k, window, 10)` 联动）、再交给 reranker、最后 `take(k)`**。决策 **D-S7-01~10**、任务拆分 **S7-01~05**、测试 **S7-T1~T12**；风险 **R44~R48**。

- **依赖**：Step 2（性能基线稳定后测精排延迟）；**H3 ✅ 已取形**（2026-09-14）。
- **验收**：hybrid MRR@10(1) 相对**已复现的基线**可测提升；精排延迟有实测（**NFR-12**，独立口径，不并入 NFR-02）。⚠️ **「已复现的基线」现已明确定义为「同一张冻结图上的 `--rerank off` 臂」**（前置 ① 的结论：P5 锚点自身带 ~0.6% 图漂移）⇒ 验收**必须**读 `--index` 快照、`--runs 1`；⚠️ 同时注意 **`candidate_k` 的静默夹取**（未串进窗口则 R 退化）与 **`BatchLongest` 使跨 batch 组成的分数不可逐位复现**（设计「发现 A / C」）。
- **风险**：中；CI 需沿用 `#[ignore]` 模式 + 本地缓存策略。
- **量级**：M。

### 横切任务（独立 PR，不属于任何 Step）

| 任务 | 内容 | 决策 |
| --- | --- | --- |
| **T7-21** `parallel_build` 默认翻转为「开」 | 12K 真实语料 11.676s → 2.182s（**5.35×**），50K 合成 5.26×，真实语料 oracle 重合率无差异（0.995 / 0.995）。⚠️ 必须同步改 **S2-T22**（现断言「默认必须串行」） | **D-J11 / D4** |
| **T7-24** 工程卫生 | ① **CI 只在 `branches:[main]` 触发** ⇒ 堆叠 PR 至今跑不到 CI（Step 2 已吃过亏）；② `.gitignore` **未覆盖图 sidecar 与 manifest tmp**（31MB 级误提交风险）；③ 清理 11 条已合并远端分支 | 复审 A4-④ |

> **状态（2026-09-14）：两个横切任务均已并入 `main`，本批结清** ——
> **T7-24 工程卫生** = PR **#48** → **`e756bcc`**：① CI 去掉 `pull_request.branches: [main]`
> （原写法 ⇒ **堆叠 PR 完全没 CI**）+ 补 `workflow_dispatch` + 补 **`concurrency`**（取消被取代的旧 run，
> 实测旧 run 结论 = `cancelled`）；② `.gitignore` 补 `*.hnsw.{graph,data,manifest}`（图 sidecar **本体**
> 原先可被 `git add`）；③ 清理 5 条远端已合并分支 + 陈旧 ref `refs/remotes/pr/40` + 本地对应 5 条。
> **T7-21 `parallel_build` 默认翻转** = PR **#49** → **`516b2c7`**：只翻**门面**默认
> （`SearchIndexBuilder` / `Config`），**低层 `HnswRsIndex::with_capacity` 保持 `false`**（11 处构造点）。
> ⚠️ **评审 P2-1 修正了本条原文的一处错**：并行**不只**对「一次性全量建图」生效 —— 增量 `flush` 交付的是
> **整个 `pending`**（`add()` 先推全部 chunk 再查阈值）⇒ 批量 = `(batch_size − 1) + k` ⇒
> **大单文档（`k ≥ 937~1000`，默认 `Chunker(512/64)` 约 42~45 万字符）同样走并行**
> ⇒ **「增量建库一定可复现拓扑」不成立**。
> ⚠️ **S2-T22 已同步改为显式构造**（`.with_parallel_build(false)`）—— 否则「串行」臂会静默吃门面默认、
> 退化成「**并行 vs 并行**」而**断言仍全绿**。
> ⚠️ 另开 issue **#50**（CLI 缺 `--no-parallel-build` 逃生舱，来源 #49 评审）—— 待办、不属本批。

### V2.1 —— 读写并发 + 相关性深化 + 场景机制 + 过滤延续（后续迭代）

> 📌 **2026-09-16 计划修订**（审视结论 15 条，索引见 **§附-4**）。**V2.1 的开工纪律与 V2.0 一致**：每步先出
> `v2-stepN-design.md`（含 D-SN-01~xx / SN-Txx 测试计划 / SN-xx 任务拆分与 PR 切分 / 风险续号），
> 再走实现 PR 链，最后收尾回写四处定义面。
> ⚠️ **评测类任务承 Step 7 的基线纪律**：任何「A/B 比质量」都必须在**同一张冻结图**上做
> （`--index` 读快照 + `--runs 1`）—— P5 锚点 `hybrid MRR@10(1) = 0.6922` **自身带 ~0.6% 图漂移**。
> ⚠️ **任务编号**：沿用 `T7-xx` 序列，**新增从 T7-25 起**（T7-10 仍为预留、不启用）。
> ⚠️ **风险编号**：V2.1 的设计**从 `R49` 起**分配（⚠️ 不要按旧口诀从 R44 起算）。

#### Step 8 · 读写并发（原 Step 7）· 跟踪 issue **#58**

| 任务 | 内容 | 解决 |
| --- | --- | --- |
| **T7-25**（**硬前置**） | **R34 修法**：精确扫描的遍历改**逐层 `get_layer_iterator`**（每层一个独立 `IterPointLayer`、层间 guard 不重叠 ⇒ 消除**递归读**同一把 `RwLock`）。⚠️ **必须先于 T7-06**：现「不可达」仅因 `HnswRsIndex::add` 要求 `&mut self`（单写者），一旦读写并发**该路径即活**，后果是**永久挂死**（std-only 探针 **3/3** 复现） | 架构 **R34** |
| **T7-26** | **查询侧会话池**（每 worker 独立 session）+ 先答**挂起的 Q3**（不同 `sessions` / `intra_threads` 是否改变向量数值 —— 若改变会破坏 NFR-10 ① 的逐位一致前置）。这是 **R43 的复审触发条件**、也是 **NFR-10 ③ 的唯一出口** | **R43** / NFR-10 ③ |
| T7-06 | delta 分段（FR-17 完整版：读不阻塞写）—— `into_searcher()` 平滑换成 `searcher()`，API 形态不变 | Q-U1 |
| T7-18 | 旧 API `#[deprecated]`（D-I6 复议）—— ⚠️ issue **#5** 的原始定位是「**评估**」（输出可能是「不废弃」），本表按「评估 → 依结论执行」读 | Q-U2 |

- **依赖**：**T7-25 → T7-06**（硬前置，见上）；T7-18 依赖 T7-06；T7-26 与 T7-06 **可并行**（前者动编码侧、后者动索引侧），但两者**都要求 T7-25 先落地**。
- **开工前置（设计期必答）**：**H4**（delta 分段后的 manifest / 原子发布形态，必要时出 **ADR-B**）、**H5**（「读不阻塞写」的语义）、**H6**（NFR-11 写延迟目标值）；⚠️ 另需**择一**：FR-17 §5.3.5 的「写入后立即可查」与 NFR-11 的「`commit()` 后立即可查」**口径不一致**（见 §附-4 P1-7）。
- **验收**：FR-17 §5.3.5 四条（`commit()` 后立即可查（**按 H5 取形预写 —— FR-17 §5.3.5 原文措辞待 Step 8 设计期一并更正**） / 合并期间持续检索**无失败、无超时** / 延迟变化在 **NFR-02** 范围内 / 不得以「重建索引中」为由拒绝查询）+ 新增 **NFR-14**（写期间读延迟与可见性口径，**草案**）+ **NFR-10 ③ 重测**（会话池落地后，vector / hybrid 的 `QPS(4)/QPS(1)` 并入判据）。
- **T7-25 的回归锁（第 1 轮评审补强建议，2026-09-16 采纳）**：验收须含 ① R34 的 **std-only 探针复跑**（证「递归读已消除」；探针现成、成本极低）；② **覆盖等价断言**「每点恰 `yield` 一次」（`v2-step5-design.md` §3.1 **v0.2** 的关键纠正：`get_layer_iterator(0)` **不是全量**、漏 `level ≥ 1` 的点、实测 **3.74%**）⇒ **逐层写法钉死为 `for l in 0..=pi.get_max_level_observed()`**（API 实测存在：`hnsw_rs-0.3.4/src/hnsw.rs:469` / `:814`）。两条均登记进 `v2-step8-design.md` 的测试计划（S8-Txx）。
- **风险**：高（跨两棵树合并 BM25 统计量 + **并发下 R34 可达** + **H4 属 ADR 级改动**）。
- **量级**：L。
- **PR 切分建议（照 V2.0 惯例）**：① 设计 PR（`v2-step8-design.md`，含 H4/H5/H6 + ADR-B + 四处回写）；② **T7-25 R34 修法**（可独立评审、**建议先合**）；③ delta 分段内核（T7-06，含 manifest / compaction 改造）；④ 查询侧会话池（T7-26，含 Q3 实验）；⑤ 旧 API deprecated（T7-18）；⑥ 收尾 PR（S8-xx 回写 + NFR-14 / NFR-11 定稿）。

#### Step 9 · 相关性深化（原 Step 8）

| 任务 | 内容 | 解决 |
| --- | --- | --- |
| **T7-16**（**前置**） | Agent query 评测集（LLM 生成 + 多轮 + 自查）—— ⚠️ **顺序修正（2026-09-16）**：原表把 T7-14 的依赖写成「Step 7」，但「在**分桶**上优于固定权重」需要**分桶依据**，而现 qrels 来自人类日志（Q-R4）⇒ **T7-16 是 T7-14 的前置** | Q-R4 |
| T7-14 | 自适应融合（paraphrase 桶弱路 BM25 稀释修复）—— 判据落新 **NFR-15** | Q-R1 |
| T7-15 | 假负例敏感度对照（仅 qrels 语料重建）—— ⚠️ **只作对照，不替换主基线** | Q-R3 |

- **依赖**：**T7-16 → T7-14**（顺序修正，见上）；T7-14 另依赖 Step 7（Reranker 就位）；T7-15 独立。
- **评测资产纪律（新增，必读）**：T7-15 / T7-16 都会**新造 qrels / query 集** ⇒ 一律**只作对照、不替换主基线**（主基线 = T2Ranking 12K / 320 query）；若要替换，则 P5 锚点、Step 7 的 `R = 20` 标定、NFR-12 的 8.8s / 10.9s **全部作废并重测**，该决定**须显式拍板**（**H7**）。
- **验收（新增，可证伪）**：自适应融合在**分桶**上优于固定权重，且判据须在**设计期定值**：① 指标（建议 hybrid **MRR@10(1)** 为主、NDCG 为辅）；② 阈值（如 ΔMRR ≥ 0.01）；③ **统计口径** —— Step 7 的教训是「配对符号检验**不显著**」（最小 p = 0.1553）⇒ 须**预先声明**「按幅度读、不声称显著」，或给出可行的样本量；④ 评测集与目标场景的**同构性声明**（沿用「结论不外推」纪律）。
- **风险**：高（自适应融合需重测，防过拟合）。
- **量级**：M。

#### Step 10 · 场景机制（面向知识/记忆，原 Step 9）

| 任务 | 内容 | 解决 |
| --- | --- | --- |
| T7-19 | 命名空间 / 会话隔离（依赖过滤下推） | Q-S1 |
| T7-20 | 时间衰减打分钩子（可插拔，D-J5）—— ⚠️ **先定「作用于哪个分」**：`Hit.score` 语义已随开关而变（融合分 → `σ(logit)`，架构 **R46**） | Q-S2 |
| T7-02 | MMR 结果去重（FR-24） | — |
| T7-03 | token budget 裁剪（FR-25） | — |
| T7-04 | 多 query 融合接口（FR-21） | — |
| **T7-27**（**新增**） | **prefilter / 排序键索引**（承接架构 **R11 / R13** 的「V2.1 复议 prefilter」）—— 第一用例 = **高基数数值字段**：字段索引有基数保护（`terms + numbers ≤ 1024`），毫秒时间戳 / 雪花 ID **约 512 篇**即永久降级、该字段过滤回落 O(N) 全扫 ⇒ **Q-I1 对最常见的真实过滤场景收益为 0**（架构 §5.5 / ADR-010 代价段） | R11 / R13 / Q-I1 |

- **依赖**：T7-19 依赖 Step 1（过滤下推）。
- ⚠️ **T7-27 先做「是否投」的判定**：架构 §8.3 已把它挂在一个可读信号上 —— **`Metrics.vector_shortfall`**（`Exact` + 缺口 > 0 ⟺ 图未覆盖全部 allowed）。判据 = 用评测集跑出「高基数字段过滤下 shortfall 的实际分布」，**有数据再决定投不投**（同 Step 6 的 E1/E2/E3 先例：**收益不确定的任务写成 spike + 决策门**）。
- ⚠️ **与 Step 7 的窗口机制交叉（新增注记）**：T7-02 MMR 需要**比 k 更大**的候选池、T7-03 要按序裁剪、T7-04 要进融合阶段 ⇒ 三者都会撞 **R45「窗口被 `candidate_k` 静默夹取」**（`candidate_k` 同时是**融合输出上限**）⇒ 设计期必须先定「后处理阶段看到多少候选」。
- **风险**：中（**T7-27 属「收益待证」类，须 spike 前置**）。
- **量级**：M（**T7-27 若判定「投」则另计**）。

#### V2.1 横切任务（独立 PR，不占步骤号）

> **新增（2026-09-16）**：V2.0 有横切任务区（T7-21 / T7-24），V2.1 此前**没有** ⇒ 下列已排队但**无归属**的事项在此登记。

| 事项 | 来源 | 处置 |
| --- | --- | --- |
| **#54** hnsw ANN 路径在失控 `k` 下会 panic（库内 `ef_arg.max(knbn)` 抵消上限） | PR #53 评审 P3-3 | 独立 PR（修法明确：入口夹取 `k`）；**不阻塞 V2.1 任何 Step** |
| **#50** CLI 缺 `--no-parallel-build` 逃生舱 | PR #49 评审 | 独立 PR（S 级） |
| **#42 / #34** 两条 flaky（`integration.rs` / `graph_persist.rs`） | CI 实测 | 走 `flaky-test-triage` 流程；⚠️ **#34 的断言余量取自另一条代码路径**，属**测试自身缺陷**、不是产品缺陷 |
| **R41** 建库路径峰值 RSS 无预算（实测 2.32~3.33 GB vs NFR-05 的 372MB） | S6-10 | 保持「**只给形态不给数**」；要定预算须先定 `batch` 与序列长度的取值口径 |
| **R44** 精排常驻内存无独立上限（增量 **2.64 GiB / 7.71×**） | S7-05 | 条目保持开放；要定上限须先定「精排默认关」的产品口径 |
| **R40** 残余（ID 复用的「不冲突」证明依赖三条隐含前提） | S6-10 | 靠 S6-T8 测试锁；**前提被打破（如 compaction 压缩尾部）须重审** |
| **#2** Step 6 遗留清单 | Step 6 | 见其评论 `#issuecomment-5654947790`；**保持 OPEN** |
| ⚠️ **R43 不在本表** | S6-10 | 已升格为 **Step 8 的 T7-26**（不再挂横切） |


---

## 5. 需求编号草案（评审通过后回写 `requirements-spec.md`）

> 回写纪律：需求文档 §1.3 要求编号唯一。**FR 无「升级」机制**——已存在编号一律**原条目内修订**，不新建同编号条目；新需求才新增编号。

> ⚠️ 「对应步骤」列已于 2026-09-07 按重排后的编号更新（旧号见 §附-2）。

| 编号 | 需求 | 优先级 | 对应步骤 | 与既有需求的关系 |
| --- | --- | --- | --- | --- |
| FR-18 | Reranker 接入真实模型（**原条目内修订**：保留单编号，将「第一版只留位」改为「V2.0 接入 bge-reranker-v2-m3」）—— ✅ **已实现并收尾（2026-09-16）**：实现 PR **#52 / #53 / #55 / #56** + 收尾 **S7-05（#57 → `62acb37`）**，设计 `v2-step7-design.md` **v0.3**；⚠️ **模型来源更正为 `rozgo/bge-reranker-v2-m3`**（**非** BAAI 官方） | Must | Step 7 | 修订 FR-18 |
| FR-17 | 增量写入（**标注「部分交付」**：V1 已交付主索引增量；「读不阻塞写」完整版 V2.1 Step 8） | Must | Step 8 | 修订 FR-17 交付状态；**2026-09-16 补 Step 8 任务号（T7-25 → T7-06 / T7-26）与 NFR-14** |
| FR-15 | 幂等 upsert 与删除（**向量删除并入**，删除语义扩展到向量侧） | Must | Step 1 | 修订 FR-15 |
| FR-16 | 索引二进制快照（**原子性并入**：save 原子替换 + 图持久化格式） | Must | Step 2/3 | 修订 FR-16 |
| FR-26 | 向量软删除 + 存活过滤 | Must | Step 1 | 强化 FR-15 |
| FR-27 | 过滤下推 + 字段索引 | Must | Step 1 | 新增 |
| FR-28 | 增量构建（只 embed 新增） | Should | Step 6 | 新增 |
| FR-29 | 图持久化 | Must | Step 2 | 强化 FR-16 |
| FR-30 | 墓碑物理回收（compaction） | Should | Step 4 | 强化 FR-15/26 |
| **FR-31** | 原子快照 | **Must**（2026-09-07 由 Should 升：Q-C3 定级「高（正确性）」且为 V2.0 必做） | Step 3 | 强化 FR-16 |
| FR-32 | 命名空间隔离 | Should | Step 10 | 新增（≠权限/多租户，见 §2.1 范围外） |
| FR-33 | 时间衰减打分钩子 | Could | Step 10 | 新增 |
| **FR-34** | **prefilter / 排序键索引（高基数字段）**（**草案，待评审**）：字段索引有基数保护（`terms + numbers ≤ 1024`），毫秒时间戳 / 雪花 ID **约 512 篇**即永久降级、该字段过滤回落 O(N) 全扫 ⇒ 要求「高基数字段的过滤成本与语料规模解耦」。⚠️ **是否投由 T7-27 的 spike 判定**（判据 = `Metrics.vector_shortfall` 的分布） | Should | Step 10 | 强化 FR-27；**承接架构 R11 / R13** 的「V2.1 复议 prefilter」 |
| **FR-35** | **融合策略自适应**（**草案，待评审**）：融合权重按桶（paraphrase / exact / 长尾）自适应，修复弱路 BM25 的稀释 | Should | Step 9 | 新增；⚠️ **与 FR-21「多 query 融合接口」不同** —— 本条是**单 query 内**的融合权重（原 Q-R1；定名 `FusionStrategy` 一族，见架构 §5.6） |
| FR-21 | 多 query 融合（**收编**进入 V2 范围，原即 Could=v2） | Could | Step 10 | 原有 |
| FR-24 | MMR 结果去重（收编） | Could | Step 10 | 原有 |
| FR-25 | token budget 裁剪（收编） | Could | Step 10 | 原有 |
| NFR-03 | 索引构建（**D-J8 拆双口径**：首次全量 < 240s / 增量追加 < 首次 × 变更比 × 1.2） | Must | Step 6 | 修订口径（原「1 万 chunk <120s」无路径可达） |
| NFR-10 | 并发读吞吐 | Should | Step 6 | 新增（前置：Step 5 的 T7-23） |
| NFR-11 | 增量可见性与写延迟（**口径修订**：`add` 后需 `commit()` 才可见 ⇒ 改为「commit 后立即可查；写延迟 = flush 一批耗时」） | Should | Step 6 | 新增 |
| **NFR-12** | **精排延迟**（**2026-09-16 定稿**（2026-09-14 补为三部分）：① **延迟判据 = 双列，均标「拟」** —— **端到端 `took` P50/P99** + **精排段 `rerank_elapsed` P50/P99**（**定稿 = 双口径**：**CPU/FP32 上界** 默认 `R=20` 下 P50 ≤ 8.8s / P99 ≤ 10.9s；**GPU/量化待测**），口径 = **仅 `rerank()` 调用本身**（**不含**回捞与 `explain` 组装），独立口径、不并入 NFR-02；② **质量判据** = hybrid MRR@10(1) 相对**同图**基线可测提升；③ **可观测** = `Metrics.rerank_window` / `rerank_elapsed` + `Explain.rerank_score.is_some()`，不得静默；⚠️ 精排**未启用**时 `LocalReranker` **编译期不存在** ⇒ 不设运行时降级字段） | Should | Step 7 | 新增（H3）；**「≤500ms（拟）」已废弃（2026-09-16，S7-05）**：实测最小档 `R=10` 的 P99 = **5220 ms**（差 **10.4×**）⇒ **CPU-only / FP32 / 未量化下不可达**；定稿改为**双口径**且**仅显式开启精排时适用**（默认关，D-S7-04）（同 NFR-13 / NFR-10 先例）；⚠️ A/B 必须在**冻结图**上做 |
| **NFR-13** | **低选择度过滤延迟**（10 万级 + 低选择度档位的 P99） | Should | Step 5 | 新增（D-J9；此前该场景不在任何 NFR 口径内） |
| **NFR-14** | **读写并发下的读延迟与可见性**（**草案，待评审**）：① **可见性** = **`commit()` 后立即可查**（对齐 NFR-11；**FR-17 §5.3.5 的「写入后立即可查」措辞待 Step 8 设计期一并对齐**）；② **读延迟** = 合并期间持续检索**无失败、无超时**，且 P99 **仍在 NFR-02 预算内**（与 FR-17 验收同源）；③ 不得以「重建索引中」为由拒绝查询。⚠️ **数值与测量口径由 `v2-step8-design.md` 定稿**（同 NFR-13 / NFR-10 先例：先落「拟」、标定后定稿） | Should | Step 8 | 新增（**H5**）；Step 8 此前**只有 FR-17 §5.3.5 的四条定性验收、无编号 NFR** |
| **NFR-15** | **自适应融合的质量判据**（**草案，待评审**）：在**分桶**上优于固定权重；⚠️ 指标 / 阈值 / **统计口径**须设计期定值 —— Step 7 的教训是**配对符号检验不显著**（最小 p = 0.1553）⇒ 须**预先声明**「按幅度读、不声称显著」或给出可行样本量 | Should | Step 9 | 新增（**H7**）；Step 9 此前**无任何 FR / NFR 编号**（见 §附-4 P0-4） |

---

## 6. 验收门槛（Gate）

**V2.0 发布门槛**（Step 1~7 全部满足；✅ = 已达成）：

- [x] **Step 1**：Q-C1 修复有回归测试——删除后三路（含向量/hybrid）不再召回该 doc（PR #6）
- [x] **Step 1**：过滤下推后，1 万 chunk 口径单 query 过滤耗时在 NFR-02 预算内；10 万级扩展验证已另测另录（`eval-report.md`）
- [x] **Step 2**：图持久化后 12K 冷启动 ≈100ms（< 2s），且**同一快照两次加载**结果逐位一致（消 R-P5-13）
- [x] **Step 3**：原子快照通过故障注入测试——崩溃后 `load` 要么旧快照、要么新快照，**绝不 `SnapshotCorrupted`**（PR #27 已合；fsync 代价 +0.027~0.035s，见 `eval-report.md` §8.7）
- [x] **Step 4**：compaction 后三个体积（向量图 sidecar / `raw_vectors` / 快照正文）不无界增长，且 `GraphStatus` 回到 `Loaded` —— 10K churn 0.3×5 实测：graph+data **84.6→33.7MB**（-60%）、`nb_point` 25000→10000、reload `Loaded` 冷启动 91~99ms（`eval-report.md` §8.8）
- [x] **Step 5**：10 万级低选择度档位 P99 进入 **NFR-13** 预算（常规最差 **7.06ms** ≤ 20ms / 降级字段最差 **22.84ms** ≤ 35ms）；`Metrics` 有单测（`step5_query_observability.rs` 7 条）+ bench 采集点（PR #36 / #37 → **`590315d`**）
- [x] **Step 6**：**NFR-03 按双口径**重测达标 —— ① 首次全量 **233.1s < 240s** ✅；② 增量追加 **23.705s < 27.973s** ✅（余量 15.3%；`eval-report.md` §8.11）；**NFR-05** 内存口径**收窄为「检索路径（batch 1）」**、建库路径内存**独立为架构 R41**（数值待定）；**`Searcher` 跨线程并发读吞吐 + 正确性有数据** —— 正确性（① 前置条件）✅ **逐位一致**，吞吐实测 `QPS(4)/QPS(1)` = bm25 **2.98×** ✅ / vector 1.88× / hybrid 1.63×（`eval-report.md` §8.12）。**2026-09-13 S6-10 裁定**：NFR-10 ② 的适用范围**收窄为「不含查询侧编码的读路径」**（bm25 达标），**NFR-10 整体保持「打开」**、编码侧并发登记为架构 **R43**，**复审触发条件 = 查询侧会话池落地时**（需求 **v1.15**）。**D-S6-01（T7-09）已结案：判「不投」**（E2 增益 +11.7%/+16.0% 而峰值 RSS +96.4%/+289.2%；E3 调优后仅 +1.2%）
- [x] **Step 7**：Reranker 接入后 MRR@10(1) 相对**已复现基线**可测提升（**S7-04 实测**：分层 60 子集上 hybrid `ΔMRR` = **+0.0384**，其中 87.1% 来自窗口放开；⚠️ **配对符号检验不显著**、**子集口径不可与全量 320 横比**）；精排延迟满足 NFR-12（**2026-09-16 定稿**：双口径 + 仅开启时适用）。⚠️ **评测集与目标场景不同构** ⇒ 结论不外推。**2026-09-14 设计已出**：基线口径**收紧**为「**同一张冻结图**上的 `--rerank off` 臂」（P5 锚点自带 ~0.6% 图漂移）；NFR-12 补为**三部分**（增量 P99 ≤ **500ms 拟**、**与默认 `R` 联动** / 质量 / **可观测**）；交付形态与 S7-T1~T12 见 `v2-step7-design.md` v0.1。✅ **已实现（#52 / #53 / #55 / #56）+ 收尾（S7-05）**
- [x] `make fmt && make lint && make test && make deny` 全绿；CI 全绿（**2026-09-16 核实**：Step 7 的 4 个实现 PR + 收尾 PR #57 均 CI 全绿；本地守门链读数见各 PR 正文）

**V2.1 门槛**（Step 8~10，**2026-09-16 改写为可判定条目** —— 原为一句定性话，四个判据里三个无阈值）：

- [ ] **Step 8**：`searcher()` 与写入**并存** —— ① 合并期间持续检索**无失败、无超时**且 P99 仍在 NFR-02 预算内（**NFR-14** 定稿）；② 可见性 = **`commit()` 后立即可查**；③ **T7-25（R34 修法）先落地**（否则并发下可挂死）；④ **NFR-10 ③ 重测**（会话池落地后，vector / hybrid 的 `QPS(4)/QPS(1)` 并入判据）
- [ ] **Step 9**：自适应融合在**分桶**上优于固定权重（**NFR-15** 定值：指标 + 阈值 + 统计口径），且**评测资产是否替换主基线已显式拍板**（**H7**）
- [ ] **Step 10**：命名空间低选择度过滤**召回不回退**（**容差须定义**）+ MMR / token budget 有单测与示例 + **T7-27 的 spike 有「投 / 不投」结论**（判定「投」则另立预算与 NFR）
- [ ] **V2.1 全体**：`make fmt && make lint && make test && make deny` 全绿；CI 全绿（含 `--features local-rerank` / `--features charabia` 两条 feature 腿）

---

## 7. 进度追踪（V2）

> 每完成一个任务在此勾选。

| 步骤 | 任务 | 状态 |
| --- | --- | --- |
| Step 1 | T7-07 向量墓碑 + 存活过滤 | ✅ 已完成（2026-09-05，PR #6 → `61e3dac`） |
| Step 1 | T7-08 过滤下推 + 字段索引 | ✅ 已完成（2026-09-05，PR #6 → `61e3dac`） |
| Step 2 | T7-05 图持久化 + 并行建图 | ✅ 已完成（2026-09-07，PR #12/#15/#16/#17/#18 → `68074c9`/`e9ac2b0`/`15c1900`/`1e51156`/`9db2c00`） |
| **Step 3** | T7-13 原子快照（S3-a~e，含 R19） | ✅ 已完成并合并（PR #27 → `ed25d5c`，2026-09-08；评审回应 `a474d86` + 集成补强 `dc31b3f`） |
| **Step 4** | T7-12 墓碑物理回收（compaction，含 manifest 重发）/ S4-03~S4-07 | ✅ **已完成并合并**（核心 PR #30 → `6f34ef2`，2026-09-09；含评审响应：无墓碑早退、`bytes_before=None` 诚实语义、失败语义 rustdoc） |
| **Step 4** | S4-01 `flush` 侧幽灵向量防线（设计期新发现） | ✅ 已完成（PR #29 → `1efc128`，2026-09-09） |
| **Step 4** | S4-08 CLI `helix compact` + S4-09 churn workload 实测 + S4-10 文档回写 | ✅ **已完成并合并**（PR #31 → `ac9fcbe`；集成测试补强 `aed8829`：CLI 语义 5 例 + compaction 场景 3 例） |
| **Step 5** | T7-22 低选择度暴力兜底 | ✅ **已完成并合并**（设计 PR #33 → `ee42b34`；实现 PR #36 → `48c0ac7`；收尾 PR #37 → **`590315d`**，2026-09-11，issue #22 已结案） |
| **Step 5** | T7-23 `query::Metrics` 可观测化 | ✅ **已完成并合并**（同上；`Metrics` 已进 `SearchResponse`，`step5_query_observability.rs` 7 条单测 + bench 采集点） |
| **Step 6** | T7-09 embed 并行 / T7-11 增量构建 / T7-17 并发检索压测 | ✅ **已完成并合并**（设计 PR #38 → `46ab3a4`；spike S6-01~03 → PR **#40**；增量构建 S6-06/07/09 → PR **#43**；并发采集点 S6-08 → PR **#44** → `8f4d08c`；收尾 **S6-10** → 本 PR，2026-09-13；`v2-step6-design.md` 升 **v0.4**） |
| **Step 6** | T7-09 embed 并行（**先 E1/E2/E3 spike**） | ⛔ **判「不投」**（E2 +11.7%/+16.0% 而峰值 RSS **+96.4%/+289.2%**；E3 调优后 +1.2%）⇒ **S6-04 / S6-05 不开工**；`coreml` feature 保留为默认关闭的**探针基础设施**（`eval-report.md` §8.10） |
| **Step 6** | T7-11 增量构建 | ✅ 已完成（PR #43；`helix build --index` 追加路径 + NFR-03 ② 实测 **23.705s < 27.973s**，余量 15.3%；`eval-report.md` §8.11） |
| **Step 6** | T7-17 并发检索压测 | ✅ 已完成（PR #44；`helix bench --threads` + S6-T11/T12 + NFR-10 实测；`eval-report.md` §8.12 / **§8.13**。⚠️ **编码侧并发未达标 ⇒ 登记架构 R43、NFR-10 保持「打开」**） |
| **Step 7** | T7-01 Reranker 接入（bge-reranker-v2-m3） | ✅ **已完成并合并**（设计 PR **#51** → `1859dbf`；实现 **#52** → `e082f8b`、**#53** → `45b1768`、**#55** → `9a957dd`、**#56** → `742c229`；收尾 **S7-05** = 本次 PR。`v2-step7-design.md` **v0.3**，2026-09-16；**开工前置三条已结清**、留 4 项待评审拍板） |
| Step 8 | **T7-25**（**硬前置**）R34 修法：精确扫描改逐层 `get_layer_iterator` | ⬜ **计划已修订（2026-09-16）** |
| Step 8 | **T7-26** 查询侧会话池 + 挂起的 Q3（解 R43 / NFR-10 ③） | ⬜ **计划已修订（2026-09-16）** |
| Step 8 | T7-06 delta 分段 | ⬜ |
| Step 8 | T7-18 旧 API deprecated（**#5**，先评估、后依结论执行） | ⬜ |
| Step 9 | **T7-16** Agent query 评测集（**T7-14 的前置**，顺序已修正） | ⬜ **计划已修订（2026-09-16）** |
| Step 9 | T7-14 自适应融合 | ⬜ |
| Step 9 | T7-15 假负例对照（**只作对照、不替换主基线**） | ⬜ |
| Step 10 | T7-19 命名空间隔离 | ⬜ |
| Step 10 | T7-20 时间衰减钩子（**先定作用于哪个分** —— R46） | ⬜ |
| Step 10 | T7-02 MMR 去重 | ⬜ |
| Step 10 | T7-03 token budget 裁剪 | ⬜ |
| Step 10 | T7-04 多 query 融合 | ⬜ |
| Step 10 | **T7-27**（**新增**）prefilter / 排序键索引（承接 R11 / R13，**先 spike 判投不投**） | ⬜ **计划已修订（2026-09-16）** |
| **横切** | T7-21 `parallel_build` 默认翻转（独立 PR，含改 S2-T22） | ✅ **已完成并合并**（PR **#49** → **`516b2c7`**，2026-09-14；只翻**门面**默认，低层 `HnswRsIndex::with_capacity` 不动；⚠️ 评审 P2-1 修正：增量 `flush` 交付**整个 `pending`** ⇒ **大单文档同样走并行**） |
| **横切** | T7-24 工程卫生（CI 触发 / gitignore / 分支清理） | ✅ **已完成并合并**（PR **#48** → **`e756bcc`**，2026-09-14；CI 去掉 `pull_request.branches: [main]` + 补 `workflow_dispatch` + **`concurrency`**；`.gitignore` 补 `*.hnsw.{graph,data,manifest}`） |

> T7-10 预留（原「量化评估」，D-J6 决定不引入后空出，不启用）。
>
> **V2.1 计划修订（2026-09-16）**：上表 Step 8~10 已按 §附-4 的审视结论修订 —— 新增 **T7-25 / T7-26 / T7-27**、修正 **T7-16 的顺序**、补 **NFR-14 / NFR-15 / FR-34 / FR-35** 草案，并把横切事项单列于 §4 的「V2.1 横切任务」。

---

## 8. 文档回写计划（**执行中**：Step 1 ~ Step 7 设计均已回写）

> 回写节奏：**每步一个 PR 收口**，四处定义面（需求 / 架构 / `docs/README.md` / 本文）与 CHANGELOG 同批落地。
> ⚠️ 本文此前把本节标题停留在「Step 1 / Step 2 已回写」，实际 Step 3~5 均已回写（Step 4 见 `#32`、Step 5 见 `#33`/`#37`）——**本次一并更正**。

1. `requirements-spec.md`：
   - ✅ FR-18 原条目内修订（保留单编号）；FR-17 标注「部分交付 / 完整版 V2.1」；FR-15/16 扩展删除与原子性语义。
   - ✅ FR-26~33 / NFR-10~12 正式入表（含与 FR-15/16 的修订关系）。
   - ✅ **NFR-03 备注刷新为 225.5s**（P6 重测值，替换 P5 的 236.7s）；NFR-04 明确「快照 + 图加载」口径。
   - ✅ **2026-09-07 复审回写**：NFR-03 拆「首次全量 / 增量」双口径（D-J8）；**新增 NFR-13**（低选择度过滤延迟，D-J9）；NFR-11 口径改为「commit 后立即可查」；FR-31 优先级 Should → **Must**；NFR-10/11/12 的「对应步骤」按重排编号更新。
   - ✅ **Step 5 回写**（`#33` → v1.10；S5-04 标定定稿 → **v1.12**）：NFR-13 由「拟 ≤20ms」改为**双口径**（常规 ≤20ms / 降级字段 ≤35ms）+ 成本口径修正 + NFR-07 落地路径。
   - ✅ **Step 6 设计回写（→ v1.13）**：NFR-03 ② / NFR-05 / NFR-10 / NFR-11 补「设计已出」与设计锚点；⚠️ **NFR-10 数值目标缺失**（设计 §9.2 Q4）与 **NFR-03 ② 的 10% 单位**（Q5）登记待拍板；**§1.1 版本表补齐 v1.11 / v1.12 两行**（清「定义面漂移」）。**未修订任何指标**。
   - 🆕 **Step 6 评审收口（本轮 → v1.14）**：**NFR-10 口径按方案 A 修订**——① **前置条件**（各线程 `hits`（`chunk_id` + `score`）逐位等于单线程）+ ② **吞吐判据**（`--threads 4` QPS ≥ `--threads 1` × **2.5**，允许 40% 折损）+ **写明测量口径**（预热丢弃首轮 / 顺序交错 / 报告运行范围）；⚠️ **×2.5 标「拟」**，随 **S6-08** 实测定稿（同 Step 5 的 NFR-13 先例：v1.10 落「拟 ≤20ms」、v1.12 定稿）。**NFR-03 ② 的 10% 单位 = 文档数**（chunk 数只作参考、不进判据）。**这是 Step 6 唯一触碰 FR / NFR 的改动。**
   - 🆕 **Step 7 收尾回写（S7-05 → 需求 v1.18 / 架构 v1.17 / 本文件 v0.18）**：**NFR-12 定稿** + **NFR-05 回填精排内存实测**（增量 **2.64 GiB / 7.71×**）+ **NFR-06 更正 tie-break 机制句**（「保持输入序」→ **`chunk_id` 升序**）+ **架构 §14.5 按实测回填 R44 / 收窄关闭 R48 / R47 全部实测** + **锚点改符号指代** + **T8/T9/T10 本地实跑**（读数 `eval-report.md` §8.14.10 / §8.14.11）。
   - 🆕 **Step 7 设计回写（→ v1.16）**：**NFR-12 补三部分口径** —— ① **延迟判据 = 双列（均标「拟」）**：**端到端 `took` P50/P99** + **精排段 `rerank_elapsed` P50/P99**（**定稿 = 双口径**：**CPU/FP32 上界** 默认 `R=20` 下 P50 ≤ 8.8s / P99 ≤ 10.9s；**GPU/量化待测**）（口径 = **仅 `rerank()` 调用本身**，**不含**回捞与 `explain` 组装；独立口径、不并入 NFR-02；采集点 = `helix bench --rerank-window <R>` 的 **`took` 与 `rerank_elapsed` 两张直方图**）；② **质量判据** = hybrid **MRR@10(1)** 相对**同图**基线可测提升；③ **可观测** = `Metrics.rerank_window` / `rerank_elapsed` + `Explain.rerank_score.is_some()`（**不得静默**）。**「≤500ms（拟）」已废弃（2026-09-16，S7-05）**：实测最小档 `R=10` 的 P99 = **5220 ms**（差 **10.4×**）⇒ **CPU-only / FP32 / 未量化下不可达**；定稿改为**双口径**且**仅显式开启精排时适用**（默认关，D-S7-04）（同 NFR-13 先例：v1.10 落「拟」→ v1.12 定稿）。**NFR-05 补限定词「不含精排器」**（`model.onnx.data` ≈2,187MB 常驻；精排内存**单独立为架构 R44**）。**NFR-06 / NFR-07 补精排注记**（确定性：score 单调变换、`chunk_id` 序不变，⚠️ `BatchLongest` ⇒ 跨 batch 组成不可逐位复现；可观测：`Metrics.rerank_window` / `rerank_elapsed`）。**FR-18 补设计锚点 + 模型来源更正**（`rozgo/…`，非 BAAI 官方）。⚠️ **本版不新增任何 FR**。
   - 🆕 **Step 6 收尾（S6-10 → v1.15）**：**NFR-10 由「拟」定稿**（实测 bm25 **2.98×** ✅ / hybrid **1.63×** / vector **1.88×**）**并收窄适用范围** —— ② 只覆盖「**不含查询侧编码的读路径**」，且 **NFR-10 整体保持「打开」**（编码侧并发登记为架构 **R43**，**复审触发条件 = 查询侧会话池落地时**）；**NFR-05 补口径限定词「（检索路径，batch 1）」**（建库路径内存独立为架构 **R41**）；**NFR-03 ② 增量口径实测 23.705s < 27.973s** ✅；更正 NFR-10 备注措辞（评审 **P3-8**）。⚠️ **本版不新增任何 FR**。**S6-10 是 Step 6 收尾 PR，故本轮四处定义面 + CHANGELOG 一次落地。**
   - 🆕 **V2.1 计划修订（本轮 → 需求 **v1.19**）**：新增 **FR-34 / FR-35 / NFR-14 / NFR-15** 草案（**待评审**）；**FR-18 状态同步为「已实现并收尾」**（§5.2 摘要行此前停在「待评审」）；**FR-17** 补 Step 8 任务号与 NFR-14；**§1.1 与附录 C 两处版本表同批加 v1.19 行**。⚠️ **本版不修订任何既有指标数值**。

2. `architecture-design.md`：
   - 新增 **ADR-A**（图持久化格式 + 多文件原子性）、**ADR-010**（向量软删除 / 过滤下推 / 图持久化的架构决策）。
   - **第 14 章风险表 R1 补「已关闭」状态**（P5 已换 hnsw_rs，instant-distance 移除）；**p5-design 的 D7 结案**（图持久化落地）——**✅ 已由 V2 Step 2 完成**（2026-09-06）：ADR-A 方案 C，12K 完整冷启动 11.57s → ≈100ms，NFR-04 达标；R-P5-13 一并消解（图落盘即冻结拓扑，同快照两次加载逐位一致）。
   - ADR-002（instant-distance）在 ADR-010 落地时标废。
   - ✅ **Step 2~5 回写**：§14.1 R19~R25（v1.6）→ §14.2 R26~R30（v1.9）→ §14.3 R31~R35（v1.9/v1.11）。
   - 🆕 **Step 6 设计回写（本轮 → v1.12）**：**§14.4 新增 R36~R40**（R36/R37 **条件性**）；**无 trait / 结构变更**（`Embedder` 不改、`ConfigFingerprint` 不扩——EP 配置编码进 `embedder_id`，理由同 R35 的破坏性判定）。
   - 🆕 **Step 6 收尾（S6-10 → v1.14）**：**§14.4 由 R36~R40 扩为 R36~R43** —— **R36 / R37 消解**（T7-09 判「不投」；E2 +11.7%/+16.0% 而 RSS +96.4%/+289.2%、E3 +1.2%）、**R38 / R39 关闭**（S6-09 硬失败 + S6-T13 测试上锁）、**R40 上锁但残余仍在**（ID 单调有测试，但「不冲突」的证明仍依赖三条隐含前提）、**新增 R41**（建库路径峰值 RSS 与 NFR-05 的 372MB **差一个数量级**；预算按 `batch × 序列长度` 给、**只给形态不给数**）/ **R42**（依赖安全公告漂移：`bincode` / `paste`，**长期开放**）/ **R43**（**查询侧编码封顶并发**，**保持打开**，复审触发条件 = 查询侧会话池落地时）；§14.4 表头状态改为「**实现完成 + S6-10 收尾回写**」。**无 trait / 结构变更**。
   - 🆕 **Step 7 设计回写（本轮 → v1.15）**：**§14.5 新增 R44~R48**（按架构 §14.5 的实际条目）—— **R44** 精排器的**查询路径常驻内存**（`model.onnx.data` ≈2.19GB；⚠️ 与 **R41** 的**建库**路径峰值**不是同一件事**）／**R45** 精排**延迟主导 + 窗口被 `candidate_k` 静默夹取**／**R46** **`Hit.score` 语义随开关而变**（融合分 → `σ(logit)`，**R35 一族**）／**R47** **精排数值可复现性未证实**（`PaddingStrategy::BatchLongest`）／**R48** **512 token 截断使长段落判据失真**；+ §14 导读补 §14.5 指引与两条纪律（**R44 ≠ R41**、**R47 反例不必修**）；**§5.7 / §5.8 补 `candidate_window`（provided 方法，默认 `k`）与 `Explain.rerank_score` / `Metrics.rerank_window` / `rerank_elapsed`**。⚠️ **无 trait 破坏**（`Reranker` 签名不变），且 **`Config` / `SearchParts` 一律不改** —— 窗口走 **trait provided 方法**（**D-S7-02**），**不存在「新增字段」这回事**（本条初版写「`Config` 新增字段属向后兼容的加法」，与 D-S7-02 直接矛盾，已更正）。
   - 🆕 **V2.1 计划修订（本轮 → 架构 **v1.18**）**：**R34 / R43 的「残余」列补任务号**（分别落 Step 8 的 **T7-25 / T7-26**）、**R11 / R13 的「应对」列补 prefilter 落点**（Step 10 的 **T7-27** + **FR-34**）、**§14 导读补「V2.1 承接」段**（含「**V2.1 不新增风险编号 ⇒ R49+ 由 `v2-step8-design.md` 起分配**」）。⚠️ **不新增风险条目**；**§1.1 与 §15.2 两处版本表同批加 v1.18 行**。
3. `CHANGELOG.md` / README「已知局限」随版本发布更新。
4. 详细设计（ADR-A、H2/H3 等）在对应步骤开工时单独出 `pX-design.md`，不在本文件展开。

---

## 附-1：证据清单

- `crates/core/src/index/mod.rs:173` `remove` 只回滚倒排与墓碑，不含向量；`:247` `is_live_chunk` 定义了但未调用
- `crates/core/src/retriever/vector.rs` 无存活过滤；`vector/mod.rs` trait 无 `remove`
- `crates/core/src/query/filter.rs` 的 `allowed_chunks`（全表扫描）；`query/searcher.rs` 的融合后截断处（`take(...)`）再 rerank（⚠️ **2026-09-16 更正**：本行原用**本仓行号** —— `filter.rs` 的 `allowed_chunks` = `:39`、`take(k)` 曾记 `:196` → `:286`、`candidate_k` = `:118`、`rerank(...)` = `:316`；**实装后全部漂移**（实测 `allowed_chunks` = `:53`、`candidate_k` = `:149`、`take(take_n)` = `:337`、`rerank(...)` = `:375`）⇒ 按架构 §14.5 锚点规则**改符号指代**，历史行号仅作记录）
- `crates/core/src/embed/local.rs:67,75` `Mutex` 串行 embed
- `crates/core/src/search/index.rs` `remove` 后 `save()` 原样带走已删向量、`load_with` 全量重灌（注释「会被丢弃」不成立）
- `docs/devel/eval-report.md` §8 NFR 实测、§9 数据诚信、§10 残留问题
- 依赖源码（已核实）：`hnsw_rs-0.3.4` —— `parallel_insert`/`search_filter`/`FilterT`/`load_hnsw` 属实；图落盘为**双文件**（`HnswIo` + `DumpInit`，`hnsw.graph` + `hnsw.data`）；**无 remove/delete API**。`fastembed-6.0.2` —— `TextRerank`/`RerankerModel::BGERerankerV2M3`/`with_intra_threads` 属实；`bge-reranker-v2-m3` 含 `model.onnx.data` 外置数据文件。

- **2026-09-07 复审补充核实**：`fastembed-6.0.2`
  `src/init.rs:30-33` —— `intra_threads` 默认 `None` = 用满所有核；`src/init.rs:133` —— `with_intra_threads` 存在；
  `src/text_embedding/impl.rs:373` —— `embed` 单 session 按 `DEFAULT_BATCH_SIZE=256` 串行分块；
  `src/models/text_embedding.rs` —— **无中文量化变体**（量化仅 `NomicEmbedTextV15Q` / `ParaphraseMLMiniLML12V2Q`）；
  `src/models/reranking.rs:6-11` —— `RerankerModel::BGERerankerV2M3` 存在且带外置 `model.onnx.data`。
  `ort-2.0.0-rc.13` `Cargo.toml:145` —— 有 `coreml = ["ort-sys/coreml"]` feature。

- **2026-09-11 Step 6 设计期复核（**三处更正**，路径以 `v2-step6-design.md` 附录 C 为准）**：
  ① ⚠️ 上行「有 `coreml` feature」**容易读成"加个 feature 就能用"** —— 实际 **`fastembed-6.0.2` 没有 coreml 透传**
  （`[features]` 里只有 `directml = ["ort/directml"]`）⇒ 走 CoreML **必须把 `ort` 提为直接依赖**并开 `coreml`；
  ② ⚠️ EP 类型名 **不是 `CoreMLExecutionProvider`** —— rc.13 的真实路径是 **`ort::ep::CoreML`**
  （`ort/src/ep/coreml.rs:68`，`#[cfg(feature = "coreml")]` gate 在 `ort/src/ep/mod.rs`）；
  ③ 🔴 **路径更正（S6-10 复核，2026-09-13 —— 本条此前把两条锚点都写反了）**：
  `intra_threads` 在 **`src/init.rs`** 的 `InitOptionsWithLength`（**`:20`**；`src/text_embedding/init.rs:20`
  上的 `TextInitOptions` 只是它的**类型别名**）—— 原写的 `src/text_embedding/init.rs:33` **属
  `InitOptionsUserDefined`**（`:27`），**不是**我们走的路径（`TextEmbedding::try_new(TextInitOptions)`）；
  内部切分 `texts.chunks(batch_size)` 在 **`src/text_embedding/impl.rs:373`**（`:364` 是
  `batch_size.unwrap_or(DEFAULT_BATCH_SIZE)`）—— 原写「切分在 `:364`、非 `:373`」**恰好相反**；
  **且实际进入 ONNX 的 batch 是 64** —— 调用链在 `SearchIndex::flush` 的 `batch_size`(64) 处已切片
  （`search/index.rs:390`），内部 256 的分块**从不触发**。
  ⇒ **以上一律以 `v2-step6-design.md` 附录 C 为准**（该附录是本次源码复核的落点）。
  ④ 新增核实：`fastembed-6.0.2/src/lib.rs:89` 重导出 `ExecutionProviderDispatch`（只重导出**类型**，不透传 feature）；
  `src/text_embedding/init.rs:51/83/122` 的 `with_execution_providers` 确实存在。

---

## 附-2：步骤编号映射（2026-09-07 重排）

> 重排依据：2026-09-07 全量复审（调整项索引见 §附-3）+ 用户拍板 D-J8~D-J11。**Step 1 / Step 2 编号不变**。

| 新编号 | 主题 | 旧编号 | 变动原因 |
| --- | --- | --- | --- |
| Step 3 | 可靠性（原子快照） | Step 3 | 不变；范围按 ADR-A 重写 |
| Step 4 | 资源回收（compaction） | **Step 5** | **提前**（D-J10 / D3） |
| Step 5 | 查询性能与可观测 | —— | **新增**（D-J9 / D2：低选择度兜底提前 + Metrics 可观测） |
| Step 6 | 构建性能与多线程 | **Step 4** | 顺延（D-J10 / D3）；embed 并行改 spike 制 |
| Step 7 | 精排 | Step 6 | 顺延 |
| Step 8 | 读写并发 | Step 7 | 顺延 |
| Step 9 | 相关性深化 | Step 8 | 顺延 |
| Step 10 | 场景机制 | Step 9 | 顺延 |

**历史文档的编号**：`v2-step1-design.md` / `v2-step2-design.md` 中的 Step 编号**指重排前**，
已在各自文档头部加注记；`plan.md`（V1）与 `p0~p6-design.md` 不受影响。

---

## 附-3：2026-09-07 复审 —— 调整项索引与建议执行顺序

> 复审当时的结论不另立文档，本表保留**可追溯性**：每条调整项对应本文的落地位置。

### A1~A7 索引

| # | 调整项 | 优先级 | 落地位置 |
| --- | --- | --- | --- |
| **A1** | Step 3 范围/验收按 ADR-A 重写（快照**本体**至今非原子，崩溃 = `SnapshotCorrupted` = 索引丢失，不是降级） | 高 | §4 Step 3（S3-a~e） |
| **A2** | Step 4（现 Step 6）的技术前提有一半被证伪（`intra_threads` 默认满核、批次已测、并行建图仅占 4%）⇒ embed 并行降级为 spike，并纳入从未评估的 **CoreML EP** | 高 | §4 Step 6（E1/E2/E3） |
| **A3** | NFR-03 `<120s` 无已识别路径可达 ⇒ 需求侧决策 | 高 | §2.3 D-J8、§4 Step 6 双口径、需求文档 NFR-03 |
| **A4** | 4 个 S 级任务插队：parallel 默认翻转 / 低选择度兜底 / `Metrics` 可观测 / 工程卫生 | 中 | §4「横切任务」T7-21、T7-24；Step 5 的 T7-22、T7-23 |
| **A5** | Step 5（现 Step 4）补「compaction 后必须重发 manifest」+ workload 脚本，并提前 | 中 | §4 Step 4 |
| **A6** | Step 6（现 Step 7）补三条前置：复现基线 / 模型下载可行性 / H3 未决 | 中 | §4 Step 7 |
| **A7** | 计划文档自身一致性（版本状态、进度表、NFR-04 数字、FR-31 优先级、NFR-11 口径） | 低 | §2 / §6 / §7 / 需求文档 |

### 建议执行顺序（含横切任务）

| 顺序 | 内容 | 量级 | 决策点 / 依赖 |
| --- | --- | --- | --- |
| 1 | **Step 3** 原子快照（S3-a~e，含 R19） | S | 无；排最前（Q-C3 定级「高（正确性）」） |
| 2 | **T7-24** 工程卫生（CI 触发 / gitignore / 分支清理） | S | 无 |
| 3 | **T7-21** `parallel_build` 默认翻转 | S | 独立 PR；须同步改 S2-T22 |
| 4 | **Step 5** T7-22 低选择度兜底 + T7-23 `Metrics` 可观测 | S | 新增 NFR-13；T7-23 是 Step 6 前置 |
| 5 | **Step 4** 墓碑回收（含 manifest 重发 + workload 脚本） | M | Step 1 |
| 6 | **Step 6** spike（E1/E2/E3）→ 视数据决定是否投 M 级 | S → M | NFR-03 按 D-J8 新口径 |
| 7 | **Step 7** 精排 | M | ✅ **三条前置已结清（2026-09-14）**（复现基线 → 发现图漂移 / 模型已实下载验哈希 / H3 已取形）；**设计已出、待评审** |

> 排序原则：**正确性与确定性优先于性能**；收益不确定的（A2）排在确定的之后。

### 跟踪缺口（待用户决定，未在本轮处理）

- Step 3 ~ Step 7 目前**只有 Step 6（构建性能）有跟踪 issue**；标签只有
  `V2-Step2 / V2-Step4 / V2-Step6 / V2-Step6+`，**缺 Step 3 / Step 5**，
  且 `V2-Step4` 的语义已由「构建性能」变为「资源回收」⇒ 重命名会动到历史 issue。

---

## 附-4：2026-09-16 V2.1 计划审视 —— 结论索引与落地位置

> 背景：V2.0（Step 1~7）全部合并（main `62acb37`）后、V2.1 开工前的一次**计划面审视**。
> 结论**不另立文档**（体例同 §附-3），全部落进本文与另三处定义面；**代码零改动**。
>
> **一句话结论**：**V2.1 的「范围」没错，但它的「计划密度」比 V2.0 低一个档** —— 三节各只有
> 「任务两列表 + 依赖/风险/量级三行」，既无 §4.0 式 H 项、也无含数字的验收门、也无 PR 切分；
> 更硬的是**风险表与任务表不闭合**（见 P0-1 / P0-2）。

| # | 结论 | 级别 | 落地位置（本次修订） |
| --- | --- | --- | --- |
| **1** | **R34 自指空洞**：架构 §14.3 R34 写「**归 Step 8**」，而 Step 8 的任务表**没有它**。R34 = 精确扫描遍历在**层切换时递归读**同一把 `RwLock` ⇒ 写者等待时可**永久挂死**；现「不可达」仅因单写者（`add` 要 `&mut self`） | **P0** | §4 Step 8 的**依赖行硬前置**（先于 T7-06；⚠️ **不是 H 项** —— H4~H7 是「开工前置」、T7-25 是「任务依赖」，两套机制） + §4 Step 8 新增 **T7-25**（且标「必须先于 T7-06」）+ §7 进度表 + 架构 §14.3 R34 残余列 |
| **2** | **R43 同款空洞**：R43 写「复审触发 = **查询侧会话池落地时**」，而会话池属读路径并发 ⇒ 属 Step 8，但 Step 8 无此任务。两处互相指认、无人接 | **P0** | §4 Step 8 新增 **T7-26**（含挂起的 Q3）+ §4 横切表标注「R43 不在此列」+ 架构 §14.4 R43 残余列 + issue **#58** |
| **3** | **架构许过的「V2.1 prefilter / 排序列方案」在计划里没有任务或编号承接**（架构 **5 处**指向它：§14.3 R11 / R13、§5.5、§8.3、ADR-010 代价段）⚠️ **更正（第 1 轮评审 P3-2）**：原写「**0 命中**」不准确 —— `plan-v2.md` §8 的 T7-23 条目**有 1 处判据句**提及 prefilter，`requirements-spec.md` 全文才是 0 命中；**实质结论不变**（那只是判据指针、无任务 / 编号承接） | **P0** | §2.1 范围 + §4 Step 10 新增 **T7-27** + §5 新增 **FR-34** + 架构 R11 / R13 应对列 |
| **4** | **Step 9 在需求面是空的**：§5 只给了 Step 8 / Step 10 的编号，Step 9 的 T7-14/15/16 **一个 FR / NFR 都没有**，而 §6 却要求「在分桶上优于固定权重」 | **P0** | §5 新增 **NFR-15**（+ FR-35）+ §4 Step 9 补可证伪验收（含统计口径要求） |
| **5** | **Step 9 会动评测资产，但没定新旧基线关系**：T7-15 重建 qrels、T7-16 换 query 集 ⇒ 一旦替换，P5 锚点 / Step 7 的 `R = 20` 标定 / NFR-12 的 8.8s / 10.9s **全部不可横比** | **P0** | §4.0 **H7** + §4 Step 9「评测资产纪律」 |
| **6** | **Step 8 缺「读不阻塞写」的语义**（读不取写锁 / 写不抬高读延迟 / 读能看到未 commit 的写），且 **NFR-11 的写延迟目标值仍是空白**（现有 1.14 s/批是从累计 embed 耗时**推算**的） | P1 | §4.0 **H5 / H6** + §5 新增 **NFR-14** |
| **7** | ⚠️ **口径冲突**：FR-17 §5.3.5「**写入后**立即可查」 vs NFR-11（Step 6 定稿）「**`commit()` 后**立即可查」 | P1 | §4 Step 8 开工前置 + `requirements-spec.md` §5.3.5 注记 + **NFR-14 ①** |
| **8** | **V2.1 没有 §4.0 式 H 项**（V2.0 的 H1 逼出 ADR-A、H3 逼出 `R` 的标定）；Step 8 至少需要 **H4**（多段树 manifest / 原子发布 ⇒ **反向改动 ADR-A 方案 C**） | P1 | §4.0 新增 **H4 / H5 / H6 / H7** |
| **9** | **Step 10 的三个后处理任务会再撞 Step 7 的坑**：MMR 需要比 k 大的候选池、token budget 要按序裁剪、多 query 融合要进融合阶段 ⇒ 都碰 **R45**（窗口被 `candidate_k` 静默夹取）；T7-20 还叠在 **R46**（`Hit.score` 语义随开关而变）之上 | P1 | §4 Step 10 交叉注记（两条 ⚠️ 段） |
| **10** | **T7-16 应是 T7-14 的前置**（而非相反）：现 qrels 来自人类日志（Q-R4 明说缺 Agent query），而「在**分桶**上优于固定权重」需要**分桶依据** | P1 | §4 Step 9 依赖（顺序修正）+ §7 进度表 |
| **11** | **FR-18 状态两处没跟上 S7-05**：`plan-v2` §5 与 `requirements-spec` §5.2 的 FR-18 行仍写「设计已出（2026-09-14，v0.1，**待评审**）」（同文档 §5.3.6 详情行已更新、摘要行漏改） | P2 | `plan-v2` §5 FR-18 行 + `requirements-spec` §5.2 FR-18 行 |
| **12** | **#24 / #25 未关**：T7-21 / T7-24 已随 PR #49 / #48 合并、§7 已标 ✅，issue 仍 OPEN | P2 | 本次**不开 PR 处理**（仅口头提示，见 CHANGELOG 的「已知遗留」）；按纪律**不擅自关**他人 issue |
| **13** | **V2.1 缺「横切任务」区**（V2.0 有）⇒ 排队但无归属：#54 / #50 / #42 / #34 / R41 / R44 / R40 / #2 | P2 | §4 新增「**V2.1 横切任务**」小节 |
| **14** | §6 的 V2.0 门槛最后一条（`make fmt && lint && test && deny` 全绿）**仍未勾** ⇒ V2.0 的「关闭面」未收口 | P2 | §6 V2.0 门槛末条改为 `[x]` + 核实说明 |
| **15** | **Step 8 是 L 级 + 高风险 + 含 ADR 级改动，但只有 2 个任务行、无 PR 切分** | P2 | §4 Step 8 补「PR 切分建议」（6 段） |

> **本次修订的边界（评审请重点核这三件事）**：
> ① **新增的四条需求编号（FR-34 / FR-35 / NFR-14 / NFR-15）全部是「草案」** —— 数值一律留白、标「待评审 / 设计期定值」，
> **未触碰任何既有指标**（NFR-02~13 的阈值、预算、时限一字未动）；
> ② **T7-27（prefilter）的「投 / 不投」没有被预先承诺** —— 写成 spike + 决策门（同 Step 6 的 E1/E2/E3 先例）；
> ③ **R34 / R43 / R11 / R13 / R17 / R18 一律不标「已解决」** —— 本次只把「谁在哪一步接」写实，不改风险状态。
